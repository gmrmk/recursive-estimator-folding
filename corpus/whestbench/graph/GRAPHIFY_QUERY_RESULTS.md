# Graphify traversal results and audit

Generated locally with Graphify 0.7.13. The initial semantic extraction used
the installed Ollama `llama3.2:latest` model and no external service. It was too
lossy (5 nodes, 4 edges), so it is retained only as a diagnostic. The operative
graph was rebuilt from the proved/measured campaign ledger with NetworkX, then
clustered and traversed by Graphify: 41 nodes, 45 edges, 8 Graphify communities.

## Path query

Question: shortest path from `Layerwise Hermite defect sources` to
`Winning legal adjusted score`.

```text
Layerwise Hermite defect sources
  --supplies_features [INFERRED]-->
H1 equivariant learned residual closure
  --high_upside_path_to [INFERRED]-->
Winning legal adjusted score
```

Both edges remain hypotheses. The path is useful because it identifies a
minimal falsifier; it does not validate the estimator.

## H1 explanation

Graphify placed H1 in community 2 with degree 10. Its neighbors are:

- connected finite-width four-point vertex;
- winning adjusted score;
- dyadic depth-memory features;
- permutation, gauge, and O(256) quotient;
- full-covariance Gaussian closure;
- grouped-CV residual R2 > 0.965 gate;
- morphogenesis translation;
- renormalization translation;
- layerwise Hermite defect sources;
- gauge-fixed contraction edges.

Audit: the high degree is partly designed because H1 is a proposed synthesis.
Betweenness is therefore a prioritization heuristic, not independent evidence.
The R2 gate is the decisive edge.

## Next-test query

Graphify's breadth-first query connected the promoted sample-count mutation to
the random 32,256 estimator, its zero-failure gate, and the target. It also
reached H1 through the private-generalization symmetry quotient and recovered
the full-covariance baseline, Hermite defects, gauge-fixed edges, depth memory,
and the 0.965 falsifier.

Audited decision: keep random 32,256 as the deployable fallback; run a small
grouped-whole-MLP ridge premise for H1 before any GNN. The optimistic rank-5 k4
ceiling subsequently failed downstream sign stability, so H3 is rejected. Do
not revive H2 sign prediction unless it passes independent-scramble transfer.

The H1 premise subsequently reached only OOF residual R2 0.662672 versus the
0.965 gate and is also rejected. The path query records why it was tested; it
must no longer be read as a live route to the target.

## Community-level hyperconnections

1. Deployment community: score, sample count, budget failures, private
   generalization, and symmetry quotient.
2. Control-failure community: sign-changing oracle blends, scramble noise,
   approximate-mean and mediant no-go results.
3. Learned-closure community: H1, Hermite sources, invariant edges,
   morphogenesis/message passing, and renormalized couplings.
4. Analytic-bottleneck community: full covariance, terminal oracle,
   terminal analytic failure, copula failure, and the four-point vertex.
5. Cubature community: radialization, high-degree residual, MUB design,
   frame annihilation, and budget law.
6. Tensor community: H3, rank cost, rank-5 falsifier, and tensor-network
   translation.
7. Multiscale-memory community: retinal residuals, memristic depth memory,
   and fractal/tau dyadic scales.
