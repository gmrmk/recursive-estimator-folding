# M169--M177 publication graph addendum

Status: response-free graph fold. This addendum records mechanism relations only; it does not read outcomes, change the champion, or authorize a submission.

The new graph branch is deliberately typed into three non-interchangeable failure classes:

- **Structural lower bound:** M170 proves that the audited dense M166 normal form needs seven product families. Dense batching can reduce dispatches but cannot lower this rank. The separate triangular-kernel escape remains unresolved.
- **Coordinate/certificate failure:** M171 kills the fixed physical-coordinate panel, while M173 repairs one near-parallel transverse chart by scaling the boundary layer. The repair is conditional and not an all-PSD provider.
- **Interface/liveness failure:** M169 removes the 155-call wall conditionally; M174 shows no actual caller produces the labelled retained state that M169 assumes. M172 consequently remains static-algebra-pass but development-sealed.

The failure curve therefore migrates from algebraic support, to dispatch, to state provenance. The next evidence-bearing repair is an explicit bounded-lifetime labelled `W, mu, V, J, source` ABI, not another dense polarization or an ungraded endpoint panel.

## M175--M176 dependency closure

M175 tests the fixed response-free B=8 schedule proposed by M174. The schedule is dependency-coherent in principle, but current code cannot lawfully populate it. Its two blockers are deliberately separated:

- the first link is an exact labelled zero-order BackgroundArchive/Jacobian producer;
- the second, independent link is a layer-owned `Source211 -> TangentState` formula.

M176 pins the first link's recurrence and complete local Jacobian, then proves the implementation no-go: the installed path has no endpoint-complete, FlopScope-metered bivariate ReLU value-and-Jacobian primitive. Consequently, the graph contains a real dependency loop:

```text
dispatch fusion -> staging ABI -> exact background producer
                                  -> bivariate endpoint calculus
                                  -> valid labelled staging values
```

This loop changes the next mutation target. Memory blocking is no longer the first unresolved mechanism; the bivariate value/Jacobian primitive is. M175 and M176 remain audits with no resource, source-variance, or efficacy credit.

## M177 and the master postmortem coordinate

M177 closes the logical PSD-classification gap without pretending that a numerical service exists. Its dispatcher covers non-PSD refusal, SPD interior, positive/negative rank-one endpoints, one-zero-variance faces, and the fully deterministic face. The endpoint algebra and feasible one-sided tangent policy survive.

Runtime construction remains formally blocked. Installed FlopScope exposes billed univariate `Phi/phi`, but not certified numerical remainders and not a `Phi2`/Owen-`T` primitive. The known `556` FLOPs per unordered pair is therefore only a strict floor before the missing special-function work. Charging it as the complete cost would omit the binding provider.

The first remaining numerical link is a fixed or deterministically bounded, billed `Phi2`/Owen-`T` evaluator carrying value and derivative remainder bounds on the scale-normalized domain. A pass would open only M177's bivariate value/Jacobian primitive and then M176; it would grant no source, variance, resource, efficacy, or champion credit.

The master failure postmortem supplies the representation key:

```text
SourceAtom(owner, PSD chart, feasible tangent, coefficient, remainder)
```

This is the graph's synthesis node. `owner` prevents collision double count, `PSD chart` selects the lawful endpoint formula, and `feasible tangent` prevents a path-underdetermined face derivative from entering the carrier. The key is topological compression, not a claim that the resulting atom inventory is affordable.
