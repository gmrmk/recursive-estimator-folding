# Erratum 1 to the V31 V5-d3 grouped proposal (2026-08-11)

Status: **REPAIR BEFORE SOURCE CONSTRUCTION**. This is an append-only correction
to `CODEX_V31_V5D3_G4B1152_U1_PROPOSAL_20260811.md`, SHA-256
`8E21F282F939C2BFD9ED1EFE59E881DC88F075B16A0BC8E200CF367060A2BA42`.
It authorizes no execution.

The proposal's displayed V5 arithmetic, seed-11 class census, B1152 arena
ledger, and B4096/B1152 workspace projections reproduce exactly. The following
claims and contracts must be corrected before a child source or preexecution
manifest is sealed.

## E1. Seeds 21--25 are repository-bound

The proposal states that the seeds 21--25 raw width sequences and generator are
absent. They already exist at:

- `experiments/uf1_attack_judge/j2_eligibility.json`, SHA-256
  `6192B23D61F25CCD4AB95FCBBB83D6E8E379C1F3AAC06A8047DCF5F07A1E2AD6`;
- `experiments/uf1_attack_judge/j2_eligibility.py`, SHA-256
  `8FB2A0D99FEC13250EA3B5AE7374D22A30D9B4A353929DB1E2445C92FCDAD710`.

All ten stored shape tapes are therefore repository-replayable. Regenerating
seeds 21--25 is not hermetic because the interpreter/package environment is not
bound. Separately, the new V5 aggregate replay still needs a committed
calculator and per-hook receipt; raw tape provenance alone does not prove the
printed V5 totals.

## E2. B4096 is parked pending official memory rules

`V5D3-G4B4096` projects about `701.727 MiB` and 385 calls; B1152 projects
`495.553 MiB` and 1,078 calls at the same analytical V5 bill. B4096 is killed
only by the proposal's retained self-imposed 512-MiB gate, not by a published
Phase-2 rule. Its correct status is `PARKED_PENDING_OFFICIAL_MEMORY_RULES`.

If the official limit safely permits the larger live set, B4096 has the better
static call schedule. If a 512-MiB-class cap binds, B1152 is the surviving
orientation. Each is a separate child because changing atomic block height can
change float32 association; there is no post-result switching.

## E3. Existing M116 is an arithmetic primitive, not the child schedule

Replace "executable V5 schedule" with "target V5 arithmetic/movement class."
M116 implements only the square core, requires writable contiguous storage,
has no ragged correction, no immutable-parent fallback, and no phase-overlaid
arena. It is supporting component evidence, not an integrated implementation.

## E4. Freeze the dispatcher domain

The equal fourteen-group schedule exists only for production geometry. V5 may
run only when all of the following hold:

```text
ctx.width == 256
m == 64_512
kc >= 8
nc >= 8
V5(m,k,n) < owned_batched_candidate_bill(m,k,n).total
```

If either `kc < 8` or `nc < 8`, or any condition above fails, replay the parent
dispatcher byte-for-byte. Small validation contexts and other row counts remain
parent paths unless separately specified and audited.

The printed fixture tuple must be written
`(m,k,n)=(64_512,253,255)`; its current comma formatting is ambiguous.

## E5. Source prerequisites

Before source construction, the manifest must bind all of these seams:

1. move the predict-time activation into setup so group views are genuinely
   prebound;
2. adapt banks to rectangular `kc,nc` slices and physical row stride 256;
3. replace the old disjoint/C-contiguous ownership rule with a phase-aware
   interval and lifetime proof;
4. prove WHT/V5 arena exclusivity across repeat predictions and exceptions;
5. preserve correction-before-core and tail-before-core chronology;
6. preserve the parent's 4,096/3,072 fallback partition and counters;
7. vendor and hash all source instead of importing an absolute-path draft;
8. account for setup reshape/view work under the eventual official meter.

## E6. Zero-reference comparisons

When the float64 reference norm is zero, use the already sealed absolute/Frobenius
error bound. Require bitwise zero only for explicit zero-operand fixtures. A
valid float32 reassociation can leave a small rounding residual after exact
cancellation. Apply the same rule to a zero-GUARDS-norm comparison.

## Disposition

The family remains zero-evidence and cost-promising. Do not construct source,
run a generated network, or choose B4096/B1152 until the official Phase-2
meter and memory policy are bound and this erratum is incorporated into a new
preexecution manifest. GUARDS remains the sole integrated candidate.
