# Headroom-recursion packet — Generation 6 (2026-08-10, post-convergence, owner-corrected)

Produced by the headroom-recursion pass at the campaign's fixed point,
incorporating the owner's two corrections: (1) the entropy is COMPUTATIONAL,
not ontic — pseudo-randomness, not randomness; (2) the roads are bound — one
S(B) curve, multiple attack points, not a single ended road.

## Champion state
- Kerdock v3.1 GUARDS: live #326094 adjusted 1.832e-7 (raw 2.818e-7, C/B
  0.650); hardened tar sha256 8382e269...ea06 staged for the Phase-2 flip.
- Score distribution: per-net 11x tail = rotation-draw chi2_1 speckle;
  zero measured bias (N8c); no fitted component; on-budget with headroom.
- Evaluator: whestbench pinned v0.14 CLI, flopscope v0.10.0 cost model.

## Ledger state (240 records)
- PROMOTED: h4 parent, row-blocked Winograd, Kerdock v3, terminal fold
  (+4.8% billed), pilot-rescued pruning (+25.1% billed, adjusted-optimal),
  v3.1 M186/M187 guards.
- KILLED: 7 root-cause families (see FAILURE_MODE_GRAPH), all with named
  causal boundaries; every kill final.
- UNRESOLVED/RUNNING: S16 (residual-decomposition confirm, expected
  CONFIRM-identity), S17 (the S(B) curve + IBC floor — the reopener).

## The corrected god-node (the packet's core)
The residual is a deterministic function of POSSESSED weights that behaves as
strong PSEUDO-randomness against every sub-budget test we constructed. The
seven failure families certify PRNG strength, not information absence. Define
S(B) = minimax achievable MSE at B FLOPs; the campaign has measured points:
cheap observables extract ~0 (S15 1.56%); analytic closures extract deg<=2
exactly then plateau (9.6e-5); design sampling rides the 1/N line (us:
2.8e-7 at 0.65B); B=inf gives 0. The unprobed region between the closure
plateau and the sampling line (~380x wide at our budget) is enterable ONLY by
seed-side methods — estimators that use the weights to un-randomize structure
at the source instead of testing the output.

## Residual-error correlation among survivors
The exact-control (seed-side) class is the ONLY surviving class whose error
law is not champion-correlated speckle (S9 measured r~0.05 for the one
built exemplar). Decision-layer instruments (S1/S4) operate on the score
distribution, orthogonal to both.

## THE ONE NEXT-MUTATION PROPOSAL (per the skill: exactly one mechanism)
The recursion does NOT invent a new arm — it endorses and SHARPENS the one
already queued, which is the unique legal continuation:

  **M245 (Sol's weighted-Galerkin spectrum), re-motivated as the seed-side
  PRNG attack**: its mission, stated in the corrected frame, is to measure
  how much of the computational entropy can be un-randomized per FLOP by
  exact source-level structure — i.e., to measure the seed-side segment of
  the S(B) curve that no output-side method can reach. S17 (running) supplies
  its quantitative target: the gap between the sampling floor and the
  absolute floor. If M245's spectrum shows fast (geometric) capture, the
  seed-side road is real and Gen-7 builds on it; if it crawls, S(B) is flat
  between the plateaus and the campaign is COMPLETE at the floor.

Constraints carried: blade-gated (Sol predeclares, adjudicates); fable runs
the long shards on the committed hash-bound V2 trigger; no output-side
estimator mutation (fixed point certified); no attack on the max-entropy
speckle by sub-budget observables (proven futile 7 ways).

## Holdout firewall (restated)
No truth/scorer/private-target reads; synthetic nets + cached MC only. The
Sep 20-30 private fresh-seed re-run is the untouched holdout and is never
exposed to mutation generation. The champion's zero-bias/no-fitted-component
construction is specifically what makes this firewall COSTLESS to us.

## Verdict on the convergence certificate
CONFIRMS with amendment: the fixed point holds for the output-statistics
road; the system is not one road. The bound-together statement: sampling
(optimal, done), decision layer (measured, done), seed-side (M245, open,
now sharply motivated), and the floor (S17, computing). The recursion's
next iteration is Sol's.
