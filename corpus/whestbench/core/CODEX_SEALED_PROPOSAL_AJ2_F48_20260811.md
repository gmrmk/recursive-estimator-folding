# Codex sealed proposal AJ2-F48: cost-unproven worst-envelope reflection

_Private pre-execution proposal record, 2026-08-11_

## 📋 Authority and disposition

This is the active static proposal. It has zero generated-network, synthetic,
truth, hosted, leaderboard, or score evidence. It is not an implementation, cost
PASS, resource PASS, scientific PASS, candidate, submission, or execution
authority.

Normative parent:

- path: `CODEX_SEALED_PROPOSAL_AJ1_V2_20260811.md`;
- SHA-256: `D792C67DF0C8933BAF7EFA7CDD3836F8D48B62DF72DCECB175F3048D7F387A90`;
- size: 59,472 bytes and 1,212 LF lines.

Immutable incumbent/fallback archive SHA-256:
`8382E269C9B32E0935492734DDF8182560120F7E9331621AA18839D5D1F4EA06`.

The preceding F56 record is static-killed and non-normative. No F56 synthetic,
scientific, candidate-gate, truth, score, or hosted evidence transfers. F48
independently rebinds the common dispatcher source and static finite-domain bill
below; that reusable source constraint is not F56 empirical evidence. F48 is a
fresh family selected before any synthetic or scientific observation.

Phase-2 rules remain unbound. The organizer states that Phase 2 opens on
2026-08-18 at 00:00 UTC and that its details will be posted separately nearer
launch.[^phase2] The image, versions, architecture, budget, score, numerical
policy, wall/RSS, descendant-process meter, quota, and designation rules must be
bound before source construction receives a static PASS. Incompatibility kills
F48; it never resizes or retunes itself.

GUARDS remains the only integrated artifact and is never modified by this work.

## 🎯 Frozen F48 geometry

Freeze primary `d=n=256`, depth `L=32`, discovery band `calL={1,...,8}` with
`omega_l=1/8`, and rank `r=1`. Depths 8 and 16 are transfer controls only.

Freeze two fixed production halves:

\[
h=48,\qquad
D_A=\{0,\ldots,47\},\qquad
D_B=\{63,\ldots,110\}.
\]

Omitted frames are

\[
D_{omit}=\{48,\ldots,62\}\cup\{111,\ldots,125\}.
\]

Each selected frame retains all 256 directions, every antipode, and equal weight.
Each half therefore has 12,288 base directions, 24,576 antipodal production rows,
and exactly six 4,096-row blocks. Total production geometry is 96 frames and
49,152 rows. No subset, row, weight, antipode, or frame is selected from weights,
Pi, production `Q`, truth, timing, cost, score, or a result.

F60, F56, and any other count are separate families. They may not be screened on
numeric output and then compared with F48. After manifest seal, any frame change
is AJ3 and starts from zero.

## 🔭 Honest pre-evidence forecast

There is no positive empirical anti-J signal. F48 also carries a larger nominal
variance burden than F56. The deliberately skeptical subjective planning forecast
for complete adjusted-score ratio versus W0 is:

- subjective 90% interval: `[0.80,1.60]`;
- planning median: `1.15`;
- conditional winner tail, only after premise and Arm-1 PASS: `[0.15,0.50]`;
- honest-tier stretch target: pointwise upper-90 ratio `<=0.20`.

At W0's approximate `1.8321e-7`, the unconditional interval is approximately
`[1.4657e-7,2.9314e-7]`, the conditional tail is approximately
`[2.7482e-8,9.1605e-8]`, and ratio `0.20` is `3.6642e-8`. These are subjective
decision forecasts, not measured confidence intervals, and not literal public
number-one arithmetic.

Before any F48 execution, bind

```text
r_board = best officially eligible adjusted score / revalidated W0 score
```

with source, timestamp, eligibility rule, score law, and both scores. A number-one
claim additionally requires
`upper98.5714[complete adjusted-score ratio Arm2/W0] <= r_board`, using the same
predeclared seven-gate bootstrap bound below. External rules may bind only
previously symbolic legality, billing, and score fields; they cannot retune
scientific choices.

## 🔗 Closed-world inheritance

The effective specification is the exact D792 parent plus this child. This child
wins only on the enumerated overrides below; any other conflict is fatal.

Inherited unchanged:

- D792 lines 94-108: model convention, architecture, layer band, and rank;
- lines 113-162: strict deterministic external chart and hidden gauge law;
- lines 164-190 and 199-297: randomness boundary, exact numeric role IDs, tuple
  construction mechanics, iid fixed-radius folds, Pi, held-out/deployed `Q`, and
  `O(d)` convention;
- lines 339-390: cross-fitted residual operator and matrix-free action;
- lines 392-423 and 517-582: six-action dense Rayleigh--Ritz, direct residual,
  controls, aligned projector, and left Householder action;
- lines 425-515: deterministic serial/parallel scheduler, task shards, separate
  meter receipts, barriers, process-tree accounting, and score-bearing isolation;
- lines 807-824: exact float64 runtime-data rank-one kernel, normalization,
  reduction order, dtype/layout, nonfinite/orthogonality checks, no specialization,
  and `c=0` fixture; only its old 63/126-frame fixture is excluded;
- lines 584-671: independent fixed-direction transfer premise gate;
- lines 873-970: truth replicas, signed bias, raw MSE, kappa reduction,
  same-invocation mean-of-products adjusted score, and ratio-domain checks;
- lines 1000-1057: panel seeds, pairing, atomic bootstrap, multiplicity mechanics,
  and fail-closed missing/nonfinite policy;
- lines 1092-1129: success-path gauge/permutation predicates and evidence labels.

Explicitly excluded rather than silently inherited:

- D792 subjective forecast and old board arithmetic at lines 48-92, except the
  rule that `r_board` must be bound before evidence and the separate honest-tier
  stretch label;
- every 63/126-frame core, half, fixture, arm, and cost clause;
- universal late-W0 fallback, `.20*C_W0` setup cap, and AJ1 reserve equations;
- AJ1 Arm-1 thresholds and five-gate promotion family;
- AJ1 stream-version, family name, arm codes, fallback codes, and archive names.

Freeze `AJ2_F48_STREAM_VERSION=4` in place of the parent's version word. Reuse
the parent's role constants prospectively but no AJ1/F56 values. Freeze

```text
panel_code = {
  provider:0, premise:1, confirmation:2, depth8:3, depth16:4,
  depth32:5, hostile:6, synthetic_contract:7, truth:8
}
```

and freeze

```text
arm_code = {W0:0, F48_I:1, F48_AJ:2, F48_R:3}
branch_code = {
  SUCCESS:0,
  EARLY_W0:1,
  PRODUCTION_RESERVE_REJECT:2,
  LOCAL_NUMERIC_REJECT:3,
  WHOLE_GUARD_REJECT:4,
  PROVIDER_FAILURE:5
}
```

The RNG tuple retains the parent's exact fixed-width word schema with its first
word replaced by version 4; proposal hashes are not silently inserted as extra
PRNG words. The manifest, stream-helper source, task namespace, and receipt bind
the exact parent/child hashes plus arm, branch, panel, network, depth, role,
replicate, sample, block, and action. There is no silent seed reuse, rename,
field-width change, or mutable spawn order.

## ⚙️ Provider, halves, and reducer-visible outputs

Arm 0 is one direct invocation of the immutable 126-frame W0 archive on the paired
production rotation. Its output, branch, FLOPs, wall, RSS, and guards are one
atomic same-invocation record.

Arms 1, 2, and R share one F48 core. Their runtime-data transform kernel is

```text
Q_B = Q - 2*c*v*(v^T Q).
```

Freeze:

```text
Arm 1: core(Pi, Q, Q_B), c=0, v=u.
Arm 2: core(Pi, Q, Q_B), c=1, v=u.
Arm R: core(Pi, Q, Q_B), c=1, v=u_random.
```

Arm 1 executes and bills the complete `c=0` rank-one kernel; it is never
constant-folded or aliased to `Q`. `Q_B` acts only on `D_B`; `Q` acts only on
`D_A`. `Pi`, chart, operator folds, `u`, and the first unredrawn random-control
axis are independent of held-out production `Q`. A wrong-side `Q@R`, second QR,
dense unbilled replacement, outcome-selected axis, or changed subset is failure.

The inherited executable kernel is binding: cast `Q,v,c` to float64, normalize
`v` in increasing-coordinate order, form the rank-one update in the parent's
exact order, cast back to Q dtype/layout, reject nonfinite or nonpositive norm,
and require float64 `||Q_B^TQ_B-I||_F<=1e-4`. No child clause weakens those checks.

Pi alone owns every prune, fold, active, rescue, demotion, kink, and tangent shape.
Production values cannot choose those shapes. A final whole-output guard may read
the combined output only through the enumerated branch contract below.

Let `S_A(Q)` and `S_B(Q_B)` be the two manifest-bound half sufficient-statistic
records before the shared tangent correction. Freeze a Q-independent Pi-owned
affine map `T_Pi` and require, exactly in the declared arithmetic,

\[
Y_A=T_\Pi(S_A),\qquad Y_B=T_\Pi(S_B),
\]

\[
Y_C=T_\Pi\!\left(\frac{S_A+S_B}{2}\right)
=\frac{Y_A+Y_B}{2}.
\]

The production provider computes the combined map once and applies one whole
guard. It never finalizes or guards halves independently. The sealed scientific
harness may reconstruct `Y_A,Y_B` from retained sufficient statistics and the
same frozen affine coefficients; that diagnostic work is separately metered
experimental cost and cannot affect provider output or bill. If the tangent or
terminal correction is not affine under this exact decomposition, F48 fails
statically.

Arms 1/2/R use identical call graph, transform operations, shapes, allocations,
and source bill on guard-quiet success. They run in fresh processes with no
cross-arm cache. Measured wall/RSS remain arm-specific.

## 🛡️ Analytic completion and Q-independent reserve gate

Before chart, Pi, operator, or production, construct a complete scorer-shaped
Q-independent analytic fallback `Y_an(W)`. It is distinct from the strict D792
chart: the current analytic source uses floors while the chart forbids them.
Build and bill them separately unless a pre-evidence source proof establishes
identical conventions, floor equivalence/removal, dtype/order, and both outputs.

State machine:

1. Retain complete immutable-W0 reserve while building and finite-checking
   `Y_an`.
2. If the completed stack reaches the predeclared local finite/schema validator
   and returns the named `ANALYTIC_INVALID` state, cancel/join/release all
   analytic work and invoke immutable W0 from scratch on the same panel
   view/production stream. An exception while building or validating `Y_an` is
   not this state and is `PROVIDER_FAILURE`.
3. After `Y_an` validates, cache it for this invocation only, release W0 reserve,
   and build the strict chart, Pi, operator, and transfer objects.
4. Before drawing, reconstructing, or reading production `Q`, create an immutable
   Pi-owned `RouteCertificate` containing every ordinary/terminal shape, exact
   remaining success bill, every later prefix/analytic-return bill, guard,
   cleanup, wall/RSS reserve, and payload hash.
5. If any remaining path does not fit strictly, record
   `PRODUCTION_RESERVE_REJECT` and return all of cached `Y_an`.
6. Otherwise construct production `Q`, run F48, and return `Y_C` only on complete
   quiet-guard success. Any named local later reject returns all of `Y_an`.
7. Unknown exception, provider/meter failure, actual hard-budget exhaustion,
   interrupt, or unreserved resource failure is `PROVIDER_FAILURE`, never a
   successful analytic return.

There is no late W0, elementwise `where`, clamp, favorable mix, partial sampled
row, redraw, retry, alternate count, or cross-invocation analytic cache. Every
repaired-arm `branch_code != SUCCESS`, including `EARLY_W0`, is premise,
confirmation, and depth FAIL. Hostile fixtures may test safe completion but earn
no scientific credit.

The certificate can read only weights, public base identity, Pi, immutable assets,
published rules, source shapes, and runtime hashes. It cannot read production Q,
truth, score, held-out labels, measured timing/RSS, system load, or leaderboard
state. Because it is production-Q-independent and panels retain every branch,
the gate does not condition successful Haar rotations. Any certificate/source
shape mismatch is `PROVIDER_FAILURE`.

## 💰 Exact path ledgers and provisional cost orientation

The typed manifest must prove strict FLOP, wall, and aggregate process-tree RSS
bounds for every prefix maximum:

\[
C_{early}=A_{prefix}+G_{early}+C_{cleanup}+C_{W0,complete},
\]

\[
C_{setup}=A+S_{prefix}+G_{fail}+C_{cleanup}+C_{return}(Y_{an}),
\]

\[
C_{reserve}=A+S+C_{certificate}+G_{fail}+C_{cleanup}+C_{return}(Y_{an}),
\]

\[
C_{late}=A+S+C_{certificate}+P_{48,prefix}+G_{fail}+C_{cleanup}
         +C_{return}(Y_{an}),
\]

\[
C_{success}=A+S+C_{certificate}+P_{48}+G_{success}+C_{return}(Y_C).
\]

Add success cleanup explicitly:

\[
C_{success,total}=C_{success}+C_{cleanup,success}.
\]

Every non-success provider/abort prefix also has the mandatory bound

\[
C_{abort,max}=\max_{t\in\{A,W0,S,certificate,Q,production,guard,return,cleanup\}}
\left(C_{spent\ through\ t}+C_{cancel/join,t}+C_{cleanup,t}
      +C_{abort-return,t}\right)<B.
\]

`C_abort-return,t` is zero when the invocation lawfully emits no output; it may
name W0 or `Y_an` only for the exact enumerated branch states. Bind corresponding
critical-path wall and simultaneous process-tree RSS maxima for every `t`. An
unknown/provider/meter failure remains a failed invocation even when its abort
resource prefix is safely bounded.

`C_W0,complete` includes W0 output return and teardown. `A` is only the complete
analytic cache by default. `S` includes strict chart, Pi, folds, JVP/VJP, solver,
transfer, RNG, reductions, copies, allocations, controls, and setup cleanup.
Maximize `C_late` over every production prefix, including complete production.
Add every launched task's maximum cancellation latency and work before join.
No actual hard failure is treated as a reserve decision.

Provisional Phase-1 orientation only:

The common static arithmetic is independently bound to repository HEAD
`2d275b4d28b2741a047e396e7c9b71ea4ad66669`, `cost_model.py` Git blob
`13a0ffe191b163c041c26ec0fb558bf09ed7af02`, and `fold3_estimator.py` Git blob
`80a71e449097a9a42c1e40ffa8c205d2e621fdd1`. It is rederived for F48 rather than
imported as a F56 result.

- W0 guard-quiet witness: `259,700,821,492`, not a worst-case bound;
- provisional budget: `272,000,000,000`;
- F48 conditional full-width ordinary-lane saving: `49,514,479,616`;
- F48 abstract worst-shape ordinary-lane envelope: `178,255,411,456`;
- W0 full-width ordinary lane: `207,977,512,960`;
- envelope saving: `29,722,101,504`;
- witness headroom plus envelope saving: `42,021,280,012`;
- dtype-repriced partial setup anchor: `17,782,131,200`;
- separate analytic-stack analogy anchor: `133,444,775`;
- arithmetic remainder before all other work: `24,105,704,037`.

The envelope is exact only for 28 positive-width ordinary two-half calls under the
frozen dispatcher. It is not a full-provider bill and does not include first
transforms, terminal work, analytic/cache, chart, certificate, guards, wall, RSS,
or return. The partial anchors are not source upper bounds. These numbers justify
F48's position in the static ladder, not implementation or execution.

For auditability, let `b_m(k,n)` be the exact frozen
`owned_batched_candidate_bill(m,k,n).total`, set `F_0(256)=0` and all other
`F_0(k)=-infinity`, and recurse

\[
F_l(n)=\max_{1\le k\le256}\{F_{l-1}(k)+b_{24576}(k,n)\},
\qquad l=1,\ldots,28.
\]

Then `2*max_n F_28(n)=178,255,411,456`. This maximizes over an abstract superset
of positive-width shape sequences; it is an upper envelope, not a claim that a
generated network reaches the maximizing path. If a Pi route contains a zero
ordinary width, the certificate must either use a separately source-proved exact
zero-width skip bill or take `PRODUCTION_RESERVE_REJECT` before production Q; it
may not apply this positive-width DP silently.

## 📐 Exact variance and feasibility algebra

For vector half outputs, define `V_A(W),V_B(W),C_AB(W)` using the D792 normalized
squared-norm/trace scalar, centered over held-out rotations within fixed network
`W`. Define

\[
\kappa=\frac{2C_{AB}}{V_A+V_B},\qquad
D_{48}=\frac{V_A+V_B}{4}.
\]

Then

\[
V_C=D_{48}(1+\kappa).
\]

With W0 variance `V_0`, define exact design debt

\[
d_{48}=\frac{48(V_A+V_B)}{252V_0},
\qquad
\frac{V_C}{V_0}=\frac{63d_{48}}{48}(1+\kappa).
\]

Only under unearned `d_48=1` and equal population bias does raw-MSE parity reduce
to

\[
\kappa<-5/21=-0.238095\ldots.
\]

For a population raw-MSE ratio target `rho`, the exact requirement is

\[
\kappa<\frac{\rho MSE_0-B_{48}}{D_{48}}-1.
\]

If `B_48=B_0` and `d_48=1`, this becomes

\[
\kappa<\frac{48}{63}\left[\rho-(1-\rho)\frac{B_0}{V_0}\right]-1.
\]

The often-quoted `rho=0.20` value `-0.847619...` additionally assumes zero common
bias `B_0=B_48=0`, not merely equal bias. It is not an adjusted-score prediction;
the complete same-invocation multiplier remains authoritative.

Let population `B_48=||E[Y_C]-theta||^2/p>=0`; never confuse it with signed
finite `b2hat`. Population raw-MSE parity with W0 requires

\[
\kappa<\frac{MSE_0-B_{48}}{D_{48}}-1.
\]

For panel sums over network occurrences, including duplicates inside a bootstrap
replicate, freeze

\[
D_{panel}=\frac14\sum_W[V_A(W)+V_B(W)],
\]

\[
\kappa_{min,panel}=
\frac{-2\sum_W\sqrt{V_A(W)V_B(W)}}
{\sum_W[V_A(W)+V_B(W)]},
\]

\[
\kappa_{required,panel}=
\frac{\sum_W MSEhat_{W0}(W)-\sum_W b2hat_{Arm1}(W)}{D_{panel}}-1.
\]

Recompute all terms in the parent's canonical outer-occurrence order inside each
bootstrap replicate. This signed-estimator screen targets raw-MSE parity with W0.
Failure to place a one-sided upper bound on
`kappa_min,panel-kappa_required,panel` below zero is a conservative failure to
certify feasibility, not proof of population impossibility.

The fixed-subset Haar theorem applies to the pre-guard functional under weights,
Pi, route decision, and fixed reflection independent of production Q: each node
has the spherical marginal and `RQ` is Haar for `Q` Haar on `O(d)`, so reflection
adds zero incremental pre-guard bias to the affine equal-half average. It does not
claim that conditioning on a Q-dependent quiet-guard event preserves Haar. Panels
drop or redraw no occurrence: any guard activation kills the complete panel.
This does not prove benchmark unbiasedness, higher-order Kerdock exactness,
finite-PCG64 exact Haar, or analytic/guard neutrality. Direct bias and MSE gates
remain mandatory.

## 🧪 Arms, reducers, and gates

Use the exact D792 premise panel, operator/transfer gates, confirmation/depth
seeds, paired rotations, two-truth construction, and 50,000-replicate atomic
hierarchical bootstrap. No network, rotation, branch, or duplicate occurrence may
be dropped.

Define equal-occurrence raw-MSE ratio

\[
R_{MSE}(X:Y)=\frac{\sum_W MSEhat_X(W)}{\sum_W MSEhat_Y(W)}.
\]

Recompute numerator and denominator fully inside every bootstrap replicate and
require both finite and strictly positive before forming the ratio. Adjusted-score
ratios retain D792's same-invocation mean of `L*g` and positive aggregate checks.

Run Arm 0 then Arm 1. Every occurrence must be `SUCCESS` with quiet guards. Define

\[
d_{48,panel}=\frac{48\sum_W[V_A(W)+V_B(W)]}
{252\sum_WV_{W0}(W)}.
\]

Arm 1 passes only if all three one-sided `96.667%` bounds hold:

```text
upper96.667[complete adjusted-score ratio Arm1/W0] <= 1.50,
upper96.667[BiasNI(Arm1:W0)] <= 0.05,
upper96.667[d_48,panel] <= 1.25.
```

These three use nominal Bonferroni familywise one-sided 90% empirical coverage.
Separately, require finite positive panel variances and a `96.667%` upper bound on
`kappa_min,panel-kappa_required,panel < 0`. That stop-only gate has no added
familywise-coverage claim. Failure stops before Arm 2.

After Arm-1 PASS, reflection credit requires all seven one-sided `98.5714%`
empirical bounds:

```text
upper98.5714[kappa_panel(R_AJ)-kappa_panel(I)] <= -0.20,
upper98.5714[R_MSE(Arm2:Arm1)] <= 0.95,
upper98.5714[complete adjusted-score ratio Arm2/Arm1] <= 0.95,
upper98.5714[R_MSE(Arm2:W0)] <= 0.80,
upper98.5714[complete adjusted-score ratio Arm2/W0] <= 0.80,
upper98.5714[BiasNI(Arm2:Arm1)] <= 0.05,
upper98.5714[complete adjusted-score ratio Arm2/ArmR] <= 0.95.
```

Bonferroni allocation gives nominal familywise one-sided 90% empirical coverage.
Retain the separate pointwise honest-tier stretch gate
`upper90[adjusted-score Arm2/W0]<=0.20`; it is not literal public-rank-one evidence.
A number-one claim additionally requires pre-evidence `r_board`, official rules,
`upper98.5714[complete adjusted-score ratio Arm2/W0] <= r_board`, and a hosted
grade. This strengthens the existing seven-gate score threshold; it is not an
eighth statistic. Depth 8 and 16 require point estimates
`kappa_AJ-kappa_I<0` and adjusted-score `Arm2/Arm1<1` as descriptive vetoes.

## 🚦 Parallelization

Default is one process, one worker, one native math thread, and `P=1`. If final
rules explicitly permit descendant parallelism and the static meter proof covers
the complete process tree, independent `H_1/H_2`, final Rayleigh replicas, and
non-scored fold tasks may use the inherited deterministic scheduler. Each
six-action Krylov chain remains sequential. Canonical reductions never follow
completion order.

Every score-bearing provider invocation is isolated with no sibling workload
unless a pre-evidence proof makes its complete multiplier schedule-invariant.
Concurrency never discounts operations: FLOPs sum over workers, wall includes
spawn/barrier/cancel/join/teardown, and RSS is simultaneous aggregate. No worker
count, batch, merge tree, or cache policy is selected from results.

## 🧱 Source seams and static fixtures

Required source changes, all before a generated-network forward:

1. `kerdock_v3_estimator.py`: replace the hard-coded trimmed 126 bank with
   `trimmed[0:48]` and `trimmed[63:111]`, corresponding to raw asset slices
   `2:50` and `65:113`; omit trimmed `48:63` and `111:126`; allocate two exact
   24,576-row/six-block paths.
2. `fold3_estimator.py`: move Q-independent analytic construction before Q;
   replace every production-derived topology decision with Pi; emit half
   sufficient statistics; prove affine `T_Pi`; combine before one correction and
   one whole guard.
3. `base_estimator.py`: keep floor-bearing analytic fallback and strict no-floor
   chart distinct unless an exact fusion proof passes.
4. `row_blocked_winograd.py`: bind six blocks per half and exact right-pack/cache
   lifetime; credit no cache absent source and lifecycle proof.
5. `estimator.py`: keep immutable W0 separate; implement reason-coded early W0,
   post-cache analytic return, route reserve, cleanup, and no elementwise repair.
6. common core: enforce runtime `c`, left rank-one action, identical Arms1/2/R
   success trace/bill, and no duplicate analytic/tangent/terminal/guard work.

Synthetic-only fixtures must prove exact phase order/nonoverlap/omissions;
ordered A/B argument detection; runtime `c=0` work; affine half decomposition;
Pi-only topology under changed production Q; Q-independence and invocation-local
analytic cache; early invalid analytic invokes same-view W0; post-cache
chart/setup/reserve/late failure returns entire analytic bytes and never W0;
single-entry nonfinite cannot mix outputs; route-certificate/source equality;
and deterministic serial/permitted-parallel shards. Fixtures can kill but never
tune or promote.

Bind D792's exact A4 generator provenance at commit
`78bc4513b430fefdaa9145e16e04102e52cdf627`, path
`corpus/whestbench/experiments/a_series_granular_adversarial/run_a4_hostile.py`,
and source SHA-256
`52CBA048E782673E3E8ACF4CEC2F1052153A493B7B0462CF82990E74293FDA09`,
including its seven frozen families and exact parameters. Those hostile runs test
completion only. The collapsed-chart fixture first validates a separate complete
analytic cache, then forces the strict chart to fail and must return that whole
cache. A separate invalid-analytic fixture must take `EARLY_W0`. Neither branch
earns promotion; a crash, hang, mixture, unreserved path, wrong branch, or
nonfinite emitted output kills F48.

## 🧭 Integration and family identity

1. Bind exact Phase-2 rules and revalidate immutable W0.
2. Build all providers, analytic branch, route certificate, reducers, truth,
   tests, scheduler, ledgers, and one manifest before any network numeric.
3. Run no-forward source/schema/package/bill/path/RSS/wall/lifetime gates.
4. Under separate authority, run only frozen hand-constructed synthetic fixtures.
5. Run the truth-free premise/transfer screen; require every network
   `TRANSFER_SCREEN_PASS` and `SUCCESS`.
6. Generate and seal truth replicas under truth-only authority.
7. Run Arm0 then Arm1; stop on branch, guard, debt, bias, or feasibility failure.
8. Run frozen Arm2 and ArmR; compute seven gates from sealed atomic records.
9. Run frozen depth, hostile, and metamorphic controls; rebuild exact archives.
10. A hosted canary requires final rules and explicit user approval.

Stop on the first rule, source, path, bill, wall, RSS, chart, operator, transfer,
branch, guard, bias, variance, covariance, raw-MSE, adjusted-score, depth, hostile,
or reproducibility failure. Do not repair F48 in place.

Family identity is

```text
(parent_sha256=D792C67DF0C8933BAF7EFA7CDD3836F8D48B62DF72DCECB175F3048D7F387A90,
 this_child_sha256,
 pre_evidence_manifest_sha256).
```

After the first synthetic or scientific numeric, any changed byte, tolerance,
draw, frame, rank, layer, action, arm, branch, schedule, gate, source, runtime, or
dependency is AJ3 and reruns from zero. Before premise data, the owner selects
exactly one revealed family or an approved canonical NULL.

`BOTH_KILLED` is honourable. F48 earns candidate status only after every integrated
gate. Until then it is zero-evidence and cost-unproven.

[^phase2]: AIcrowd, Mohanty. (2026). [Phase 1 submission selection and Phase 2 launch date](https://discourse.aicrowd.com/t/phase-1-submission-selection-phase-2-launch-date/18143).
