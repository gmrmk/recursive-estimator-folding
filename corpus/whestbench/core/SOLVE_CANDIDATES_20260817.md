# Solve candidates — swarm synthesis, judged 2026-08-17 (~22:4x UTC)

Provenance: six generation lenses + six adversarial refuters ran (Fable-tier;
the workflow was stopped after twelve results to cap token burn — the
remaining refuters and the synthesizer were cancelled and this synthesis was
written by the session judge from the banked journal instead). Two proposals
are therefore **UNREFUTED** and marked so. Going forward, swarms run Opus-5
with preseeded context packets; cells are cold-verified (Eidolon adaptation,
§5).

## 1. Ranked candidates

### #1 — AJPRE-1: anti-J W0→W_I precondition (SURVIVES, narrowed) — first live cell
Zero ledger collisions by name AND number (grepped: anti-J, d_48, kappa_AB,
W_I, 269/525, 0.5124, F48 — all clean; nearest records examined and cleared:
refraction-optics bolt, M133/M134 kappa namespace, flatworm KAPPA, M180, P4).
Mechanism: measure d_48 (design debt of the F48 split) and kappa_AB(I) (arm
coupling at identity reflection) on synthetic dev nets; exact algebra
V_C/V_0 = (63·d_48/48)(1+kappa); every published gate currently rests on the
UNEARNED d_48=1 and an UNMEASURED kappa_AB(I). Trap-immune by construction
(no eigensolve exists for the ≥129 forced −1 eigenvalues to poison).
**Refuter's binding corrections (adopted):** (a) as originally costed there
are zero degrees of freedom for the variance-defined gate quantities — the
cell must be power-first (rotation replicates within net, df computed before
bands); (b) an explicit INCONCLUSIVE band is mandatory — fail-closed bands
would let a wide CI at 7–15 df permanently kill a family the full 50,000-
replicate panel might pass; (c) "d_48 near 1" must be a predeclared number.
Most probable outcome per the record's own priors: a cheap kill (each
48-frame half loses the exact-2-design property → d_48 > 1). That outcome
still retires the family honestly — which is the point.
Confidence: implementation 0.85 · mechanism 0.9 · generalization 0.6 ·
compliance 0.95.

### #2 — Q1: lawful clone-survivor adoption, stage 1 (SURVIVES, narrowed)
The largest measured lawful score delta available: L2-fringe peeling
−4.948% score on the clone's OWN g4-paired-accumulation baseline (the −9.09%
cross-receipt headline is WITHDRAWN — it mixed a λ-priced Linux receipt
against our hosted receipt; incommensurable). Stage 1 is synthetic-only and
minutes of work: extract survivor configs from
`Backups\whestbench-20260817\codex-clone-campaign-layer-20260817.tgz` (the
only custody-clean source), replay on synthetic nets, assert output parity
with a CROSS-lineage tolerance predeclared fresh (the 4.301e-8 precedent is
same-lineage), and verify the static FLOP bills reproduce the transcript
deltas before anything else counts. **Owner's key required** before any
adoption into champion lineage (custody is an owner-only decision,
continuation plan §6). λ-fork-robust in this form: the FLOP-only half
transfers under both branches; the wall half is re-measured on Windows under
the clone's own 24-pair contract only if λ survives.
Confidence: implementation 0.9 · mechanism 0.85 (numbers are [R] until the
archive reproduces them) · generalization 0.7 · compliance 0.9.

### #3 — WC1: certified Plackett Source211Jet provider — **REFUTED 2026-08-17 (Opus cold-refuter, file-line cited)**

The cold refutation killed it on all four axes: (1) COMPOSABILITY — WC1's core
clause is verbatim the fixed-node Plackett/Owen-T line rule **M162 killed**
(`M162…:3`, disposition `REQUIRE_SYMBOLIC_CANCELLATION_PLUS_RANK_AWARE_INTERVAL
_CERTIFICATE`, none of which WC1 supplies); M178 is bivariate-only ("OPENS ONLY
M176"), M165 is rank-one-only ("no operation credit"), M173 is a rank-2 layer
certificate only — the union is not the atlas (insight 113 names the full-rank
trivariate Source211Jet as still *uncovered*). (2) COST — M205's minimal
provider is already **2.595B > 1.987B** strict headroom; certified integration
is additive on top. (3) PRIOR KILL — **M211 `KILLED_FROZEN_RESOURCE_GATE`**
(2.0025B vs 1.987B), gm_m179_m199 `KILL_CONFIRMED` (M198 covariance goes
non-PSD at layer 12/32, width 256). (4) NON-IDENTIFIABILITY — WC1 evaluates an
orthant *probability* and calls it a physical *coefficient provider at native
cost*: two different objects, shortcutting exactly the layer-bound provider the
record names as the block. **WC1 is the M205 god edge relabeled.** Surviving
residue: none as a provider; only the research program M162/M165/M173 already
say is required and unbuilt (symbolic rank-face cancellation + rank-aware
interval certificate + native cost trace + nontransverse/zero-marginal charts).
The exact-control spine (M151→M179→M198→M205→M125b) stays blocked on that
program, not on a composable shortcut. Kill is final.

### (archived draft of #3, kept for the record)
The highest ceiling in the set: composes preserved tissue only —
M162's trivariate-orthant compression identity × M178's certified
Phi2/Owen-T primitive (4,048 FLOPs/pair) × M165/M173's boundary-strata
theorems — into the full-rank-interior provider that insight 113 calls "the
first uncovered interior theorem" and insight 130 prices as the campaign's
first unearned god edge (1.986871472B strict headroom). The wildcard lens
also certified the negative: NO other preserved cross-family component
substitutes (M130-as-control killed by M140; M135 low-rank killed at r≥234)
— this is the only live shortcut to the exact-control spine.
**Gate before anything:** one Opus cold refuter with the three parent
records + the M205/M198 chain as its entire context. If it survives, this
becomes the campaign's Phase-2 flagship cell.

### #4 — Q2: fringe-knob classification (SURVIVES, narrowed)
Static per-region FLOP attribution over the archived clone configs (R0
Strassen-accounting methodology precedent; zero billed FLOPs). Deliverable
reframed by the refuter: a classification — are L2-fringe / dual-odd /
odd-width alternative settings of ONE fringe-handling knob (mutually
exclusive) or composable savings — as a conditional-increment matrix, no
predeclared overlap percentage, provenance-gated on the archive reproducing
the transcript deltas. Note the refuter's like-for-like check: g16 +
L2-fringe FLOP-only sum (−6.76%) vs the observed −9.1% receipt — the
champion is BETTER than its components' additive sum.

### #5 — Lens-1 analytic power check (refuter's surviving form of a killed gate)
Pure arithmetic over already-instrumented quantities: per-rung degree-4
shares of the k=32 betas and k=16 controls. Decides whether ANY
base-sensitivity refit is powered before designing one. The original
refit gate is dead (degree-4 deletion is a ~1–2% perturbation → dual-pass
near-certain → could never license net2). **net2 stays sealed.**

### #6 — L8 Abel: analytic layer-1 gate-0 alignment probe only
Both swarm implementations REFUTED with receipts (see §2). What survives is
exactly the lightning-ledger cell as logged: the analytic layer-1
Abel-smoothed compressed-proxy control at production d=256, β=1 with a
variance-non-increase certificate (never fitted — every fitting route is
ledger-killed), matched-cost generic-averaging control arm (S11 lesson),
dispersion measured at d=256 before any small-d battery counts. Absent all
three, it stays predeclarable-unrun.

## 2. Refuted this pass (kept; a named death is worth more than a quiet one)

- **LENS1-BASESENS-GATE-v1** — zero power: A_4/A_6 ≈ 2.3% of defect weight
  makes the discriminating outcome mechanically unreachable; dual-pass with
  p≈1. Also: base-stability is within-net; the DGFL kill was cross-net.
- **LENS1-DEGREEBAND-CENSUS** — its own constants empty both action cells
  (k=16 ratio ~1.45 > PASS ceiling; separation ~16x > FLAT ceiling); survives
  only as an unthresholded descriptive extension of the Gegenbauer census.
- **AB1 (proxy-MC Abel control)** — no production-admissible β exists:
  cross-net (DGFL), per-net rotation-replicate (M107 reversal), weights-only
  (gm_p2b: Spearman 0.0), and even ORACLE per-neuron fits lose −4.1%
  (a3_kill_heterogeneity_reaudit). Small-d batteries cannot see the d=256
  dispersion wall.
- **AB2 (layer-1 free tail atoms)** — collision by number:
  strict_band_boundary_ridgelet_control already measured the exact W1-tail
  claim false (shuffled normals comparable); factorial decay caps ridge-atom
  degree-≥8 energy at negligible amplitude; falsifier undecidable at its own
  sample sizes.

## 3. Certified closures (new arguments, stronger than the ledger's)

- **Randomized-radius lane: closed by identity.** Positive homogeneity ⇒
  E[h(X)] = E[chi_256]·E_sphere[h(u)] exactly; the fixed chi-mean radius is
  the Rao-Blackwellization of every legal radius law — any randomization adds
  pure variance at zero bias change. Closed by derivation, not measurement;
  the ledger never stated this. → graph insight + theory-kill record.
- **Coupling-beyond-antipodal: closed by union** of
  compact_group_laplacian_control (nonnegative harmonic covariance for
  smoothing-coupled pairs), s3, n8a, m138, M180; degrees ≥6 sit in irreps
  where finite fixed-rotation subgroup averaging ≡ design enlargement under
  the 1/N law. Score-neutral at best.
- **Provider substitution: closed except WC1** (the wildcard lens's negative
  certification).

## 4. Unrefuted remainder (flagged, not adopted)

- WC1 (above, gated on its cold refutation).
- MT-degree-6 composite (moment-tangent transport × M122 preserved Hermite
  algebra at degrees ≥6; N9 killed only the degree-≤2 composition, which
  frame annihilation absorbs). Same gate: one cold refuter before anything.

## 5. Protocol adaptations (Eidolon, adopted)

- **Cold-context verify**: every cell verdict is re-derived by a verifier
  whose ENTIRE context is the cell directory (predeclaration + report +
  thresholds) — no channel, no synthesis, no author reasoning. fold_search
  cells are self-contained by construction, so this is free to run and is
  now the standing review shape (Opus-5, preseeded, per the owner's cost
  doctrine).
- **Probe-one-before-scaling**: AJPRE-1 runs alone before any fold_waves
  fan-out.
- **Risk-tiered swarms**: full swarms only for authority-bearing artifacts;
  fast deterministic checks for the rest.

## 6. Bottom line, honestly

Nothing here moves the SCORE tonight. #1's modal outcome is a cheap family
retirement — the evidence for that prior: each 48-frame half loses the exact
2-design property the full bank carries (so d_48 > 1 structurally), and the
proposer's own disclosed planning median was 1.15, an expected loss. #2 is
the one lawful measured score delta (−4.9%-class,
[R] until reproduced) and needs the owner's key; #3 is the only path to the
spine and is unrefuted; everything else protects power, provenance, or
closes lanes with proofs. The forecast's 1% insight stands: the private
re-run is the biggest mechanism, and slot robustness buys rank cheaper than
variance reduction. The queue above is ordered accordingly.

---

## Addendum 2026-08-18: the k32/Lens-1 accuracy thread is dead by replication

Cells `k32_base_sensitivity_v1..v3` (gates a70464d / 5a8ec69 / 0bca673; full story in the
channel at 03:02 UTC). Six independent seeds across v1+v3: the k-high Fourier-Stein control
produces NO positive held variance reduction on width-256 challenge-family nets (phenomenon
absent 6/6, two seeds significantly negative), and fitted coefficients do not transport
across exact-2-design bases (signs preserved 0/6, cosines -0.93..+0.18). v2 is recorded as a
deterministic rerun with no independent weight (seed-plumbing failure, now structurally
impossible: harness seed-agreement protocol kill, tests 76/76).

Consequences for this ranking: any candidate that FITS coefficients per-net or per-frame is
dead as a class (noise-fitting death, now replicated). The surviving accuracy-side design
space is theorem-fixed coefficients only (population-exact structure, subtract side). The
sealed net2 spend is unjustified on current evidence. New replicated observation available
as tissue: deep ReLU pullback collinearity 0.81-0.95 at layers 8/16/24/32; new instrument:
exact-2-design base pairs with 4x degree-4 defect contrast.
