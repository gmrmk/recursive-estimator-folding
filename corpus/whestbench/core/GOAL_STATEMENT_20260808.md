# Goal statement — the standing objectives of this campaign (2026-08-08)

This document is the separate goal statement requested by the owner: the
goals as given, their lineage, and their operational meaning, so any
successor session (Fable, Opus, Codex, or a delegated agent) inherits the
objectives without this conversation. It sits beside GEN4_CLOSING (the
constraint set), SUBMISSION_DOSSIER (the playbook), and the fold ledger
(the evidence).

## The goal lineage, as given

1. **Codex's original goal** (inherited at session start): follow the repo's
   FABLE5 resume protocol exactly — the M-series dependency chain, fold
   discipline (predeclare -> cheapest falsifier -> ledger -> commit), the
   firewall (no sealed cells, no truth/scorer reads, no accounting bypass).
   Fulfilled through M177 -> M178 -> M179 and every mutation since.
2. **"Give me the number one spot and the most creative solve using
   adversarial tactics ALLOWED in the contest rules, compounding our
   successes with the failure curve."** The operative constraint is ALLOWED:
   the wall-tier accounting channel is excluded by the rules' own
   circumvention clause (§5.2), the organizers' stated DQ standard, and this
   corpus's frozen legality invariant. Adversarial-within-rules means: the
   adversarial testing program (A-series), competitor-intel mining, and the
   correction-proof positioning against the coming re-grade.
3. **"Keep going until we surpass the #1 spot on the leaderboard."** Honest
   status, permanently recorded: the displayed #1 (5e-10 tier) is an
   unmetered-compute artifact under organizer review; every honest method on
   the public record sits >= ~4.6e-8; prizes are decided EXCLUSIVELY by the
   Sep 20-30 private fresh-seed re-run (§5.4), not the public board. The
   goal is therefore pursued as: hold the best honest candidate, designated,
   correction-proof, when the re-grade tide and the private re-run arrive.
4. **"Do not take the easy way out to deliver our inevitable win; granular
   adversarial research and testing and postmortems are fed to recursion."**
   The standing process goal. Operationalized as: predeclared kill gates on
   every mutation, adversarial re-audits of our own verdicts (A3), hostile
   testing of our own champion (A4), granular decomposition before belief
   (A1/A1b/M185), and every postmortem written into the ledger and packets
   as Generation constraints.
5. **The hybridization mandates**: mutate the failures together (PB-1
   crosses), mine every winner the same way (WC-1 ablation map), route every
   uncertainty to its settling check (UNCERTAINTY_LADDER), apply
   prodigal-mathematician rigor including the trigonometric/harmonic model
   (M191 — which produced the 2-design theorem), and accelerate the
   recursion itself (the Premise Battery, 3.2x; the compute-runner harness
   fix).

## The win, stated honestly

- **Score prizes**: decided at the private re-run. Our position: Kerdock v3
  live (#326094, 1.83e-7, rank #58 public), v3.1 GUARDS validated as its
  strictly-safer successor (bitwise-identical, crash/NaN classes closed,
  hardened rebuild tar 8382e269…ea06). The wall tier above us is already slipping under
  silent re-grades (observed Aug 8 evening) and faces manual validation.
  Inevitability = being the strongest artifact left standing when the
  accounting tide goes out; the best validated local artifact is staged, but
  no prize outcome is guaranteed.
- **Algorithmic Contribution prize**: judged on rigor and novelty, not rank.
  The filing (PHASE1_WRITEUP_DRAFT, ID #326094, due Aug 17) carries: the
  exact-2-design theorem + dispersion no-go, the marginal-value map, the
  tail theorem, the 19-kill falsification ledger, and the suite-calibration
  method. This is the highest-probability payout and is ready.
- **What would change the score picture**: a band competitor publishing a
  mechanism (pre-Aug-17 scan armed), the organizer re-grade completing, or
  genuinely new mathematics against the frontier stated in GEN4_CLOSING
  (attack the variance of an exact-2-design spherical average of a
  piecewise-linear function without tractable low-degree structure, exact
  slicing, Gaussian surrogacy, or design perturbation — plus the newly
  proven constraint that the 61.6% rotation-oracle headroom is
  information-gated).

## Standing dates and duties (any successor must keep these)

- Aug 10 23:59 UTC: Phase 1 closes; auto-top-2 (#326094 + #318609) stands.
- At Phase-2 open: consider v3.1 GUARDS only after an explicitly authorized
  canary. The current staged file is `Desktop\whest-submit\
  0_kerdock_v31_GUARDS_HARDENED_8382e269.tar.gz`; the older `...BEST` copy is
  stale. Use the blind .env key-load pattern only — the
  key value is never read or displayed.
- Aug 17 23:59 UTC: file the Phase-1 algorithmic writeup with ID #326094.
- Sep 19: designation locks (v3.1 pending its graded canary). Slot count
  SETTLED at TWO nominations per team — confirmed twice: the dossier's rules
  read and the Aug 5 Discourse answer ("each team may nominate up to two
  submissions for the Phase 1 private re-evaluation"). Portfolio rule: slot 1
  = best validated sampler (v3.1 GUARDS, canary permitting), slot 2 = best
  step-change candidate (fold3cap ONLY after its own graded canary, else L2).
- Sep 20-30: private re-evaluation decides prizes.
- Always: fold discipline, the firewall, kills are final, measured sizes
  only, no wall-tier claims, no accounting bypass EVER.
