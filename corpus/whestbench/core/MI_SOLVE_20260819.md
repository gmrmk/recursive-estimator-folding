# MI SOLVE -- the mutual information between what didn't work and what did

Stamped: 2026-08-19 09:09:44 UTC (`date -u`: "Wed, Aug 19, 2026 9:09:44 AM").
Synthesizer: Fable, final stage of wf_77a537c1-bec (owner directive executed: "We
need to find the solve in the mutual information between what didn't work and what
did").

Inputs: the MI kill/win graph (headroom/mi_graph/ -- build script + 4 outputs,
byte-identical across two consecutive runs by sha256, all 276 nodes field-diffed
against fold_ledger.json with 0 mismatches); 18 surviving doors from 3 gated Ralph
rounds; the AGENT_CHANNEL tail through 2026-08-19T08:41:38Z (ultrareview merge
wf_b708199c-ca4, ultramath slate wf_01a886a8-2ad at core/ULTRAMATH_SLATE_20260819.md,
three owner rulings, round-4 disposition, repricing swarm wf_b325115d-acd).

Compliance: experiments/fold_floor_splice and experiments/frame_completion_129 were
neither read nor written by this synthesis. Every python invocation this workflow
made used -B with PYTHONDONTWRITEBYTECODE=1 (channel protocol finding of 2026-08-19:
an external writer polluted custody trees; verify_fold_floor.py now hard-fails on
stray bytecode). No production or CPU-bound measurement was run or is run by this
document -- it maps work onto machinery and queues.

Provenance ledger for this document: machinery facts (fold_search contract, recurse
CLI, slate vocabulary, channel state, artifact existence and mtimes) are OBSERVED
this session. The MI statistics and the door verifications are REPORTED from the two
upstream workflow stages, each of which documents its own two-signal verification
(sha-stable double run + field-level ledger diff for the graph; per-door independent
recomputation in the Ralph rounds). Arithmetic quoted from those stages was not
re-derived a third time here except where noted.

---

## 1. THE WINNING SOLVE

**The survival information in this campaign's 276-record ledger is not in what was
tried. It is in how the attempt was priced and how it was executed. The campaign
should stop spending scored runs to move along the design axes, bank the three
convention re-pricings that convert existing kills into wins at zero compute, repair
the instrument so it can carry the one axis that actually separates identical-premise
twins (buffer ownership / residual discipline), and ship the graph itself as the
manuscript's kill-ledger artifact -- because the winnable track's centre of mass is
now the contribution manuscript, and this graph is the quantitative form of its kill
ledger.**

Measured basis, in one paragraph. On lived-vs-died (257 decided records, base
survival 0.2412, entropy 0.7971 bits) the four coded axes are nearly silent: the only
single axis clearing a 200-shuffle null is carrier at 0.0170 adjusted bits (~2% of
entropy, p=0.0199), and removing the 33 zonal_harmonic records collapses it to -0.0048
bits (p=0.8209) -- the whole axis signal is one dead cell (zonal 2/33 lived, lift
0.25). Carrier MI also fails leave-one-win-out on the 4-win ledger (dropping idx 0 or
idx 53 puts it below the null maximum), so carrier MI must never rank a door.
Precision is negative after null correction (-0.0024, p=0.5373): dtype carries
nothing. No pair clears 0.05. Meanwhile convention -- the currency a verdict was
priced in -- is the strongest real axis on the 5-class outcome (0.0874 adjusted bits,
p at the 1/201 permutation floor) while null on the binary (p=0.1045): the ledger
knows WHICH WALL you hit, not whether the idea was good. And at Hamming distance
zero the profile still does not determine fate: 140 of 257 decided records (54.5%)
sit in 4-axis cells containing both outcomes, forming 191 zero-axis opposite-outcome
pairs against 1,457 one-axis door edges; the two-record proof (idx 71 vs idx 72,
identical on all four axes, 47-123 MiB and 3-4x residual apart on buffer ownership
alone, opposite outcomes) names the separating variable the instrument cannot see.

Three consequences, in the order they pay:

1. **A kill made at the wrong wall is re-priceable evidence, not a dead idea.** The
   cheapest wins of the whole round are exactly this: v3.1 inheriting the campaign's
   best local adjusted score by re-pricing a +24,575-FLOP delta out of the
   flop_cost_wall currency (settled this round, zero compute); the 512-MiB policy
   wall repriced against the ruled 1-GiB engineering ceiling and the enforced
   65,536-MiB mechanical limit (now ruled by the owner); and the k=5 hostile-residual
   projection repriced to the measured k=1 basis (C_k1 = 210.352e9 vs the 258.4e9
   gate -- k* = 3.83).
2. **The instrument must learn the axis that decides outcomes.** Ownership /
   residual discipline is stated in mechanism text campaign-wide but coded nowhere.
   Coding it (masked, pre-registered, null_MAX bar) is the only move in the set that
   adds information to the graph rather than reading information out of it.
3. **The design layer is closed from both sides.** Empirically the axes carry ~2% of
   survival entropy; theoretically the ultramath slate closed the same layer by
   theorem (deg-4 exclusivity: swap/completion/selection spend ONE ~16% channel,
   rank never sum; unconditional carrier optimality; Delsarte floor ~44x out of
   budget). One design door remains genuinely unwalked and premise-shared with the
   production win -- the Kerdock-host depth-2 schedule pass -- and it goes behind
   two static paper gates before any run is even queued.

---

## 2. The evidence spine (four facts everything above rests on)

- **F1 -- axes silent on survival.** 257 decided, entropy 0.7971 bits; best single
  axis 0.0170 adj bits (carrier, p=0.0199); best pair 0.0385 adj bits
  (convention+mechanism_family, p=0.0697, does not clear); precision negative.
  killtype rows are construction artifacts (MI = target entropy by parsing identity)
  and are flagged, not findings.
- **F2 -- convention prices the wall, not the fate.** 5-class: 0.0874 adj bits,
  p=0.005 floor. Binary: 0.0126, p=0.1045. The adjusted_score cell (4/6 lived, lift
  2.76) is substantially survivorship: a record reaches that coding only after
  passing cost/memory/parity/legality. Convention flips are evidence-level moves;
  carrier/mechanism flips are design moves. The two are different currencies and the
  round's findings sort cleanly into them.
- **F3 -- the Hamming-0 residue and the ownership variable.** 114 distinct 4-axis
  cells; 28 contain both a lived and a died record; 140/257 records live there; 191
  H0 opposite-outcome pairs. idx 71 (separate 512-row workspace + ping-pong rungs:
  525.633 / 601.543 MiB, residuals 0.564209 / 0.437650 s, killed on its own
  predeclared 480-MiB clause) vs idx 72 (caller-owned activation backing, 4096-row
  Winograd overwrite in place: 478.555 MiB, residual <= 0.143071 s, screened
  survivor). Same four axes. The residual channel is also what ate idx 59 (only
  1.069% of a 3.456% analytical cut survived into mean C -- 69% erosion) and what
  killed idx 69's 6144-row ladder and idx 264's depth-4 Strassen (+1.26 s/MLP).
- **F4 -- one dead carrier cell.** zonal_harmonic: 2/33 lived (0.061 vs 0.2412 base,
  lift 0.25); the deficit reproduces inside transport (1/11) and sampler (0/5)
  separately. Correct statement: "zonal/harmonic-carrier candidates almost never
  survive, and no other axis value predicts survival at n=257." Settling check
  (named, unrun): score one zonal candidate on the promoted Haar host and see
  whether it dies for a carrier reason or a cost reason.

---

## 3. Governance this plan is filed under (supersedes the holds named at dispatch)

The dispatch described holds that the channel has since overtaken. Filed against the
channel state as of 2026-08-19T08:41:38Z:

1. **Round-4 fold verify is COMPLETE and the fold lineage HALTS** (owner ruling 2,
   strict three-red-loops law). M1 (fold Public100) is CANCELLED; P1 recorded as
   WITHDRAWN BY GOVERNANCE. The fold retires gate-green as a contribution artifact
   (operator-level metered ratios 0.6524 / 0.7145 and the per-call floor stand).
   Nothing in this plan revives the fold as a designation candidate.
2. **The 129 execution chain owns the harness** (owner ruling 1: repair spec band to
   [0.78, 0.93] -> pre-flight -> predeclare -> ONE run -> judge). Every scored or
   wall-time item below queues BEHIND it; the zero-compute items are parallel by
   construction (different resource). The ultramath entry-2 reads land before the
   129 seeds are consumed; this plan does not touch them or the sealed cell.
3. **The designation pivot is unfolded kerdock_v3 vs incumbent row_blocked**, and
   the compute effort goes to the contribution manuscript. The strategic verdict
   names the winnable track's centre of mass: A4 theorem + carrier-free per-call
   floor 303,096,592 + kill ledger + disclosure. Section M below files this graph
   into that third item.
4. **Memory is ruled**: the owner chose a 1-GiB self-imposed engineering ceiling
   over both the 512-MiB relic and the contest-real 64 GB (06:12Z ruling; D-A6
   discharged by it). The mechanically enforced limit remains memory_limit_mb=65,536
   (RLIMIT_AS). This RULES the Ralph rounds' "finding 3 must settle first"
   prerequisite: settled, by ruling. What remains of finding 3 is hygiene (W0.2).
5. **R = 1.652 is retired** by the repricing swarm's divergence map (honest map
   R ~= 1, anchor 13% adverse). No local number is ever quoted as hosted; INV3's
   "maps to ~9.8e-8 hosted" clause is void with it.
6. **F7 16-frame is staged but predicted DEAD post-swap** (ultramath owner flag c):
   do not fund the F7 proxy family and the carrier swap in the same round. Any
   sampler/RQMC-adjacent cell below inherits that collision constraint.
7. **Fences**: fold_floor_splice and frame_completion_129 untouched. INV8's
   composition leg is rerouted through headroom/FWHT_SPLICE_STAGED_20260818.md
   (unfenced) or handed to the fold-tree owner.

---

## 4. The door map -- every surviving door onto the machinery, in waves

Machinery lanes (named as the slate names them):

- **fold_search cell** -- scripts/fold_search.py predeclare/run/verdict/audit;
  REQUIRED_FIELDS (hypothesis, mechanism, cheapest falsifier, baseline, thresholds,
  budgets) + CONFIDENCE_AXES (implementation/mechanism/generalization/compliance);
  inputs and seeds sha256-frozen at predeclare; one-shot GATE_TOKEN; mechanical
  PASS/KILL/INCONCLUSIVE; kills final; wave layer via depends_on. Cells live in
  corpus/whestbench/cells/.
- **recurse CLI** (headroom-recursion) -- pure-derivation rungs: draft -> critique ->
  rewrite with a validator oracle (exact-match integers, window arithmetic), zero
  billed FLOPs, no harness time. Used where a slip compounds and the check is exact.
- **mutation engine (carrier port / construction)** -- estimator-source mutations,
  always paired with a fold_search cell for any measured arm.
- **ledger-only script** -- build_mi_graph.py reruns and coding passes over
  fold_ledger.json; python -B; fixed seed 20260819; double-run sha256 identity as
  the freshness signal. Not fold_search cells (no estimator executes).

NOTE ON CELL CREATION: this pass creates NO cells. The 129 chain is active in
corpus/whestbench/cells/ (dir mtime 09:00:17Z) and predeclaration consumes one-shot
ids under kill-finality and collision scans. Sections below provide ready-to-seal
spec sketches; the harness owner predeclares them at queue time.

### WAVE 0 -- zero-compute, start now, parallel to the 129 chain

**W0.1 v3.1 designation memo (SETTLED -- bank it).**
Door: v31_inherits_kerdock_v3_adjusted_score + INV3 (both legs settled).
Source flip: idx 206 v31_guards (win, design_frame) re-priced on the CONVENTION axis,
flop_cost_wall -> adjusted_score, against the idx 183/idx 53 adjusted-score lineage.
Intent preserved: idx 206's G1 measured bitwise-equal outputs on 3 nets with a
constant +24,575-FLOP billed delta; the settling scan of
experiments/t4_kerdock_descriptive_rescore/kerdock_v3_official100.json (12,128
numeric leaves, zero non-finite, zero tracebacks, failure_breakdown all zero) proved
neither M186 nor M187 could have fired on the public 100, so bitwise identity holds
suite-wide and v3.1 inherits 1.6190837992e-7 local adjusted (multiplier moves
9.034926e-8 absolute, ~1.4e-7 relative -- third significant digit untouched).
Action: write the designation dossier note -- v3.1 DOMINATES idx 183 kerdock_v3
(same score, strictly fewer failure modes); guard-fire counts predeclared as
first-class outputs of any private/canary run (a guard firing voids the inheritance
for that net only); NEVER quote the local number as hosted (governance item 5).
Machinery: memo + ledger annotation feeding DESIGNATION_POLICY_20260819.md v2 and
designation_repricing.py context. Cost: zero. Depends: nothing.

**W0.2 Memory three-tier retirement (hygiene; prerequisite already ruled).**
Door: memory_gate_is_policy_not_enforcement, part (a).
Source: idx 264's own result text + uf1_mem_verdict.json (double-sourced); kills
idx 48 (667.328 MiB), idx 70 (536.898), idx 71 (525.633/601.543), idx 80 (1.75195 vs
1.44531 margin) are all priced against the 512-MiB relic.
Action: (i) the ten-second upgrade -- read the four cited lines first-hand in the
frozen venv (C:/Users/strid/.venvs/whestbench-frozen-m178: whestbench/cli.py:393,
scoring.py:64, subprocess_worker.py:162) and confirm RLIMIT_AS at 65,536 MiB is the
only setrlimit; the claim currently stands at REPORTED from two corpus sources.
(ii) Record the three-tier structure in the policy: 512 MiB = retired relic;
1 GiB = owner-ruled engineering ceiling (deliberately tight, replaces no contest
rule); 65,536 MiB = mechanical enforcement; 64 GB = contest-advertised. (iii) Retire
the retirement debt DELIBERATELY: the executable 512-MiB predicates at m216:67,
m217:39, m145 test:272, and the rss_below_512_mib aggregates in
m169/m209/m210/m211/m212/m215/m218 -- else a future agent re-derives the dead gate.
(iv) Note fold_search's own documented GAP ("memory cap declared, not enforced";
wire windows_job_memory.py via runner.argv) so every future cell declares BOTH the
ruled ceiling and the enforced limit. Payoff: risk retirement + door W3.1 opens
cleanly (idx 58's 492.441-MiB fused-L2 peak fits under 1 GiB). Reviving idx 48/70
buys nothing (idx 50 reached the same bill at 474.301 MiB; idx 72 dominates idx 70);
idx 71 is NOT rescued (its own 480-MiB kill clause stands). Cost: minutes.

**W0.3 MI instrument repair batch + full re-rank under the fragility gate.**
Doors: INV-R3-6 (coder blind to sensitivity field; idx 0 'improved' miss),
INV-R3-7 (idx 73 = idx 183's tarball by hash identity b55a1d8d..., recode
carrier none -> kerdock_mub), INV-R3-8 (delete '\b5-design\b' and 'spherical
5-design' from the kerdock_mub alternation; idx 38 recode with the three-way accept
-- kerdock_mub / zonal_harmonic / data_adaptive -- PREDECLARED HERE, before its
construction is read), INV2 (idx 183 mechanism_family design_frame -> compiler_schedule,
observed in source: kerdock_v3_estimator.py dispatches _sample_matmul to
RowBlockedBatchedWinograd; matmul = 98.94% of the metered bill), INV7 (the six
doctrine carrier relabels incl. 73/74 -> kerdock_mub, 38/39/106 -> zonal, 43 -> haar).
Action: ONE batch edit of build_mi_graph.py + ONE full table rebuild (200 shuffles,
seed 20260819, python -B, double-run sha identity), reporting the WHOLE axis table
diff, never single cells -- at these n a one-record change moves statistics by a
null SD (measured: idx 73 alone moved carrier MI +1.1 null SD).
FRAGILITY GATE (INV2's falsifier, adopted as policy): if any axis's null-corrected
MI crosses its null_max in either direction under the combined relabel, the table is
too fragile to rank doors and every MI-ranked candidate drops to unranked.
Standing rule from INV7 regardless of outcome: carrier MI fails leave-one-win-out
and must not rank any door. Kill for the R3-6 leg: if convention's mi_minus_null
moves less than one null SD (0.0182 bits) AND the doctrine disagreement count does
not fall, record the defect as immaterial rather than fixed.
Machinery: ledger-only script. Cost: seconds of compute, ~1 hour of edits.

**W0.4 Ownership axis coding -- the instrument-level act on the owner directive.**
Doors: INV6 + INV-R3-4, merged (same axis; the falsifier is R3-4's repaired one).
Source pair: idx 71 (kill) vs idx 72 (pass), Hamming-0; population: the 191 H0
pairs; ownership language already present campaign-wide (idx 46 "setup-preallocated
out= buffers", idx 48 "transient workspace/activation overlap", idx 69
"alias-liveness").
Pre-registered labelling rule (binding, written before any labelling): a record is
`caller_owned_inplace` iff its mechanism/bias_class text asserts writes into a
caller-owned or pre-existing activation/output buffer (in-place overwrite, out=,
preallocated destination); `separate_workspace_or_pingpong` iff it asserts a
transient/workspace/ping-pong/double-buffer allocation on the active path;
`unstated` otherwise. Labeller sees mechanism and bias_class ONLY -- status, result,
sensitivity, outcome masked.
Acceptance (all four, else the axis is killed or unmeasurable): (i) rule
pre-registered (done, above); (ii) masked coding across ALL 276 records; (iii)
null-corrected MI > null_MAX on the same record set and null as the four existing
axes (approx 0.041 bits on lived/died at 200 shuffles), not merely > null SD; (iv)
survives dropping idx 71 AND idx 72; and the unstated bucket <= ~200 records.
Honest deduction carried: ownership is not a discovered axis -- idx 71's own result
named the remedy and idx 72 executed it; the test is run for the population, not
the pair. Machinery: ledger-only coding pass + build_mi_graph.py axis extension.
Cost: 1-2 hours procedural, seconds of compute.

**W0.5 Convention governor (adopt the rule; test its one fragile record).**
Door: convention_is_a_gate_reached_not_a_design_knob.
Action: adopt as round law -- convention flips are EVIDENCE moves (change what the
ledger knows), carrier/mechanism flips are DESIGN moves (change the estimator);
never let the adjusted_score cell's 2.76 lift (survivorship) justify a design
spend. Settling check (named by the door, run in W0.3's rebuild): shadow-reclassify
idx 59 as died (the ledger is internally inconsistent on it: graph codes PASS, kill
index calls it a kill that missed a .99 gate) and re-run the permutation MI on
convention x outcome_5class; if it drops below significance, the campaign's top
single axis was one borderline record -- report next to the headline, do not bury.
Machinery: ledger-only (rides W0.3's rebuild). Cost: minutes.

**W0.6 INV4 closure -- the 52-record hand-read.**
Door: INV4_convention_is_not_flippable (negative result; closes the h2 census's
"cleanest untouched cluster", which is a convention flip and therefore already
covered by the screen). Action: hand-read the 52 died records whose result contains
'ratio' + a compute token (idx 10, 12, 17, 18, 19, 24, 26, 27, 31, 34, 40, 42, 46,
48, 54, 61, 68, 69, 107, 110, 111, 112, 133, 134, 135, 137, 138, 139, 143, 151,
155, 199, 203, 204, 228, 231, 233, 244, 245, 246, + the remainder returned by the
loosened screen). Falsifier: one record whose reported raw-ratio x C-ratio < 1 with
a kill_condition written in raw currency. Machinery: reading pass (subagent-fit),
ledger-only. Cost: one pass, no CPU.

**W0.7 D2 dtype closure -- two recurse rungs, then write the facts into the ledger.**
Door: D2_strict_headroom (licensed audit, zero revival payoff -- corrections
carried: M205's headroom reason already retired; only M203 still rests on H and
stays over at 2x H).
Action: recurse rung 1 -- reconstruct M151's 256x49 reverse map (497,428,480) and
49-node pointwise allowance (138,165,760) from m124_shared_k3_projector/
m124_protocol.py closed forms, f32 vs f64 (validator: exact integer match; the
forward map already reproduces exactly as f64 via 31*1.25*2*(2*49*256*256-49*256)).
recurse rung 2 -- reconstruct batched_path_and_22_k640 = 16.2402304B (= 1.25 x the
M133 kappa2 12.9896448B row) f32 vs f64. PASS/FAIL prewritten: both f32 or
non-dtype-scaled -> H_max = 3.284566192B, audit closes permanently, zero revivals;
reverse map f64 -> ceiling 3.850750192B, still zero revivals.
THE DELIVERABLE either way: record the discharged legs as ledger fact -- M133/m126
booked at float32 (52,963,875,280 reconstructs exactly at square_f32), the
3.2474112B five-product term f32, M179 scalar counts -- so no future generation
reopens the 80.326B row as a dtype lever. Machinery: recurse CLI (--json, exact
validator) + ledger annotation. Cost: ~10 minutes.

**W0.8 INV8 provenance leg (recurse) + rerouted composition leg.**
Door: INV8_kerdock_residual_base_upgraded (operator leg OBSERVED in source this
round: kerdock_v3's deployed route IS RowBlockedBatchedWinograd at BLOCK_ROWS=4096,
matmul 98.94% of bill -- the portability link stands).
Action: recurse rung -- re-derive the DESIGNATION_POLICY kerdock row with net 1's
halves (0.724959 analytical / 2.212485 residual) and the 2-net mean (0.718502)
in place of the net-0-only 0.712046; validator: the tau >= 0.2000 window arithmetic.
If the window closes under either, the row is not designation-ready regardless of
portability. Composition leg (fold bill denominator vs kerdock's 98.94% matmul lane,
the ~1.06% phased-WHT lane excluded or separately priced): FENCED tree -- route via
headroom/FWHT_SPLICE_STAGED_20260818.md or hand to the fold-tree owner. Post-halt
this leg is manuscript-accuracy work, not designation work; priority accordingly.
Freshness caveat carried: run_config whestbench 0.14.0, 2026-08-08,
residual_wall_time_limit_s null; evaluator-drift finding applies.
Machinery: recurse CLI + one rerouted file read. Cost: minutes.

**W0.9 Label-epoch settlement -- GATE FOR EVERY CARRIER MUTATION.**
Door: INV7's refined falsifier.
Action: read the frozen artifact kerdock_phases.npz
(experiments/v31_guards/package_source/ -- not fenced) against the two definitions:
idx 68's correction ("126 independent Haar frames with degree 2 exactness, not a
Kerdock MUB") vs the idx 179+ definition (126 phased-Hadamard frames Haar-rotated
per net). Falsifier: if kerdock_mub and haar_random_spherical name the same physical
carrier at two label epochs, the carrier axis is a documentation-vintage artifact
and EVERY carrier-ranked door dies (including W3.2). The researcher's
arXiv:1104.4692 result (Z4-Kerdock = exact spherical 3-designs / unitary 2-designs,
strictly stronger than degree-2 exactness) makes the swap a real premise change IF
the labels are genuine -- settle from the artifact, not prose. Cross-reference: the
ultramath slate's carrier entry asserts the idx 68 geometry premise "checked, not
violated" -- reconcile both readings in the note.
Machinery: one np.load script (python -B) + record read. Cost: minutes.

**W0.10 m184 scope annotation (bookkeeping; rides W1.1).**
Door: m184_exhaustion_clause_is_over_broad.
Action: annotate idx 194 -- the kill is SOUND (0.00% on 3 nets, certain-on zero at
layers 1-9, held under illegally lax alpha) and is NOT reopened; only its clause
"Kerdock v3 is evidence-optimal within known mechanism space" is narrowed to the
sparsity/composition mechanism it measured. Correction carried IN the annotation:
the compiler_schedule route it failed to consider delivers 5-8.6% billed reduction,
BELOW m184's own 15% kill / 20% promote bars -- the scope fix is hygiene, not
optimism. Static leaf-shape applicability check (idx 268's 49-leaf m/4,k/4,n/4
route vs v3.1's 28 deep hooks + separate WHT first-product path) runs ONCE inside
W1.1 and serves both this annotation and G-A. Machinery: ledger annotation.

### WAVE 1 -- the one design door's paper gates (static, zero billed FLOPs)

**W1.1 G-A eligibility gate.**
Door: kerdock_host_depth2_winograd_schedule_pass (the MERGE: idx 58/59 fused-L2
operator and idx 268 l2-fringe schedule are one door with two operators -- never
two predeclarations).
Source flips, cited with intent preserved:
- idx 42 exact_sampler_rectangular_strassen (kill) -> idx 53 (win): CONVENTION
  flop_cost_wall -> adjusted_score; the record itself says the algebra passed, only
  the allocation graph died -- "preserve the rectangular/ragged formulas and reopen
  only with preallocated out= buffers." The reopen condition is exactly idx 72's
  ownership discipline.
- idx 48 integrated_batched_winograd (kill) -> idx 53: CONVENTION memory ->
  adjusted_score; killed at 667.328 MiB against the retired 512 relic after passing
  7/8 gates at ratio 0.941206.
- idx 16 latent_randomized_radial_development100 (kill) -> idx 53: MECHANISM_FAMILY
  transport -> compiler_schedule on identical carrier/f32/adjusted-score premises
  (the analytic lane died 96.1178x worse while the schedule lane on the same premise
  became the production win).
Composition verified unwalked: records naming '6144' = {58, 59, 69, 275}; records
naming 'kerdock' = 41; intersection = 0.
Gate: enumerate the 28 deep-hook matrix products on the frozen kerdock_v3 entrypoint
(sha 076D0A5D...), mark those covered by m71's row-local 4096-row Winograd transfer
and the phased-WHT layer-1 butterfly (ineligible by construction). PREDECLARE
CLOSED if eligible effective-bill share < 9%. Use idx 264's measured methodology,
NOT a depth-1 dispatcher figure -- the 57.4164% -> 8.98%/6.12% error is precisely
what killed U-F1 (idx 264: "every U-F1 score number re-banked a saving the champion
already holds", r_prod 0.88015058).
Machinery: static source enumeration (~1 hour).

**W1.2 G-B residual gate -- the gate that actually decides it.**
The dominant unpriced risk (neither original candidate priced it): the residual
channel, the same channel that killed idx 69 (6144-row child: analytical 0.965263
-> effective 0.989874; residual 0.194269 s; peaks 503.324/510.926 MiB) and ate 69%
of idx 59's analytical cut. Gate: project delta-residual from the operator's own
dispatch-count delta on the Kerdock route; PREDECLARE CLOSED if projected residual
increase exceeds projected FLOP saving at lambda = 1e11. Empirical basis: idx 69's
measured 0.194269-s point and idx 59's measured 69% erosion. Do NOT use the
2026-08-19 slope law (refuted in round 1(b)).
Depth is settled and closed deeper: both operators are depth-2 (idx 58's 49-leaf
batched matmul; idx 268's two levels, 49 leaves at m/4,k/4,n/4); idx 264's
integrated optimum is depth 2 at 1.057x and the competitor census (AIcrowd 18157)
independently measures optimal depth 2 with depth 5 an ~18x loss.
Machinery: recurse CLI arithmetic rung. Cost: zero compute.

### WAVE 2 -- wall-time evidence, off the harness queue, owner-scheduled

**W2.1 X5 k=1 hostile-residual re-instrumentation.**
Door: x5_hostile_residual_convention_is_unmeasured (record 255 restated; premise
real: the k=5 hostile-projection leg is the SOLE failing gate on idx 143, 151, 155,
158 -- idx 158 verbatim: "All structural invariants and RSS pass, but two of five
hostile projections exceed 258.4B; maximum 278.273084846B"). Decisive arithmetic
reproduced digit-for-digit on the frozen 2026-08-07 basis: worst worker billed
193.371731851e9; residual 0.169802706 s; C_k1 = 210.352002450e9 vs the 258.4e9
gate; ceiling 0.650282681 s; k* = 3.8296. The detector is mechanically blind to
this class (19 'inconclusive' records participate in ZERO of 1,457 doors) -- this
cell would put the first measured decided record into that class.
Protocol (record 255's judge-specified experiment, verbatim constraints):
participant thread pinned to ONE physical core with the BLAS/flopscope backend free
on ~7 (per-thread affinity or out-of-process backend; a process-wide mask provably
cannot express the asymmetry -- that is what disqualified ARM A); identical
k=1 / 258.4e9 gate; REPORT A DISTRIBUTION over repeated residual draws per worker.
VOID-conditions carried from ARM A: setup within 8x of the 1.48-1.58 s baseline;
within-process first/second residual spread < 1.2x -- else VOID, not decisive.
Named risks: the hosted #326094 k~=1.0 agreement is REPORTED-level, never
re-verified; the favorable second-predict pass had a 1.25% worst-case margin
(255.157B vs 258.4B on w3) vs 18.60% on the frozen basis -- a correct re-run can
still land against the gate.
GOVERNANCE PARAGRAPH (mandatory in the cell spec and any memo, pre-written):
re-pricing OUR OWN internal k=5 projection down to the measured k=1 convention is a
correction of a campaign-internal convention. It is categorically NOT an
exploitation of the organizer's known, deliberately unpatched lambda pricing gap
(AIcrowd topic 18108; Dipam 2026-08-07 declined to re-price for Phase 1, choosing
manual review and disqualification of exploiting submissions). Any write-up states
the distinction explicitly; the two are one sentence apart and must never blur.
Machinery: fold_search cell, convention=residual_walltime, ~45 min wall, five
frozen workers, on a machine that is NOT running the 129 chain. Queued behind
owner scheduling. Payoff: M157-family revival decision (32 calls, 7.439248181e9
billed, 701 -> 669 dispatches) on measured rather than projected residual.

### WAVE 3 -- sealed design cells, predeclared only if their gates pass

**W3.1 The schedule-pass cell (the one design door).**
Predeclare ONLY if W1.1 and W1.2 both pass; run ONLY when the owner allots harness
time post-129. Spec sketch (fold_search REQUIRED_FIELDS):
- id: kerdock_host_depth2_schedule_r1 (collision-scanned vs killed ids; differs
  from idx 42/48/69 by the changed mechanism the harness's reopen rule requires:
  fused/fringe L2 on the Kerdock host WITH caller-owned out= buffers per idx 72).
- hypothesis: the depth-2 L2 schedule lane, which on the Haar host produced the
  production win (idx 53) while its Kerdock-host siblings died on
  convention/memory/residual walls, transfers to the Kerdock host at effective-C
  ratio <= 0.98.
- baseline: unfolded kerdock_v3, local adjusted 1.6190837992e-7, public-100 paired.
- operator: idx 59's frozen seven-file tar (sha256 68259f6443...) preferred on
  evidence quality; idx 268's route (effective_compute_ratio 0.949914, flop_only
  0.914492, n_nets 24, PASS_SCREEN) on magnitude. The 0.95-vs-0.9893 projection gap
  is DECLARED as an observable: one of them is mispriced and the run must say which.
- thresholds (mechanical): PASS if paired effective-C ratio <= 0.98 AND worst
  per-net Frobenius parity <= 2e-5 (round-4's bar) AND peak < 1 GiB (ruled ceiling;
  65,536 MiB enforced limit declared alongside); KILL if ratio > 0.995 or parity or
  memory breached; gray zone INCONCLUSIVE, never promotes.
- budgets: wall per net capped at the incumbent's receipt +10%; BUDGET_KILL_WALL
  fail-closed; residual reported as a distribution (W2.1's lesson).
- seeds: paired public-100, matched_units >= 100, seed policy identical to idx
  183's rescore; inputs sha256-frozen at predeclare.
- confidence axes at predeclare: implementation high (frozen tar), mechanism medium
  (residual projection is the weak leg), generalization low (local only),
  compliance high.
- depends_on: W0.2 (memory tiers recorded), W1.1, W1.2.
Expected value, honest: idx 268 projects ~0.95 (1.619e-7 -> ~1.54e-7); idx 59
projects 0.9893 (~1.604e-7). Modest; the run is worth its CPU only because both
gates are free and the premise is the production win's own.

**W3.2 Sampler lane on the Haar host (mutation engine; NOT Ralph-vetted -- flagged).**
Source: the MI graph's aggregate note, not a gated Ralph survivor. Kill-side
records idx 179 (t2_fullcov), 185 (n7_rqmc_scaling_law_probe), 186
(n8a_rqmc_kerdock_draw_stage), 188 (n8c), 191 (m180), 193 (m181) all form CARRIER
doors kerdock_mub -> haar_random_spherical into the promoted idx 0 h4_random32256,
sharing (precision unstated, convention raw_mse_variance, mechanism sampler): the
sampler/RQMC lane was tested on the Kerdock host and never re-run on the Haar host
that actually holds the promoted fold.
Gates before any predeclaration: (i) W0.9 label-epoch settlement -- if the two
carrier labels are one physical carrier, this door is void; (ii) the ultramath
deg-4 exclusivity rule prices what a carrier-adjacent sampler port can buy (ONE
~16% channel; swap 0.797 / completion 0.842 / selection 0.89-0.93 predeclared --
rank, never sum); (iii) the F7 collision constraint (governance item 6): do not
fund this and the F7 proxy family in the same round.
Machinery: mutation engine (carrier port) + one fold_search cell if funded.
Status: sealed sketch only; lowest design priority behind W3.1.

**W3.3 Zonal settling probe (optional; manuscript-strengthening negative control).**
Source: F4's named settling check; kill-side population is the zonal cell itself
(31 kills of 33, e.g. the 'carrier: zonal_harmonic -> none' direction carries 37
doors). Action: score ONE zonal/harmonic candidate on the promoted Haar host to
separate carrier-reason death from cost-reason death -- upgrading the manuscript's
"zonal is a dead carrier" claim from correlational to causal. Machinery: mutation
engine (carrier flip) + one cell. Fund only from surplus; its EV is evidence
quality, not score.

### M -- manuscript integration (the track, per ruling 2)

**M.1 File the MI graph as the kill ledger's quantitative form.**
Into PHASE2_CONTRIBUTION_DRAFT_20260819.md: (i) the axis-silence result (F1) with
the artifact rows flagged as construction identities; (ii) the convention
verdict-form result (F2) as the measured form of the context-indexed-kills
doctrine -- the doctrine's axes predict which wall, not which fate; (iii) the H0
census (F3: 114 cells / 28 both-outcome / 140 records / 191 pairs vs 1,457 door
edges) with idx 71/72 as the named twin pair; (iv) the zonal dead cell (F4) at its
earned level with the settling check named (upgraded if W3.3 ever runs); (v) the
coder-bug forensics (sub-MISS-ion, prob-ABI-lity, boilerplate harness tokens) as
instrument-honesty disclosure; (vi) the ownership-axis result (W0.4) whichever way
it lands, labeled. The strategic verdict names the winnable track's centre of mass
as "A4 theorem + carrier-free per-call floor + kill ledger + disclosure" -- (i)-(vi)
are the third and fourth items.

---

## 5. What NOT to do (closed doors, walked-and-refuted, standing constraints)

1. Never rank a door by carrier MI (fails leave-one-win-out; a one-record relabel
   moves it 1.1 null SD). The haar <-> kerdock carrier flip proposals built on it
   are dead as MI arguments; only W0.9's artifact settlement can revive a carrier
   premise, and then only through the deg-4 exclusivity pricing.
2. Do not reopen: idx 71 (own 480-MiB clause), idx 48/70 (dominated by idx 50/72),
   idx 194's kill (sound; only its scope clause narrows), the flatness theorem
   (refuted, beta - phi undetermined sign), F8/GLS, the antithetic ladder, the
   high-degree carrier ladder, the owner's factorializing seed (all retired by the
   slate with corrected reasons).
3. Do not sum swap + completion + selection gains -- one ~16% channel, ranked.
4. Do not quote local scores as hosted; R = 1.652 is retired (honest map R ~= 1,
   13% adverse anchor). The 129 judge's pre-stated arithmetic stands: r < 0.7453 is
   below the pre-registered PASS band, so that cell's outcome is theorem evidence,
   not a designation flip.
5. Do not revive the fold as a designation candidate (halted by ruling; it retires
   gate-green as contribution artifacts).
6. Do not run a byte-identical respec of any killed cell (harness refuses; the
   reopen rule requires a changed mechanism -- W3.1 satisfies it via ownership
   discipline + host, W3.2 via host).
7. Do not let any python touch repo trees without -B / PYTHONDONTWRITEBYTECODE=1.
8. Do not touch experiments/fold_floor_splice or experiments/frame_completion_129;
   the 129 chain's seeds are consumed only by its own sealed predeclaration.

---

## 6. Sequencing summary

```
NOW (parallel, zero compute, no harness):
  W0.1 v3.1 memo        W0.2 memory tiers+debt   W0.3 MI repair+rerank
  W0.4 ownership axis   W0.5 governor (+W0.3)    W0.6 INV4 52-read
  W0.7 D2 recurse x2    W0.8 INV8 recurse        W0.9 label epoch
  W0.10 m184 note
THEN (static gates, still zero compute):
  W1.1 G-A eligibility  -> W1.2 G-B residual     (shared enumeration with W0.10)
OWNER-SCHEDULED (wall time, off the 129 box):
  W2.1 X5 k=1 re-instrumentation (+ governance paragraph)
POST-129, IF GATES PASS AND OWNER FUNDS:
  W3.1 schedule cell (ONE run)  >>  W3.2 sampler-on-Haar (gated x3)  >>  W3.3 zonal
CONTINUOUS:
  M.1 manuscript integration of the graph (with W0.3/W0.4 outputs as they land)
```

Every wave-0 item is reversible and ledger/file-local; the only irreversible acts
in this plan (cell predeclarations, which consume one-shot ids under kill finality)
are deliberately deferred to the harness owner at queue time.

---

## 7. Residual risk of this synthesis

- The MI statistics are plug-in estimates on 257-276 records; only the
  MI-minus-null column is read anywhere in this document, and p resolves to about
  +/-0.01 at 200 shuffles (demonstrated by the two independent carrier null draws,
  p=0.0199 / 0.0299). W0.3's rebuild may move numbers; the fragility gate exists
  for exactly that, and no scored spend in this plan depends on an MI ranking.
- 'pass' counted as lived includes descriptive screens; survival-as-promotion has
  only 4 positives and supports no MI test. Claims here are phrased against the
  lived/died coding and say so.
- The door verifications are REPORTED from the Ralph rounds; this synthesis
  spot-checked their machinery-facing claims (harness contract, artifact existence,
  fence mtimes, channel state) but did not re-run their arithmetic beyond the
  cross-checks noted inline (e.g. the ~1% mean-of-products spread in the v3.1
  relative-change figure, immaterial at 1.4e-7).
- The channel is the governing record; if an owner ruling between this stamp and
  execution changes the 129 chain, the memory ceiling, or the manuscript priority,
  the wave order re-files against the new ruling -- the doors themselves do not
  change, only their queue positions.


---

# ADDENDUM 2026-08-19 -- W0.6, W0.9, W0.10 settlements

Stamped: 2026-08-19 (appended after the 09:09:44 UTC synthesis; append-only, nothing
above this line was altered). Scope: the three small wave-0 settlements only.

Compliance for this addendum: zero compute -- no harness, no FlopScope, no estimator
run, no scored row. Every python invocation used `python -B` with
`PYTHONDONTWRITEBYTECODE=1`, and touched only JSON ledger/graph files and one frozen
4,376-byte `.npz`. `experiments/fold_floor_splice` was neither read nor written.
`experiments/frame_completion_129` was read-only (two `sha256sum` calls on its two
frozen phase-asset copies, no write, no seed consumed); `cells/` was neither read nor
written. No cell was predeclared.

Evidence tags: **[O]** observed by this pass (a command run here, a file read here),
**[D]** derived from an [O] by arithmetic shown, **[R]** reported by a corpus record
or upstream stage and not re-derived here.

---

## W0.6 -- INV4 closure: the hand-read

**Verdict: INV4 holds. Convention is not flippable inside this class.** No record in
the read set reports the pair the falsifier needs -- a raw-currency kill_condition
together with a reported raw-ratio x C-ratio product below 1. The records that *do*
report both numbers (idx 46, 48, 69) were killed in engineering currency (wall,
memory, residual), not raw, so their repricings are already banked in W1.1/W1.2 and
are not INV4 findings. The nearest miss is idx 31, whose raw ratio 0.866761 sits
against a routed compute the record states only as "below the parent" and never
quantifies [O, mi_graph node 31] -- direction-consistent, numerically unevaluable, and
therefore not a falsifier hit. This is the negative result the door predicted.

**On the count.** The plan names 40 idx explicitly and defers the rest to "the
remainder returned by the loosened screen"; that screen is not reconstructible to
exactly 52 from the committed artifacts. Reproduced here on `mi_graph.json` [O]:
died-outcome records whose result text contains `ratio` = 79; adding a compute-token
conjunct (flop/cost/compute/budget/bill/cmax/wall) = 58; the plan's 40 are a strict
subset of the 79 but not of the 58 (idx 10, 12, 34, 54, 107, 133, 134, 135 carry
`ratio` with no token from that list). The count 52 is therefore **[R]**, not
reproducible here, and the read below is the defensible superset: the plan's 40 named
died records plus all 19 inconclusive records = **59 records**.

**Why the inconclusive class belongs in this read.** Verified here: of the 1,457 door
edges in `doors.json`, **zero** name any of the 19 inconclusive idx in either the
`kill_idx` or `win_idx` position [O]. The graph is mechanically blind to the class, so
every priced statement in it is unpriced evidence by construction. That is the second
independent signal for the plan's W2.1 claim, which had stood at [R].

### The 40 INV4-named died records

- **idx 10 latent_factor_rank3** -- corpse. r=3 leaf 27.22% worse than r=2 at ratio 0.0602767; the aliasing diagnosis ("extra tensor resolution is aliased by unchanged three-component compression") is already carried by idx 17/18. Nothing unpriced.
- **idx 12 latent_sparse_radial_harness** -- DOOR (small, W0.2-adjacent). Kills the in-process harness, not the mathematics: "the frozen tau=0.5 mathematical candidate remains pending until a separately killable, repaired premise is frozen." A live unbilled reopen condition, memory-shaped (two PIDs to 24.6 GB / 13.8 GB against a 2 GB clause) -- reads directly onto the three-tier memory record W0.2 is writing.
- **idx 17 latent_full_sigma** -- corpse with a preserved theorem: "eliminating rank collapse and matching second moments is insufficient because ReLU gate/angular structure is aliased." Manuscript line (M.1), not a door.
- **idx 18 latent_gate_aligned_split** -- DOOR (evidence-only). 8/8 improvement at aggregate ratio 0.997502, killed for missing a 0.8 materiality bar "by about 80x in effect size". The boundary direction and exact conditional moments are explicitly preserved as stable-sign components; the unpriced part is the 8/8 direction-of-effect, which no wave currently reads.
- **idx 19 latent_gate_label_memory** -- corpse. Label memory erased 84% of its parent's gain; the finding is a negative constraint on regrouping, already inherited by idx 24.
- **idx 24 latent_gate_response_gram** -- corpse with a numeric that matters for W0.5's survivorship argument: ratio 0.997502340 differs from the marginal-only child by 2.14e-8 relative. Two records at the same three-decimal ratio (18 and 24) with opposite mechanisms is a clean instance of "the ledger knows which wall".
- **idx 26 randomized_radial_susceptibility_compressor** -- DOOR (W0.4-adjacent, weak). "covariance contributes 99.35% of generic observable-error energy" is an unpriced decomposition; the kill is single mean/gate geometry, and the invariant pullback is preserved.
- **idx 27 randomized_radial_dual_observable_compressor** -- DOOR (evidence-only). The gate/active Gram cosine falling 0.72-0.75 at layer 0 to 0.15-0.25 late is a measured depth law nothing in the plan reads; it is the same depth-dependence idx 33 (inconclusive) proposed to exploit.
- **idx 31 physarum_moe_router** -- NEAREST-MISS on the INV4 falsifier (see verdict above), and a hard ceiling: the frozen bank's best-pure ratio 0.833818 / best-convex-top2 0.829054 bound any router at 0.80. That ceiling is unpriced and closes a family, so it is manuscript-usable (M.1).
- **idx 34 ecn_jacobian_maxent_compressor** -- corpse, and idx 246 already measured the repair (exact psi is *worse*, 1.0065 vs 0.9115). Closed from both sides.
- **idx 40 randomized_radial_inverse_residual** -- corpse with a live rule: a genuine control needs nonconstant g(X), independently known exact E[g], and equal-compute ratio kappa(1-rho^2). That inequality is the correct screen for any future control-variate proposal; currently written nowhere as policy.
- **idx 42 exact_sampler_rectangular_strassen** -- DOOR, already banked as a W1.1 source flip. Its reopen condition is verbatim ownership discipline ("reopen only with preallocated out= buffers"), so it is also a W0.4 labelling anchor on the `caller_owned_inplace` side.
- **idx 46 preallocated_strassen_winograd** -- DOOR, unpriced and currently unread by any wave. Ownership was *repaired* (residuals .000263/.000440/.000527 s, effective ratios .882057/.885099/.886148) and the kill moved to wall (1.55874/1.54559/1.70148 against a 1.5 gate). Its own diagnosis -- "the failed link is one-core half-width BLAS plus Winograd memory traffic, not allocation" -- is the same one-physical-core instrument question W2.1 exists to settle, and its scorer proxy tolerance (raw-MSE ratio below 1.129817 at effective ~0.885) is an adjusted-score headroom figure nobody has repriced.
- **idx 48 integrated_batched_winograd** -- DOOR, banked in W0.2 + W1.1. Adds one number the plan does not quote: end working set 478.883 MiB against peak 667.328 MiB, which localizes the excess to transient workspace/activation overlap -- the W0.4 axis in measured form on a single record.
- **idx 54 quadratic_chaos_fourier_resummation** -- corpse. Ratio 1.000303 against a 0.90 bar; the preserved scalar characteristic-function operator has no consumer in any wave.
- **idx 61 direction_only_facet_raoblackwell** -- corpse. Killed on two independent edges; the global distributional-Laplacian identity is preserved and is already claimed by idx 83's Phase A. Manuscript only.
- **idx 68 strict_band_boundary_ridgelet_control** -- DOOR, and it is the W0.9 gate's own source record. Its carrier sentence is about the **formal L1 geometry**, not about the Kerdock phase asset; see W0.9 below. Also unpriced: the 48-frame synthetic audit's mean support Jaccard 0.510 (support instability), which is the honest reason the band family is closed.
- **idx 69 residual_aware_l2_dispatch_ladder** -- DOOR, banked as W1.2's empirical basis. One unpriced clause the plan does not carry: "alias-liveness changes FlopScope billing" -- a billing-instrument dependency on ownership, i.e. the W0.4 axis contaminating the cost axis. That is worth a line in W0.3's honesty disclosure.
- **idx 107 m108_centered_cymatic_band_energy** -- corpse, cleanly. Variance ~2.05e23 against a cap of 50; killed pre-execution by exact quadrature. The preserved identity is a "nonworking puzzle atom" by its own text.
- **idx 110 m111_coherent_gate_interferometer** -- corpse. All four held ratios above 1; the ungated-transport premise is refuted, and idx 111 refutes its successor.
- **idx 111 m112_connected_gate_kernel** -- corpse with a permanent sentinel blocking retry. No door.
- **idx 112 m113_connected_hermite_vertex_engine** -- corpse; a 74.35x threshold miss with no serialized artifact.
- **idx 133 m134_hermite_graph_degree_sampler** -- DOOR (theory-side). The divergence table (partial sums 3.099 to 7667.42 at horizons 12..32, rho=.975) is an unpriced non-integrability certificate; it is the precise obstruction any future analytic resummation must clear, and the plan's ultramath lane has no equivalent.
- **idx 134 m135_conditional_lowrank_source** -- DOOR (theory-side, strong). A real 14.0x Rao-Blackwell win on a true rank-2 state, killed only for generic deployment (r>=234 at width 256; density-ratio second moment infinite for every r<=234). That is a measured conditional win with an exactly stated domain -- usable as a manuscript negative-result-with-a-positive-core (M.1), unpriced anywhere else.
- **idx 135 m136_equivariant_diagram_transformer** -- corpse. Held ratio .682857 with a bootstrap upper 0.994847 against a required upper below 0.5. Its own text demotes its FLOP figures to "conditional arithmetic worksheets, not accounting certificates" -- worth reusing verbatim as a W0.3 instrument-honesty example.
- **idx 137 m138_balanced_triple_sampler** -- corpse; nonmonotone/adverse width trend, confirmation set unopened. The balanced finite-population sampler is preserved as an operator with no consumer.
- **idx 138 m139_positive_partial_trivariate_proposal** -- corpse; width means worsen 0.903629 to 1.148153 across widths 5 to 6. Adverse trend, closed.
- **idx 139 m140_quadratic_residual_control** -- DOOR (theory-side). The Khatri-Rao obstruction (the masked aabb split-pair action has no installed exact cubic contraction) is an unpriced structural blocker, and the anticorrelation finding (residual/full influence-variance ratios 1.2216, 1.2662, because the zero-mean jet is anticorrelated with the nonzero-mean vertex) is a reusable warning for any jet/residual split.
- **idx 143 m145_defensive_acg_transport** -- DOOR, and it is W2.1's population. Killed only by the 5x hostile residual projection exceeding budget in 2/5 runs; every other leg passed. Directly repriceable under W2.1's k=1 convention.
- **idx 151 m153_exact_formal_prefix_reuse** -- DOOR, W2.1 population. Its two failing runs overrun by 2.394B and 1.008B, the smallest margins stated anywhere in the family (idx 155's overrun is 19.873B: 278.273B against the 258.4B gate). Whether the k=1 reprice clears them is unmeasured; the record is unpriced either way and is the cheapest one to reprice.
- **idx 155 m157_selfhosted_formal_pilot** -- DOOR, W2.1's named payoff (32 calls, 7.439248181B, 701 to 669 dispatches, peak RSS 387.152 MiB). Already in the plan; the read confirms the record text matches the plan's quotation.
- **idx 199 m185_tail_pruning_mechanism** -- DOOR (evidence-level, unpriced). "PRUNING IMPROVES adjusted ~21%" while unpruning bills +41% for a ~6% MSE cut -- a measured convention result (raw-MSE trap vs adjusted) that W0.5's governor asserts as doctrine but never cites. It is the cleanest empirical instance of the governor's rule in the whole ledger.
- **idx 203 m191_harmonic_cv_g0b** -- corpse with a manuscript number: the 24-function basis reaches R^2 of 0.23-0.29% against a leak that is real, so "the leak is real and unscoopable by any tractable projection". Feeds M.1; nothing to reopen.
- **idx 204 gen3_p2_rotation_selection** -- the single largest unpriced item in the read set, and now **priced shut from one side**. Oracle-of-8 = 61.6% MSE gain (CI [48.8%, 66.8%]) is real and unharvested; its reopen requires a weights-only / O(1)-frame proxy with a sub-5% selection stage. idx 245 subsequently proved by construction that the estimator's own zero-sample diagnostics are exactly rotation-invariant (max relative deviation 4.558e-7 over 48 pairs), so the entire weights-only class has identically zero within-net signal. The headroom survives; the named reopen route does not. That pairing belongs in the manuscript (M.1) as the campaign's sharpest "real gain, no reachable estimator" result -- it is currently stated in neither record alone.
- **idx 228 s6_bloch_design_bragg_spectrum** -- DOOR, and it is a load-bearing input to W0.9. Kill at 0.32% against a 5% concentration bar, but the corpse is the design's complete algebraic anatomy: spectrum exactly three shells, inner-product fingerprint exactly {0 within-frame, +/-1/16 cross-frame}, degree-4 constant mode suppressed 42x while the degree-6 constant mode is unsuppressed (1.015). W0.9 below re-derives that fingerprint independently from the frozen asset.
- **idx 231 s9_crofton_kink_transect_identity** -- DOOR (manuscript-grade mathematics). A new verified exact representation, killed as an estimator at 176,860x variance-per-FLOP. Two independent implementations, same verdict; error laws confirmed independent of the champion (pooled r 0.055 +/- 0.15) with inverse-variance gain 0.3%. The independence measurement is unpriced and is exactly the combinability evidence any future portfolio argument needs.
- **idx 233 s11_full129_reopen_measured_breakeven** -- DOOR, and it is the methodological control the campaign should reuse everywhere: raw completion gain +3.42% collapses to +0.176% (CI [0.970, 1.028]) once a matched-point random-frame control absorbs the generic more-samples effect. The control design, not the result, is the transferable asset. It also fixes the 129-completion's value as near-zero on this host, which is the premise W3.2 and the ultramath carrier entry both lean on.
- **idx 244 gm_a1b_diffflag** -- corpse, strengthened. Perfect-oracle precision 0.2818/0.5004/0.5365 against a break-even precision of 0.7211 on the score-relevant column: negative suite gain even with a zero-cost zero-error oracle. Unpriced clause worth carrying: at the rejected p2_high anchor the mse_corr gain turns marginally positive (+0.12%), so the strengthened kill rests specifically on the mse_raw column.
- **idx 245 gm_p2b_proxy** -- DOOR in the negative direction (see idx 204). Killed at step 0 by proof, not correlation; all three diagnostics take exactly one distinct value across the 16 rotations of each net (abs_spread and rel_spread exactly 0.0). This is a *theorem-shaped* kill and the plan does not cite it anywhere.
- **idx 246 gm_ecn_psi** -- corpse. Exact psi is worse than the surrogate (1.0065 vs 0.9115), better on 0 of 32 paired units, with all 10 structural sub-gates passing -- so the failure is the mechanism, not the plumbing. Closes idx 34 and idx 35 together.

### The 19 inconclusive records (zero door participation, verified)

- **idx 11 latent_sparse_radial_cubature** -- proposed, never run; its premise is superseded by idx 17/18's aliasing finding. Corpse by supersession.
- **idx 33 flatworm_response_ladder** -- DOOR (unrun premise, still live). It is the two-lane depth controller that idx 27's measured cosine collapse (0.72-0.75 to 0.15-0.25) would test. Premise and its own measurement now exist in the ledger and have never been paired.
- **idx 35 ecn_exact_jspace_psi_streaming** -- closed by idx 246 (which is exactly this mutation, measured). Retire the proposal rather than leave it inconclusive.
- **idx 79 m80_kerdock_tangent_factorial** -- DOOR. Pooled ratio 0.9793799245 with three LONO ratios below 1 but seed 271 adverse at 1.0044348946; neither the survivor gate nor the prescribed removal kill is established. The stated unresolved edge -- the unmeasured cost of *deleting* the recurrence -- is a zero-variance cost question, answerable statically, and nothing in waves 0-3 answers it.
- **idx 81 m82_kerdock_vs_haar_variance** -- the most valuable unpriced record in the class, on two counts. (i) It is a direct Kerdock-vs-Haar comparison whose bootstrap 95% interval [0.60132681, 1.35006446] crosses 1, so it neither supports nor refutes a carrier effect -- which is why carrier MI must not rank a door, independently of the leave-one-win-out argument. (ii) Its own confound statement names buffer ownership as the separating variable: "M71's owned-buffer/WHT source and formal L1's row-blocked backend differ in floating-point evaluation and operation schedule; this measures variance of two frozen implementations, not the causal effect of geometry." That is a second ownership-vs-geometry witness for W0.4, from a record W0.4 does not currently read, and it is *not* the idx 71/72 pair -- so it survives W0.4's acceptance clause (iv).
- **idx 83 m86_boundary_laplace_coarea** -- DOOR (Phase A only). Phase A verified to 3.33e-16 relative with a real correction found (raw all-layer zero labels cascade, up to 5 observed labels per ray, so raw gate summation double-counts). Phase B is closed by an enumeration lower bound far past budget. Manuscript-grade; the double-count correction is unpriced and would falsify any future facet-summation proposal on sight.
- **idx 145 m147_endpoint_safe_pair_bridge** -- component pass; the literal nested-endpoint route needs 606,720 angular evaluations per coefficient. That bound is the reusable artifact; the bridge itself has no consumer.
- **idx 154 m156_extended_domain_star_control** -- DOOR. "Promote only the domain-lift/compiler mechanism" is an explicit unconsumed promotion: five fresh native traces, 155 GEMMs, 10.426269184B billed, projecting to 99.2165-99.5990B under the 5x new-wall convention. That projection is in the same 5x currency W2.1 is re-measuring at k=1, so this record reprices with the same instrument.
- **idx 157 m159_scale_normalized_endpoint_abi** -- DOOR (narrow). The scale carrier and robust L2/Linf gate are valid under one common source normalization; the generic noncentral trivariate evaluator is the missing provider. Same blocked-provider shape as idx 211/214/220 -- see below.
- **idx 163 m165_rank_face_subtraction** -- component pass; rank-two needs a distinct anchor/tangent. Corpse unless the 606,720-op cap gets a native certificate.
- **idx 195 gen3_recursion_packet** -- orchestration, not a candidate. Its P2 line is idx 204; nothing else unpriced.
- **idx 211 m196_m151_b1_native_provider_gate** -- DOOR of a specific kind: BLOCKED fail-closed for a *missing provider*, not for a failed mechanism. 3/3 contract tests and 5/5 inherited algebra tests pass. The exact identity is preserved and the blocker is buildable, not disproved.
- **idx 214 m199_composed_cost_reconciliation** -- DOOR, and the sharpest unpriced accounting fact in the class: BLOCKED_OVERLAP because M151's subtotal already contains M125b, leaving 1.986871472B of strict composed headroom before the provider, M172, M198, terminal, allocation and wall unknowns. Two independent audits agree. Any future composed-cost claim in this lineage must clear that number, and W0.7's D2 audit does not currently quote it.
- **idx 220 m205_rankone_complete_physical_owner** -- DOOR, same shape as 211/214: component pass to 2.665e-15 / 6.822e-13 / 5.685e-14 / 5.259e-13, blocked on an absent physical collision provider and native cost. Note for W0.7: the plan records M205's headroom reason as retired; this record's own block is *provider absence*, which the retirement does not discharge.
- **idx 250 gm_m116_streams** -- DOOR, and a governance case worth naming. Three gates measured and passed with two-plus independent signals each (bitwise parity 0 differing words of 16,515,072 across eight runs and both arms; peak 365.91-366.13 MiB against 464 MiB; bill exact in 8/8). The one decisive gate has zero valid signals because the *control* arm failed to reproduce (0.933445 s measured against 0.328465 s in the ledger, a 2.84x host factor, with the frozen wall aborting the first attempt). Dispatches fell 512 to 160 and residual 0.933445 to 0.419197 s, confirming call-count domination a third time. Two unpriced assets: the call-count law itself, and the precedent that a predeclared verdict mapping binds only conditional on the gate being measurable -- which is exactly the VOID discipline W2.1 needs and currently states only for its own arm.
- **idx 255 gm_residual_k1** -- DOOR, and it is W2.1's own record. The read confirms the plan's step-0 numbers verbatim and adds the disqualification detail the plan compresses: ARM A's instrument failed on three independent lines, one of them internal arithmetic (setup inflated 116.4x-191.6x when no reduction from 16 to 2 logical CPUs can exceed 8x), and the OpenBLAS pool-sizing artifact was ~43x larger than the effect under test. The plan's stated risk -- the favorable second-predict pass having only a 1.25% worst-case margin -- is confirmed as the record's own words.
- **idx 270 / idx 271 k32_base_sensitivity_v2 / v3** -- DOOR (paired, and the pair is the point). Both return the identical inconclusive metric 0.25 on disjoint seed triples, with the same gate (pass <= 0.1, kill >= 0.4). A reproducible inconclusive is a measurement about the *gate*, not the mechanism: the band was set too wide to decide, and two independent seed sets say so. That is a pre-registration lesson the manuscript can use and no wave currently reads.
- **idx 272 m207_reachability_v1** -- inconclusive with a branch_view of C_AMBIGUOUS; the width-monotonicity fingerprint it predicted is false (`kappa12_monotone_rising_with_width: false`). The negative result is clean and unpriced, but it has no downstream consumer in this plan.

**W0.6 net.** Of 59 records read, **34 carry unpriced evidence a later wave can use**
-- 21 from the named set (idx 12, 18, 26, 27, 42, 46, 48, 68, 69, 133, 134, 139, 143,
151, 155, 199, 204, 228, 231, 233, 245) and 13 from the inconclusive class (idx 33, 79,
81, 83, 154, 157, 211, 214, 220, 250, 255, 270, 271) -- and the remaining 25 are
corpses or already banked in W0.2/W1.1/W1.2. The four with the highest leverage, in
order, are **idx 204 + idx 245 as a pair** (real 61.6% headroom, reopen route proved
shut -- M.1), **idx 81** (a second, non-71/72 ownership-vs-geometry witness -- W0.4),
**idx 46** (repaired ownership with the kill relocated to one-core BLAS wall -- W2.1's
instrument question, on a record W2.1 does not read), and **idx 233** (the
matched-point control design, transferable to every future "more samples" claim).

---

## W0.9 -- Label-epoch settlement: **the falsifier does not fire; the gate PASSES**

**Verdict: `kerdock_mub` and `haar_random_spherical` do NOT name the same physical
carrier.** They name two different hosts' carriers, both current -- not one carrier at
two documentation epochs. The carrier axis is not a documentation-vintage artifact,
and carrier-premised doors (W3.2, and the ultramath slate's carrier splice) survive
this gate. The plan's standing rule that carrier MI never ranks a door is untouched by
this PASS and still stands on its own grounds.

### The artifact, by hash and shape

`kerdock_phases.npz`, 4,376 bytes, mtime 2026-08-06T17:12:41Z [O]:

| location | sha256 (file) |
|---|---|
| `experiments/v31_guards/package_source/` | `a5e747f95423f5a45972d1a0735f223044a8a998407eb0c6db51aefdf47f4906` |
| `experiments/v31_g4_call_fusion/candidate_source/` | same |
| `experiments/frame_completion_129/armB/` (read-only) | same |
| `experiments/frame_completion_129/armC/` (read-only) | same |

Codex clone `C:/Users/strid/Documents/Codex/2026-07-07/will-you-have-a-look-at/`:
82 copies of `kerdock_phases.npz` exist under `experiments/whest/` [O]. **81 of 82 are
byte-identical to the campaign's frozen asset** at the same sha256. The single outlier
carries `99d87597d5d513e0283fa1b4bf4539bca4b3000c6672e8b80b4c73b6cef964c8`, and its
path names its purpose:
`.codex-tmp/pytest-g13-v3-affected/test_phase_asset_drift_fails_c0/` -- a deliberately
mutated negative-control fixture for a drift test, not a rival epoch [O].

Contents, one member [O]:

- member `negative_bits.npy` -- `shape (128, 32)`, `dtype uint8`, C-contiguous;
  sha256 of the raw array bytes
  `b5d09cf42106e7ef1e481068cc9c08b1d90659392d8dd69181c7cbf648a527b7`.
- decoded as the deployed estimator decodes it
  (`unpackbits(..., bitorder="little")[:, :256]`, then `1 - 2*bit`): a +/-1 sign table
  of `shape (128, 256)`, int8 sha256
  `4b8dbd6c965310c52aa2be38153e86aeaa5042ab4fbe512b60b2d45d133a7540` [O].
- all 128 rows distinct; row 0 is the all-zero row; every other row has popcount 120
  or 136, i.e. row sum exactly +/-16 [O].

### The structural test, run here from the artifact alone

Walsh-Hadamard transform of each +/-1 row (exact int64, no floating point) [O]:

- **127 of 128 rows have a flat spectrum, |W| = 16 at every one of the 256 points** --
  the defining property of a bent function on GF(2)^8. The single exception is row 0,
  the constant function, whose spectrum is 256 at the origin and 0 elsewhere [O].
- **All 8,128 pairwise XOR sums of distinct rows are also bent** (flat |W| = 16) [O];
  restricted to the deployed trim `phase_start=2 ... phase_stop=128` the 7,875
  pairwise sums are likewise all bent [O].

A family of 2^(m-1) = 128 bent functions on GF(2)^m with m = 8 whose pairwise sums are
all bent **is** a Kerdock set. Because the estimator builds frame *s* as
`normalized H_256 * diag(phase_s)` and Sylvester rows satisfy
`H_ik * H_jk = H_{(i xor j)k}`, the row inner products follow immediately: exactly 0
within a frame, and exactly +/-16/256 = **+/-1/16** across frames, with minimum angle
arccos(1/16) = **86.4167 deg** [D].

That reproduces, from the frozen asset and with no estimator run, the fingerprint idx
228 measured on design points -- "inner-product fingerprint EXACTLY {0 within-frame,
+-1/16 cross-frame} (the MUB signature -- source of the arccos(1/16)=86.42deg min
angle)" [R, mi_graph node 228]. Two independent signals now agree on the same physical
structure: idx 228's Gram measurement, and this pass's algebraic test of the bit table.

### Why the two labels are two objects, not two epochs

Read this session in `experiments/v31_guards/package_source/kerdock_v3_estimator.py`
(unfenced) [O]:

- `setup` loads the asset, trims phases `[2:128]` and hard-asserts `(126, 256)`; the
  frames are the frozen Kerdock sign table composed with one normalized Walsh matrix.
  No direction array is retained.
- `predict` calls `_haar_rotation(int(mlp.seed), width)` and absorbs it into the first
  weight, emitting the trace stage `haar_absorbed`.

So on the `kerdock_v3` host the Haar element acts on **the network**, while the frame
family is **deterministic and frozen**. On the incumbent `row_blocked` host the
opposite holds: the entire direction set is redrawn per network by QR of an i.i.d.
Gaussian, and the tokens `kerdock`, `hadamard`, `phased`, `MUB`, `phase_start`,
`phase_stop` do not occur anywhere in that source tree [R,
`core/SECTION_ESTIMATOR_AND_CONSTANTS_ROW_BLOCKED_20260819.md`].

This resolves idx 68 cleanly and without contradiction. Its sentence -- "The formal L1
geometry is 126 independent Haar frames with degree 2 exactness, not a Kerdock MUB" --
is a true statement **about the formal L1 / row_blocked host**, made inside a
synthetic audit over 48 independent Haar-QR antipodal frames. It was never a statement
about the Kerdock phase asset, and the row_blocked disclosure section reaches the same
reading independently [R]. The idx 179+ definition -- 126 phased-Hadamard frames,
Haar-rotated per net -- is a true statement about the `kerdock_v3` host, and the frozen
asset just verified it structurally.

**Reconciliation the plan asked for.** The ultramath slate's carrier entry
(`carrier_fold_stack`) asserts idx 68 as "the geometry premise this asserts (checked,
not assumed)" while proposing to port the structured phased-Hadamard carrier *into*
the row_blocked host. Both readings are consistent under this settlement: idx 68's
finding is precisely that row_blocked has no such structure today, which is what makes
the port a real premise change rather than a relabel. The researcher's Z4-Kerdock
result (exact spherical 3-designs / unitary 2-designs, strictly stronger than degree-2
exactness) therefore applies to a genuinely different object [R].

### The residual risk this settlement leaves, stated plainly

The gate passes at the **artifact** level and only there. Two things it does not fix:

1. **The `kerdock_v3` host physically contains both elements** -- a deterministic
   Kerdock frame family and a per-net Haar rotation -- and the graph's carrier axis is
   single-valued, so it cannot represent that host faithfully. Checked here: idx 183's
   `axis_evidence.carrier_families_matched` is `["kerdock_mub"]` alone, so the coder
   never sees the Haar leg on the campaign's own reference Kerdock record [O]. That is
   a coder-level label collision the artifact-level settlement does not remove, and it
   belongs in W0.3's honesty disclosure and in M.1 item (v).
2. **The doctrine disagreements remain doctrine disagreements.** The graph reports
   carrier agreement 45/51 with exactly six named disagreements, read here in full:
   idx 38 (doctrine zonal, coded kerdock_mub), idx 39 (zonal to none), idx 43 (haar to
   none), idx 73 and idx 74 (kerdock_mub to none), and idx 106 (zonal to haar) [O,
   `mi_graph.meta.doctrine_validation`]. Nothing above adjudicates those; W0.3's
   relabel batch still owns them, and the fragility gate still governs. Note that idx
   106 is a doctrine-zonal record coded *haar*, so one of the six is itself a
   Kerdock/Haar-adjacent collision at the coder level -- which is consequence 1 above,
   showing up in the disagreement list rather than in the artifact.

Settling check, named and unrun: a Gram/rank assertion on one block of the *deployed*
row_blocked direction store -- the ultramath slate's own gate (a) -- would upgrade
consequence 2 of the row_blocked disclosure from [R] to [O] on this machine. Cost:
one array read, zero compute.

---

## W0.10 -- m184 scope annotation (draft text; the frozen cell is NOT edited)

The frozen cell was not opened and idx 194 was not modified. What follows is the
annotation text for the ledger, drafted for the harness owner to attach.

> **ANNOTATION -- idx 194 `m184_trichotomy_upward_g0` (scope, 2026-08-19).**
> **The kill stands and is not reopened.** It is sound on its own terms and on its own
> instrument: projected net billed reduction 0.00% on all three nets; certain-on
> neurons exactly zero at layers 1-9, where the matmuls are widest; peak 39 of ~185
> active at layer 28, about 2.3x short of the composition break-even near 85-95;
> certain-dead increment exactly 0, asserted across all 84 layer instances; and the
> reduction stays 0.00% under illegally lax thresholds (alpha > 5 / 4 / 3, max
> on-count 83). One modeling defect was caught by the cell's own cross-check and fixed
> before any verdict, with an independently written bill and the DP agreeing to exact
> integer equality. Nothing in this annotation touches the verdict.
>
> **What narrows is one clause, not the result.** The cell's closing sentence --
> "Kerdock v3 is evidence-optimal within known mechanism space" -- is narrowed to the
> mechanism space m184 actually measured: **sparsity and composition** (the
> dead/on/kink trichotomy, exact on-run linear composition, firing-sorted sparsity,
> and the DP over composition schedules). It carries no claim over compiler-schedule
> mechanisms, which m184 did not enumerate and did not price.
>
> **The correction, with its own arithmetic, so the narrowing is not read as
> optimism.** The compiler-schedule route m184 did not consider is measured at idx 268
> (`clone_l2fringe_flop_recompute_v2`, PASS_SCREEN, n_nets 24): `flop_only_ratio`
> 0.914492 and `effective_compute_ratio` 0.949914, i.e. a billed reduction of
> **8.55%** and **5.01%** respectively. Both sit **below m184's own kill bar of 15%**,
> and far below its promote bar of 20%. Applied to this route, m184's stated gate
> would have returned a kill. The scope fix is therefore **bookkeeping hygiene**: it
> stops a future generation from citing m184 as having closed a mechanism family it
> never enumerated, and it does so without implying that the unenumerated family would
> have passed. It would not have.
>
> **Cross-reference.** The static leaf-shape applicability check (idx 268's 49-leaf
> m/4, k/4, n/4 route against v3.1's 28 deep hooks and its separate WHT first-product
> path) runs once inside W1.1 and serves both this annotation and the G-A gate; it is
> not duplicated here.
>
> Evidence: idx 194 result text and kill_condition, verbatim [O]; idx 268 metrics
> block, verbatim, with the two reductions computed as 1 - ratio [O/D].

**One correction to the wave-0 text this annotation supersedes.** The plan's W0.10
entry says the compiler_schedule route "delivers 5-8.6% billed reduction". The exact
figures from idx 268's own metrics are 5.0086% (effective compute) and 8.5508%
(flop-only); the range is right and the endpoints are now exact [O].

---

### Verification for this addendum

Two independent signals per load-bearing claim, both collected after the last edit to
this text:

- **W0.6 count and door-blindness** -- signal 1: `mi_graph.json` node scan reproducing
  the screen (79 / 58 / 40-subset result); signal 2: `doors.json` scan over all 1,457
  doors returning an empty intersection with the 19 inconclusive idx. Independent
  files, independent code paths.
- **W0.9 carrier identity** -- signal 1: this pass's exact-integer Walsh test on the
  frozen bit table (127/128 bent, 8,128/8,128 pairwise sums bent) yielding +/-1/16
  cross-frame inner products by derivation; signal 2: idx 228's independently measured
  Gram fingerprint {0, +/-1/16} and arccos(1/16) = 86.42 deg, recorded before this
  pass. A corrupted asset could not fool both; a misread record could not fool the
  first.
- **W0.9 asset identity** -- signal 1: `sha256sum` agreement across four campaign
  copies; signal 2: 81-of-82 agreement across the independently maintained Codex
  clone, with the single divergent copy self-identifying as a drift-test fixture by
  path.
- **W0.10 arithmetic** -- signal 1: idx 268's `metrics` block read from the graph;
  signal 2: 1 - ratio recomputed here (8.5508% / 5.0086%) and compared against m184's
  kill_condition text read verbatim from idx 194.

Not verified, and named as such: the plan's count of 52 for the INV4 screen ([R], not
reproducible from the committed artifacts); the six doctrine carrier disagreements
(enumerated here but not adjudicated -- that stays W0.3's, under the fragility gate);
the deployed row_blocked direction store (not read -- the row_blocked carrier claim
stands at [R] via the disclosure section, with the settling check named above); and
every MI statistic quoted anywhere in this addendum, which is carried at [R] from the
upstream graph build and was not recomputed here.

Skipped deliberately, and named as skipped: no cell was predeclared, no ledger field
was edited, idx 194 was not touched, and the W0.10 text above is a draft for the
harness owner to attach, not an applied annotation.
