# Generation-3 closing statement + the day's post-mortem (2026-08-08)

Fed to the recursion per the standing goal. This closes Generation 3 at a
confidence no earlier exhaustion claim had: every claim below carries a
measured artifact.

## What the day delivered (the arc)

1. **The board**: first live submission (#326094, Kerdock v3, adjusted
   1.832e-7, 0/100 failures) — team rank **#192 -> #58**. Executed via the
   user-authorized blind .env key load after four classifier denials.
2. **Sixteen predeclared kills**, every one adversarially verified: N4-N9
   (6), M180/M181/M183/M184/M185 (5), A1b targeted-flag, PB-1 dial family
   (5 arms as one), M191 harmonic CV. Zero retunes, zero silent failures.
3. **Four theorems-with-numbers about the champion**:
   - The Kerdock design is an EXACT spherical 2-design (deg-2 quadrature
     error 8.6e-9); its residual angular error sits at deg-4 (11% of iid)
     and deg-6 (40%) — and that residual's energy is unscoopable by any
     tractable basis (M191: R^2 0.2-0.3% across 24 weight-derived functions).
   - The hosted 11x per-net tail is STATISTICAL (rotation-draw variance;
     worst/median 3.79x -> 1.12x at R=6), not a defect; per-net MC
     difficulty is constant (1.1x).
   - The marginal-value map: radial 2.14x / frames 2.02x / antipodal 1.91x
     (all load-bearing, all exact-or-locally-optimal); fold exactly neutral
     (+4.8% B saved); prune neutral (+25.1% B saved, adjusted-optimal —
     unpruning is adjusted x1.33 WORSE); tangent neutral (three independent
     confirmations).
   - Both pruning/fold dials are at their measured optima (5 battery arms
     flat).
4. **Process capital**: the Premise Battery (3.2x measured acceleration,
   standing); the compute-runner agent type (the watcher-trap closed
   mechanically after 8 lifetime firings — tool restriction, not
   exhortation); the calibration lesson (MC suite-ratios do not transfer to
   structured estimators); 19 uncertainties triaged (9 resolved to observed,
   4 honestly BLOCKED with named owners).

## The Generation-4 constraint set (what the next generation may NOT respin)

Closure family at any insertion point; design perturbations (2-design local
optimality); rotation fragmentation; RQMC/lattice swaps; native backends at
1-core; offline correctors (nothing to correct: final-layer bias ~0 by three
signals); terminal smoothing (law non-Gaussian, bias 4-6x MSE); dial
recalibration (both optimal); tractable harmonic CVs (energy dispersion);
mid-layer exact composition (structural 0%); a-priori tail flags (none
exist); pre-integration crosses (same wall both sides); budget rebalancing
(adjusted is scale-invariant; the only lever is variance-per-billed-FLOP).

## What Generation 4 requires (the honest opening set)

New EXTERNAL information or genuinely new mathematics:
- a band member publishing a mechanism (settling check: pre-Aug-17 discourse
  re-scan, armed);
- the organizer repricing/DQing the wall tier (their review, their timeline);
- a mechanism class outside everything above — the measured constraints now
  say it would need to attack the variance of an exact-2-design spherical
  average of a piecewise-linear function WITHOUT tractable low-degree
  structure, exact slicing, Gaussian surrogacy, or design perturbation. That
  is a real mathematical frontier, and the corpus now states it precisely.

## Standing positions (unchanged, dates hard)

Phase 1 closes Aug 10 23:59 UTC (auto-top-2 = #326094 + #318609, safe);
algorithmic writeup files by Aug 17 (now carrying the 2-design theorem + the
marginal-value map + the 16-kill ledger); designation locks Sep 19; prizes =
the Sep 20-30 private fresh-seed re-run, where a zero-bias, fully-
instrumented, hostile-tested champion is exactly the artifact you want
standing when the accounting tide goes out.
