# Gen-7 adversarial closure — the floor, earned (2026-08-10)

Owner mandate: "actually try, dammit" — attackers Opus 5, judge Fable 5,
everything seeded. Twenty adversarial agents across three fleets spent the
day trying to break the campaign from every side. Result: the champion's
optimality survived total war; the two places blood was drawn were our
PAPERWORK and our own OVER-CLAIMS, both repaired same-day.

## Fleet 1 — break the champion (6 attack lenses + 1 measured candidate)

Verdict: **FLOOR_HELD_EARNED**. Five lenses returned no candidate, each
with a named obstruction now on record (exact-identity: all four
closed-form conditional expectations already consumed; control-variate:
the 2-design absorbs every degree-<=2 statistic exactly and degree->=4
content is ~1e-5 R^2; biased-hybrid: baseline measured-unbiased so
MSE-optimal shrinkage weight ~0, every realizable form worse (-5.7% to
-38%); cost-remap: bill ~99% irreducible float32 matmul at floor rate,
f64 share 0.033% max; design-alt: DGS bound needs N>=33,152 for a
4-design and ~44x rows for degree-6 nulling, Var*C invariant on the flat
speckle).

The sixth lens FOUND the one candidate the certificate left open:
**seed-side rotation construction** (rotation = V from W0's SVD instead of
the grader-seed Haar draw). Measured under the full regime — seed 0,
common random numbers, twice-run BIT-IDENTICAL determinism, noise floor
exactly 0.0 — it is a clean null: paired t +0.19, better on exactly
50/100 nets, bootstrap CI symmetric about zero, +3.6e8 billed FLOPs for
nothing. V is marginally Haar; coupling to the weights' singular basis
buys no alignment with the fixed cubature frame. Ledger 242
(gen7_svdv_rotation_construction, killed).

## Fleet 2 — break the kills (7 families re-litigated under changed premises)

Verdict: **7/7 boundaries HELD**, several strengthened: the fidelity hold
formally retired the dtype-repricing flag (M183 measured the f64 SHARE,
0.00%, invariant to repricing); the closure hold showed all four insertion
points were killed at maximally favorable cost assumptions; the dispersion
hold named the record's ONE un-probed crack — non-smooth cell-membership
covariates — and S18 then killed it same-day (best gated OOS incremental
R^2 2.37e-5 < the 2.63e-5 bar; structural mechanism: all 64,512 directions
are singleton first-layer cells, per-point unique labels cannot
generalize; ledger 241).

## Fleet 3 — break our certainties (6 load-bearing claims)

Verdict: 1 held, 1 watch, **4 REAL_ACT_NOW — all executed same-day**:
1. Selection default-safety: my claim was level inflation (scores are not
   the default mechanism; one-vs-two slot conflict unresolved). Corrected
   on the record; explicit selection restored as REQUIRED; Jonah's walk
   protocol pushed (deadline 11 Aug 23:59 UTC).
2. Writeup floor language: S17 self-labels a lower-bound ATTEMPT — v8
   de-escalates everywhere, deletes ALL competitor-facing adjudication.
3. Dispersion model: DIFF_RATIO 1.1x refuted by our own committed data;
   S1b re-measured vD 0.08-0.12, split 17-23%/77-83%, bracket-validated
   against the hosted 15.53x spread (old model P=0); fresh-seed bands
   [1.54e-7, 2.16e-7] (50-net) / [1.62e-7, 2.06e-7] (100-net); v8 carries
   the corrected numbers.
4. Governance: the submission-supersession chain recorded; Jonah owes a
   one-sentence standing definition of M245 key-2.

## The closing statement (writeup-grade)

The champion's position is no longer an assumption, a certificate, or a
self-assessment: twenty adversarial agents with the explicit mandate to
break it — including the one seed-side construction our own theory could
not exclude — failed against measured, seeded, deterministic gates, while
the same process found and fixed every over-claim in our own record. The
floor is earned. The remaining deep question (whether NON-rotation
seed-side structure is extractable) is exactly M245's held experiment and
ednacob's U18 anomaly — one question, two probes, unresolved by design
until the two-key gate opens.
