# Overnight goal — the most honest winning push (2026-08-10, owner asleep)

Owner directive: "the most honest winning answer to push while I sleep, using
swarms to mutate the math up headroom recursion. I want the solve."

## The honest frame (stated once, governs everything below)

The solve EXISTS and is measured: the champion sits at the point-evaluation
floor (S17: 0.90x distinct-direction bound), and the convergence certificate +
241 ledger kills close every tested output-side mutation class. No overnight
swarm can mutate the math past the floor, and none will pretend to. The
recursion continues where it is still live: the DECISION LAYER. Tonight that
layer holds real, measured upside — Door A's resurrection is worth ~15x on
P(win) at the near-rival cutoff (U9: 0.877/0.940 vs the twin's 0.057/0.124),
legally, because explicit nomination makes extra Phase-1 entries free options
and the U2 fix (commit 931285d) removed fold3cap's known metering defect.

## Overnight lanes (all running or done as of 09:0x UTC)

1. DOOR A RESURRECTION — DONE 09:5x UTC, ALL GREEN: gates re-run on fixed
   source BIT-IDENTICAL to the frozen 2026-08-08 record (7 differing JSON
   leaves, all wall_s timing; 0 non-wall; _tally deltas exact: sim_cost
   6784399632/6968410237/6491132347, dp_cost 20053248 x3 — U2's
   behavior-preservation prediction CONFIRMED by measurement). New tar
   sha256 A8CAFBEF...9151 (11,776 B), validate-package ok:true; tar diff
   proves only the fix hunk + 2 derived manifest fields differ from sealed
   d3f5aefa (itself re-hashed unchanged). STAGED:
   Desktop\whest-submit\6_fold3cap_FIXED_a8cafbef.tar.gz (hash re-verified
   by orchestrator). Full record: t3_fold3_deterministic_cap/
   FOLD3CAP_REBUILD_20260810.md. Score remains unknown until graded.
2. TWIN VERIFIED (done this hour, fresh signals): sha256 8382E269...EA06
   exact; validate-package ok:true (whestbench 0.14.0, issues: []); 10 tar
   members, list-identical to the graded package's sibling.
3. BRIDGE (monitor armed): M245 static closure proceeds in its own lane;
   on double-PASS the acting /root verifies bindings and PREPARES the
   committed GO; launch still requires Jonah's explicit word (two keys).
4. SENTINEL (wakeup loop armed): each wake - board + discourse + inbox
   (U19 nomination-instructions email), agent/bridge status, re-verify
   nothing material moved. PushNotification on: fold3cap staged or
   gate-failed, bridge PASS or stall, board move affecting nominations,
   and a final pre-close reminder if Jonah has not acted by ~6h to close.

## The morning decision (Jonah's, before 23:59 UTC tonight)

Pool-freezing facts: nominations are Phase-1-only; explicit nomination
beats auto-top-2 (pre-patch #318609 hazard). The decision tree:
- Submit the TWIN (verified, zero-risk clone with crash guards): yes/no.
- Submit FIXED FOLD3CAP (if staged green overnight): yes/no. A bad grade
  costs nothing (we simply do not nominate it); a good grade unlocks
  Door A (P(win) 0.877/0.940 at the 1.55/1.6e-7 cutoffs).
- Nominations themselves wait for the organizer instructions email (U19);
  recommendation on current evidence: #326094 + best-graded of
  {fold3cap, twin}, explicitly, never the default.

## Constraints (unchanged, absolute)

No agent submits anything; whest login and every upload are Jonah's alone
(blind .env pattern, key never read/displayed). Kills are final; no
estimator-math mutations; M245 two-key gate stands; frozen tests untouched.
