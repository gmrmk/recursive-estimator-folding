# ULTRAMATH SLATE — five-lane deliberation, synthesized

Stamped: 2026-08-19 08:18:59 UTC (`date -u`: Wed, Aug 19, 2026 8:18:59 AM).
Synthesizer: Fable. Inputs: five lanes (spectral, frame/design, compute, transport,
selection) x two rounds (independent derivation, then cross-examination). Every input
was exact-rational or deterministic quadrature in the session scratchpad — 0 billed
FLOPs against B = 2.72e11, zero estimator wall seconds, no Monte Carlo, no production
measurement. `fold_floor_splice` and `frame_completion_129` are UNTOUCHED by every
entry below; the sealed 129 cell is referenced only as a fixed queue item whose
READING two zero-cost reads sharpen.

Channel context absorbed (AGENT_CHANNEL, RANK-1 merge finding): the fold's measured
paired effective-C ratio is 0.8388/0.8447, so folded row_blocked projects to 1.780e-7
— 9.9% WORSE than the unfolded kerdock_v3 already held (1.6190838e-7). The host fork
is the whole decision. This slate's algebra lands on the same side from pure theory:
the structured carrier is ~98% of the degree-four family's score value, and the
carrier + fold stack is the only path that beats both held lineages.

---

## 0. Settled arithmetic (the backbone — treat as closed in every future round)

- **The A_l ladder, five independent code paths** (L1 monomial, L2 sympy, L4 Moebius,
  L3 recurrence, L5 recurrence): X_4/S_4 = -1/128 and X_6/S_6 = +1/4096 exactly;
  A4_haar/A4_mub(126) = 128/3 exactly; completion indices k*(4) = 129, k*(6) = -4095;
  A4_mub(129) = 0 identically; degree-2 identically zero on both sides (X_2 = S_2 = 0);
  sixteen-digit float-anchor reproduction. New fourth route to the 129 constant:
  positivity of the degree-four defect forces k <= 129 = d/2 + 1, the real-MUB maximum
  — no beyond-completion structured family exists, so carrier enlargement is
  Haar-or-hybrid only.
- **Unconditional carrier optimality** (L2 K3/K4 + L1 ATK1 + L4 ATK5): the rectifier
  arc-cosine kernel is absolutely monotone (closed-form nonnegative coefficients,
  three derivations); composition preserves it; Jensen against the Parseval constraint
  makes the flat cross-Gram the exact global variance minimizer over frame unions at
  every depth, with no energy-ratio hypothesis. Lane 1's w* = 178192/9039 threshold is
  vacuous (the two-degree truncation is never a realizable kernel; realizable ratios
  up to ~2261 still have the flat optimum) and is retired.
- **Degree-four exclusivity** (L4 ATK8 self-refutation + L2 XS3 + L1 CS2): A_4 is a
  squared norm; zero defect means zero degree-four error for every function and every
  rotation, hence zero fluctuation, hence zero selection signal in that channel.
- **Flatness is dead** (L3 + L1 + L4; L5 conceded in round 2): score derivative along
  carrier size at unit scale = beta - phi exactly; neither share is zero and neither
  is measured; the tile census matched an older per-tile-additive object (exact
  reconciliation: 15.75 x per-call floor - 14.75 W-stacks = crowned generic layer).
- **The 0.3911 design-share conversion is dead** (withdrawn by L1 itself; refuted by
  L5's panel bound on all three nets, by the measured host fork, by L2's three-route
  17.8% agreement); it is UNIDENTIFIED (three readings 0.391/0.906/1.000) and one
  receipts read settles it.
- **Degree-six-and-above design closure**: exact-carrier route needs 2,861,952 points
  (~44x the deployed count — points-vs-lines corrected from the ~88x on file); the
  carrier lever above degree twelve is below 1e-8 (two exact routes; the symbolic-zero
  route was an artifact). Owner's seeds closed: "factorializing" = the double-factorial
  closed form for Q_l(0) plus the Kronecker closure (MUB x MUB = MUB, non-flat factors
  strictly dominated); the high-degree ladder has null carrier lever and its energy
  side was already killed at idx 274/275.

---

## 1. The slate — ranked by expected value per cost against Score = MSE x max(0.1, C/B)

### 1. deg4_exclusivity_rule — edit
- **hypothesis**: The carrier swap, the frame completion, and degree-four proxy
  selection are three ways of spending one degree-four error channel (~16% of
  estimator error on the Haar host) and must be ranked, not summed — composed
  selection gain scales with the residual degree-four variance share: 1 deployed,
  ~1/36 after the swap, exactly 0 after completion.
- **causal_mechanism**: A_4 is a squared norm of the carrier's degree-four moment
  tensor; a zero defect zeroes the degree-four error for every network and every
  rotation, hence zeroes its fluctuation, hence zeroes any degree-four proxy's
  correlation — selection harvests exactly the fluctuation the other two moves delete.
- **cheapest_falsifier**: One sample correlation on the committed 3x16 rotation panel
  between the degree-four design-defect functional and the remaining MSE (offline,
  stored Grams, no forwards). Near zero confirms exclusivity; strongly positive voids
  it but then refutes Lane 5's K15 reopening argument instead — no reading leaves both
  alive.
- **cost_vs_B**: 0 (theorem + one offline panel statistic).
- **host**: row_blocked primary; the rule applies to both.
- **machinery**: pure theorem — no run needed (falsifier is an offline panel statistic).
- **collision_check**: Adjacent kills idx 204/245 (proxies, Kerdock context) and the
  IDX 80 completion family — this entry is a scoring rule, not a cell; it prevents
  double-booking one channel across those lineages. No axis is re-run.
- **evidence_now**: Lane 4 ATK8 (withdrew its own round-1 zero-interaction splice),
  Lane 2 XS3/P5, Lane 1 CS2 — three independent derivations of the same annihilation
  identity; predeclared score table: swap 0.797, completion 0.842, selection 0.89-0.93.

### 2. share_and_size_identification_reads — edit
- **hypothesis**: Four zero-cost artifact reads identify the two scalars the whole
  economics turns on — the size-independent MSE share (beta) and the design-share
  conversion — before any further seed is consumed: the MSE (not the defect) at the
  recorded one-frame and four-frame bases, the denominator s11 actually divided by,
  the radial-quadrature error budget, and r0's per-degree table.
- **causal_mechanism**: On independent frames MSE(k) = b^2 + D/k exactly, so two
  recorded base sizes over-determine the floor b^2; the s11 read separates the three
  candidate conversion readings (0.391/0.906/1.000) that currently price the sealed
  completion cell at 7%, 16%, or 18% of MSE; the radial budget bounds the
  rotation-independent floor from the physics side.
- **cheapest_falsifier**: The reads ARE the falsifiers: a base-MSE ratio of one
  quarter fixes beta ~ 0 and conversion ~ 1 (Lane 1's dilution dead, MSE-side flatness
  near-exact); a ratio well above one quarter revives the shrink direction and
  reprices every enlargement item. Ledger idx 269-271 already carry the bases.
- **cost_vs_B**: 0.
- **host**: both.
- **machinery**: recurse CLI derivation (artifact reads + a two-point identity).
- **collision_check**: Resolves (does not collide with) the r0-vs-S17 object-mismatch
  tension (2.1-3.7x); IDX 80's 2.3256% bar is the yardstick. The sealed 129 cell is
  untouched — these reads sharpen only how its result will be read (its band
  discrepancy is already an owner decision in the channel).
- **evidence_now**: Lane 3 three-readings decomposition; Lane 4's band argument (the
  pre-registered band already excludes the 0.391 reading); Lane 5 K16 exact panel
  bounds (min/mean 0.258/0.385/0.499) refuting 0.3911 on all three nets; Lane 1's own
  round-2 withdrawal.

### 3. carrier_fold_stack — splice
- **hypothesis**: Porting the structured phased-Hadamard carrier into the deployed
  fold pipeline at identical line count is the largest bankable move: predicted
  raw-MSE ratio 0.845, compute ratio 0.942, score 0.796 (+20.4%), at zero point-count
  cost and zero memory increment — and it is the only construction that beats both
  held lineages at once.
- **causal_mechanism**: The two carriers differ only at degree four (exact factor
  3/128) and degree six (exact tax 4221/4096), and the structured host's first two
  layers meter cheaper (a butterfly replaces the half-height design product; the
  antipodal odd channel rides the same butterfly) while the fold's wins sit in the
  terminal layers — disjoint lanes, exact composition.
- **cheapest_falsifier**: Three gates, none running the estimator: (a) Gram/rank
  assertion on one block of the deployed carrier's stored directions — if they are not
  orthonormal frames, degree two leaks and 0.845 becomes a floor, redone with an A_2
  column; (b) one import + one composed terminal product, which today raises TypeError
  from the keyword-only vs positional sample-hook mismatch (two-line fix); (c) a
  sealed variance-only arm on existing rotation seeds, predeclared band [0.76, 0.85],
  refuted above 0.90.
- **cost_vs_B**: Gates 0; arm ~0.11% of B per call; deployment NEGATIVE ~ -4.7% of B
  on the compute leg (209.575e9 vs 222.405e9); memory increment exactly zero.
- **host**: row_blocked (the port target); realizes win-path (b) of the kill index.
- **machinery**: mutation engine (carrier port) + fold_search cell (variance-only
  attribution arm).
- **collision_check**: IDX 68 is the geometry premise this asserts (checked, not
  assumed); k32 death law satisfied (the carrier is theorem-fixed, nothing fitted);
  idx 204/245 proxy kills were Kerdock-context — the swap moves the host TO that
  context, which is what kills post-swap selection; frame_completion_129 untouched —
  this arm supplies the carrier/completion attribution split by difference (98.3/1.7
  in score terms, predeclared) without touching the sealed spec.
- **evidence_now**: Variance leg from three independent routes agreeing to 0.16pp
  (exact per-degree x r0 shares; parameter-free depth-32 kernel quadrature;
  exchangeable-correlation law with the kernel floor rbar = -1.2398e-3); compute leg
  from the committed host bills; the measured host fork (0.763 adjusted) and the
  channel's RANK-1 merge finding (folded row_blocked 1.780e-7 vs unfolded kerdock
  1.619e-7) both say the predictions are conservative and the fork is the decision.

### 4. carrier_size_two_parameter_law — edit
- **hypothesis**: Lane 5's flatness theorem is replaced by an exact two-parameter law:
  the score derivative along carrier size at unit scale is exactly beta minus phi,
  with phi (size-independent compute share) bracketed [4.11e-4, ~0.165] and beta
  (size-independent MSE share) bounded above at 0.258 by the panel and unbounded below
  — the axis's sign is the largest undetermined economic quantity in the campaign.
- **causal_mechanism**: Every crowned lane is linear in the row count except the
  per-layer weight-side operand stack (exact, tiny) and the residual wall (route entry
  charged once, dispatch growth logarithmic), while the design error scales exactly
  inversely with frame count and any radial/bias floor does not scale at all; the
  product of an affine-plus-log cost with a floor-plus-inverse error is flat only at
  the coincidence beta = phi.
- **cheapest_falsifier**: Entry 2's base-MSE read identifies beta; one census read of
  the deployed route's size-independent terms plus one residual reading at a second
  carrier size (the gate-verifier collects residuals anyway) identifies phi; flatness
  survives only if the two land equal.
- **cost_vs_B**: 0 to establish.
- **host**: both.
- **machinery**: pure theorem — no run needed.
- **collision_check**: Reprices (does not kill) every trim/prune lineage — up to ~12%
  of score per halving over-credited under the wide reading. L5's withdrawn "+25.4%
  headroom" stays withdrawn (its mechanism was wrong even though the axis is live).
  Honest boundary named: the fitted residual law has never seen a size variation and
  its constant piece is negative.
- **evidence_now**: Four lanes independently (L3 exact census reconciliation, L1
  ATK7, L4 ATK6, L5's own round-2 concession); L5's "+1.21% at most" rider refuted —
  the ceiling is phi (6.29% kappa-only; +3.15% at a single doubling).

### 5. design_axis_closure_unconditional — splice
- **hypothesis**: The design axis at fixed point count is closed by an unconditional
  theorem — the structured carrier is the exact global variance minimizer over frame
  unions for every absolutely monotone kernel at every depth — and the degree-four
  wall is settled: the completion sits 128 lines above a rank-bound floor that is
  itself unattainable, so the half-cost door is closed and the rigidity lemma
  forecloses every keep-four-fix-six proposal.
- **causal_mechanism**: The rectifier arc-cosine kernel has closed-form nonnegative
  coefficients; absolute monotonicity survives composition, making the depth-composed
  even kernel convex in the squared inner product, and Jensen against the Parseval
  constraint puts the flat cross-Gram at the exact minimum; at that corner the
  constant cross level determines every higher moment, so the degree-six defect is
  uniquely forced; the even embedding's rank bound counts lines and is
  weight-insensitive, and its equality case is a maximal equiangular frame killed by
  integrality at this dimension.
- **cheapest_falsifier**: One counterexample of pure arithmetic kills each leg: an
  orthogonal cross-Gram whose summed kernel value beats the flat value; a symmetric
  law at the Jensen floor with sixth moment below the forced cube; or a
  degree-four-exact antipodal configuration below 33,024 lines. Operationally, entry
  7's block-Gram arm is the standing hostile input.
- **cost_vs_B**: 0.
- **host**: both.
- **machinery**: pure theorem — no run needed.
- **collision_check**: Supersedes Lane 1's w* threshold (vacuous) and ADJUDICATES Lane
  4's degree-four factor-two reopening as CLOSED — the even-embedding rank bound binds
  in lines at 1 + N(d,2) = d(d+1)/2, which the halved point bound missed; Lane 4's
  degree-SIX points-vs-lines correction STANDS (~44x short, not ~88x). The optional
  Delsarte LP shrinks to a 128-line question worth <= 0.39% of carrier cost —
  demoted to negative space.
- **evidence_now**: L2 K3/K4 + L1 ATK1 (independent series derivation) + L4 ATK5
  (second-derivative route) + depth-32 numeric convexity (minimum second difference
  +7.5e-10); the rank/LP wall identity proven symbolically for all d; Neumann
  integrality at sqrt(258); sub-frame completion defect strictly positive until the
  last vector.

### 6. completion_index_ladder — edit
- **hypothesis**: File once, as settled five-signal arithmetic: the Moebius law with
  completion indices k*(4) = 129 and k*(6) = -4095, the dyadic pair -1/128 and
  +1/4096, degree two identically zero on both sides, positivity of the degree-four
  defect reproducing the real-MUB maximum d/2 + 1 (no beyond-completion structured
  family exists), the factorial closed form answering the owner's factorializing seed,
  and a null carrier lever beyond degree twelve answering the owner's high-degree
  ladder.
- **causal_mechanism**: Both defect numerators are affine in the frame count, so each
  degree's entire carrier dependence is one rational number whose sign decides
  help-or-hurt and whose root decides reachability; the within-frame excess saturates
  to one above degree four while the cross term collapses ~30x per two degrees, so the
  whole design game is degrees four and six and nothing else.
- **cheapest_falsifier**: Any exact-Fraction inequality at any degree kills it; five
  independent code paths already agree to all printed digits including the
  sixteen-digit float anchors.
- **cost_vs_B**: 0.
- **host**: both.
- **machinery**: pure theorem — no run needed.
- **collision_check**: Errata folded in: the frame-count-only tail scaling holds from
  degree ten (relative gap 2.3e-5 at eight); the symbolic route's zeros at degrees
  64/96/128 were a simplification artifact (the value recurrence is the authority).
  Owner seeds closed on the carrier side; the energy side was already killed at idx
  274/275 (Kerdock context, materiality killtype — entry 8 carries the
  carrier-indexed correction).
- **evidence_now**: Lanes 1/2/4 round 1; Lanes 3/5 round 2 re-derivations; Kronecker
  closure (MUB x MUB = MUB; non-flat factors strictly dominated).

### 7. block_gram_spectrometer — mutation
- **hypothesis**: One sealed variance-only arm with a block-Hadamard cross-Gram is
  simultaneously the only available hostile input to the closure theorem and a meter
  for the disputed degree-four share of MSE, at negative marginal transform cost on
  the structured host.
- **causal_mechanism**: A block cross-Gram raises the fourth cross moment, which
  degree six rewards (its fourth-moment coefficient is negative) and degree four
  punishes ~150x harder at the eight-way split — so the measured ratio isolates the
  degree-four share while every degree above six and all non-design error cancel; the
  construction is a truncated butterfly, strictly cheaper than the transform already
  deployed.
- **cheapest_falsifier**: Predeclared three ways before any seed: the defect table
  (degree-four x22.08 / x64.24 / x148.55 / x317.18 for 2/4/8/16-way splits; degree-six
  below the iid floor only at 4- and 8-way; degree-eight within 0.2% of one as the
  structural check); total-ratio KILL bar at the mildest setting — above 1.05 confirms
  closure, at or below 1.00 falsifies it and reopens the frame-geometry axis; at the
  eight-way split the two conversion readings separate by 32% (1.6535 vs 1.2492),
  which no seed spread blurs.
- **cost_vs_B**: Marginal metered FLOPs NEGATIVE (the truncated butterfly saves
  8.3e6-33.0e6 per net vs the deployed transform); one variance-only arm ~0.11% of B
  per call.
- **host**: kerdock_v3 (the parity clause makes it unlawful on row_blocked).
- **machinery**: fold_search cell (variance-only arm, sealed predictions).
- **collision_check**: No adjacent kill: idx 274/275 killed own-axis CAPTURE cells
  (materiality killtype); this is a carrier-defect arm — different object, different
  killtype (predicted-kill control). Lawfulness: a phase-pattern edit on the deployed
  butterfly, a reassociation on kerdock_v3, not a class-B sample substitution.
- **evidence_now**: Predictions reproduced from two independent representations (L2
  per-degree channels; L1 two-moment law), reconciled exactly by L4; family shape
  corrected (the sixteen-way split turns adverse at degree six, so eight-way is the
  true minimum); L5 re-derived the round-1 prediction to 0.08% from a third route.

### 8. deg4_rung_dual_carrier — edit
- **hypothesis**: Add the never-measured degree-four rung to the existing own-axis
  ladder and run it on both carriers: the completed carrier must return exactly zero
  (a falsifiable exact null validating the completion theorem), the deployed carrier
  about 3.35x its degree-six rung, and the corrected ceilings (2.28x short of the bar
  on the structured carrier but only 1.13x short on Haar) force the recorded ladder
  kill to be re-filed as carrier-indexed.
- **causal_mechanism**: The entry-layer kink tail is degree-four dominated (59.4% of
  all even energy at four and above) yet the shipped ladder started at six, so every
  recorded capture figure measured the tail's minority; the rung reaches the readout
  through the host's degree-four defect — suppressed ~43x on the structured carrier,
  unsuppressed on Haar — while a completed carrier's degree-four moment tensor is
  identically zero, making the rung an exact null there.
- **cheapest_falsifier**: Zero-cost first: a receipts read on whether the deployed
  control variate already spans degree four (the named counter-hypothesis —
  redundancy, not smallness). Then one rung on the already-budgeted diagnostic cell
  (strictly cheaper than the rungs already consumed); nonzero capture on a completed
  carrier falsifies the squared-norm identity and the completion case with it; zero on
  the deployed carrier falsifies the kink-tail coefficients.
- **cost_vs_B**: ~0 (diagnostic, synthetic seeded nets, charged to H).
- **host**: both (diagnostic; the carrier is the moving axis).
- **machinery**: fold_search cell (diagnostic ladder rung).
- **collision_check**: Adjacent kills idx 274/275 (KILL on materiality, Kerdock
  carrier): this changes TWO axes — the rung (four, never measured) and the carrier
  (Haar arm) — a premise change under the context-indexed doctrine, not a re-run; the
  structured-carrier kill stands at its corrected 2.28x margin.
- **evidence_now**: lambda_4 = -5.0744e-3 exact, cross-checked by an independent
  quadrature to 2e-5; the measured v2 profile matches lambda_l^2 at six rungs; Lane
  4's anchor arithmetic reproduces the committed v2 capture (0.003816); Lane 2 XS5
  supplies the exact-null arm.

### 9. selection_adjudication_and_cap — edit
- **hypothesis**: Before any F7 proxy cell is funded: the additive cap bounds a
  degree-four-driven proxy's realizable reduction at the channel's mean share (~16% on
  Haar, so the 23.4% optimum claim exceeds its own mechanism 1.45x), a
  sign-consistency read decides whether the recorded frame-dispersion proxy revives
  negated (~+10% at audited cost), and the panel's skewness decides whether ~8
  effective modes replace the fitted lognormal (making every selection figure a floor)
  — and post-swap the whole family is predicted dead.
- **causal_mechanism**: If the proxy is a function of the degree-four channel alone
  and channels are uncorrelated across rotations, selection cannot touch the
  complement's conditional mean, capping the gain at the channel's mean share
  regardless of correlation strength; a Gaussian copula lets the same correlation act
  on the whole distribution, which is exactly the disagreement; difference-built
  statistics cancel the frame-common error component identically, which is the
  mechanism behind every recorded proxy failure and the reason a consistently negative
  sign is information rather than noise.
- **cheapest_falsifier**: Three offline statistics on committed artifacts: the
  degree-four-vs-remainder correlation across the rotation panel (each branch kills a
  different lane's claim); the per-network sign record of the dispersion proxy; the
  panel's log-MSE skewness (predicted ~ -0.37 at ~8 modes). All zero estimator
  compute.
- **cost_vs_B**: 0 (reads/statistics).
- **host**: row_blocked (selection stays dead on kerdock_v3 for two independent
  reasons).
- **machinery**: recurse CLI derivation (offline statistics).
- **collision_check**: idx 245's step-0 invariance kill is carrier-INDEPENDENT and
  stays dead (the diagonal-Gaussian class is untouched); idx 204's dispersion-proxy
  kill was Kerdock-context and control-variate framing — carrier axis and killtype
  both change; the k32 death law requires any negation to be network-independent,
  which is exactly what the sign-consistency read tests.
- **evidence_now**: L4 ATK7 (the cap and the 1.45x excess), L5 cs4 (blindness
  dichotomy; independent share >= 0.131 recovered from the recorded correlation), L5
  cs5 (chi-square identification; the 0.031 left-tail shortfall already observed), L1
  ATK8 (order-statistic machinery confirmed to all digits); entry 1 supplies the
  post-swap annihilation.

### 10. pool_pin_left_hoist_dial — mutation
- **hypothesis**: The fold operator's route-entry residual (5.15% of B, the largest
  single compute lever) is pooled-scratch re-carve churn removable at zero FLOPs, the
  left operand tree hoist is worth exactly three A-lanes (0.17% of B at the deployed
  depth), and both must be booked jointly with the carrier-size axis because pin and
  enlargement amortise the same seconds (sub-additive by an exact shortfall).
- **causal_mechanism**: The carve path's shrink test sits on a knife edge between wide
  layer products and half-width terminal products, firing a grow-shrink-grow cycle
  that also clears the plan cache; the terminal fold reads only three distinct left
  arrays across six products, and the operand-lane licence prices one tree per
  matrix-side-depth; whatever the pin removes from the bill, enlargement can no longer
  earn on.
- **cheapest_falsifier**: Two integer counters riding the NEXT scheduled fold predict
  (no extra run): a carve-reallocation counter (count <= 1 kills the pin before any
  timing) and a left-tree signature counter (three distinct builds means the hoist is
  already banked). Discriminating joint prediction: after a successful pin, a
  subsequent carrier doubling's value must fall from ~7.5% to ~4.6%.
- **cost_vs_B**: 0 FLOPs; predicted -5.15% of B (pin) and -0.17% (hoist); memory risk
  named — pair with the workspace dial (exact rate ~0.044e9 of C per MiB of peak;
  rungs 0.34-2.07% of B).
- **host**: both (fold operator).
- **machinery**: fold_search cell (instrumented counters on the scheduled predict).
- **collision_check**: Distinct from the untouched fold_floor_splice cell (operator
  mutation, not the sealed splice); trades against the frozen <512 MiB clause (IDX 80
  family) — the dial prices the clause instead of violating it; the operator-tax
  DECOMPOSITION was withdrawn by Lane 3's own round-2 self-attack (underdetermined at
  one shape) — only the two-signal constancy is load-bearing here.
- **evidence_now**: Lane 3 rounds 1-2 (mechanism, exchange rates, sub-additivity law
  verified at two scales); Lane 4 confirms the kappa lever composes multiplicatively
  with whichever degree-four move wins; depth-route-only discrimination already on
  disk rules out fallback-side explanations.

### 11. hybrid_core_shell_ladder — mutation
- **hypothesis**: A structured core of the complete unbiased family unioned with an
  unstructured shell is the only lawful carrier past the structured maximum that keeps
  any degree-four suppression; its per-degree defects have a verified closed form, a
  doubled hybrid beats pure doubling by ~8% of MSE on the design side alone, and past
  ~1.9-2.6x scale the depth argmin migrates to the deeper schedule, closing the
  crowned per-call gap to 1.5%.
- **causal_mechanism**: Cross pairs inside the core sit at the unbiased level while
  every pair touching the shell has zero spherical mean at all positive degrees, so
  the aggregate defect is the core's diluted by total frame count — an exact one-line
  law; the deeper schedule's fixed per-doubling residual penalty is divided by an
  analytical saving that grows with carrier size, so the argmin crosses at a
  computable scale; a structured core above a 0.36%-of-rows fraction also revives the
  antipodal-split flag.
- **cheapest_falsifier**: GATED behind entry 2's sign (dies outright if beta >= phi —
  the ladder then runs backward). Then the construction gate: evaluate the degree-four
  and degree-six defects on the actual stored directions and require equality with the
  closed form — an equality, not a bound, so any departure kills it. Certification is
  class-B and walks the same door as the sealed completion cell.
- **cost_vs_B**: 0 to establish; doubling ~ +1.31% of B (one dispatch doubling) + 63
  MiB storage (buyable back ~1.02% of B via the dial); predicted score at the
  double-size full-core point +15-18% with the route-entry term pinned.
- **host**: row_blocked lineage (it generalizes both endpoints).
- **machinery**: mutation engine (construction) + fold_search cell (certification
  arm), queued behind the reads.
- **collision_check**: The enlargement axis has no recorded kill; class-B substitution
  — same certification clause the 129 cell walks (that cell untouched); honest
  boundary: the residual law's size-axis extrapolation was fitted over depth routes at
  one size with a negative constant piece and cannot be trusted far from the fitted
  point.
- **evidence_now**: Lane 3 (exact Fraction equality of the dilution law; the
  depth-argmin crossing at scale 1.87-2.60 on both harness nets; flag break-even
  0.003586); Lane 5 round-2 enlargement law (monotone at beta = 0; capped +2.83% if a
  budget ceiling binds — rules read named); Lane 4's rotation invariance guarantees
  zero design-side interaction with selection.

### 12. compute_ledger_corrections — edit
- **hypothesis**: Bank the bill corrections that keep the ledger honest: charge the
  transform-domain accumulate (three sums; the fusion door closes on the contiguity
  copy at a 19x smaller margin), state the crowned suite floor as host-specific (the
  unstructured host's floor is 4.38e9 per net higher, entirely in layers one and two),
  reconcile the tile census exactly, and adjudicate the leaf-lane erratum in Lane 3's
  favor.
- **causal_mechanism**: Summing legs in the transform domain before a shared decode
  costs one write per leaf element per extra leg, exactly cancelling the leaf saving
  from splitting — the contraction axis is exactly zero only once the accumulate is
  charged; the census gap decomposes to the unit as operand-tree sharing plus
  design-layer structure; the leaf formula's cited check evaluates its bracket to 448
  inline, which round two's attack misparsed as a dimension — two independent
  re-derivations confirm formula and value, so no fold bill requote is needed.
- **cheapest_falsifier**: Each correction is one line of exact integer arithmetic
  already executed (six fuse/split cases with equality; the census gap reproduced to
  the unit; the leaf value confirmed from first principles by two lanes); the
  ledger-side check is a read confirming no accumulate line exists in the committed
  honest-fold breakdown.
- **cost_vs_B**: 0 (filings; the accumulate correction makes a closed door's bill
  WORSE by +0.52% of B at depth six — filed because the ladder's value is the bill
  being right).
- **host**: both.
- **machinery**: pure theorem — no run needed.
- **collision_check**: No kill adjacency; corrects suite_27's margin without reopening
  the fusion door; the operator-tax second-shape probe stays queued (constancy
  two-signal, decomposition withdrawn); the depth-cap freeze is re-filed as
  conditional on carrier scale (entry 11).
- **evidence_now**: Lane 3 rounds 1-2; Lane 4 ATK4 (first-principles Winograd
  re-derivation, six exact cases); Lane 5 ATK7 (independent arithmetic); Lane 2's
  ATK1 erratum adjudicated as a misparse by the synthesizer with explicit arithmetic.

### 13. f8_antithetic_retirement — edit
- **hypothesis**: File the cross-rotation family as theorem-dead with corrected
  reasons: GLS weights are uniform by exchangeability (zero weight content), the
  scalar cross-carrier mixture always loses to spending the same compute on the better
  carrier (the true reason — the no-flip premise was false, a real ordering flip
  exists at degree six), and the negative-correlation ladder is the completion index
  in different coordinates with an exact floor 21.8x above the number it was costed
  at.
- **causal_mechanism**: Sherman-Morrison forces uniform sum-one weights on any
  exchangeable covariance; a scalar weight cannot express a per-degree reversal, and
  the only spectrally resolved mixture that could needs a degree-six projection whose
  axis family scales as the cube of the dimension over six — closed on cost; convexity
  of the composed kernel bounds any pair correlation below by the unbiased-level
  value, which the deployed structured carrier already saturates at every degree
  simultaneously.
- **cheapest_falsifier**: Exhibit any frame pair with realized error correlation below
  -1.2398e-3 (falsifies the convexity floor), or any exchangeable ensemble with
  non-uniform optimal weights (falsifies the rank-one identity); the measured -0.027
  must be re-filed as a different object than the score-law rbar.
- **cost_vs_B**: 0.
- **host**: both.
- **machinery**: pure theorem — no run needed.
- **collision_check**: Retires a queued cell (F8) and Lane 5's round-1 antithetic
  search; the false no-flip premise is corrected in the record rather than patched
  (Lane 5's own round-2 report); the sole named survivor — a spectrally resolved
  degree-six mixture — is filed closed-on-cost so it is not re-proposed as a scalar
  cell.
- **evidence_now**: Lane 5 K11 + round-2 premise correction; Lane 2 ATK7 (the exact
  floor; the ladder's value at the deployed frame count IS the carrier-swap number to
  five digits); Lane 3's identification of the ladder with enlargement; Lane 1 CS2
  consistency.

### 14. manuscript_errata_bundle — edit
- **hypothesis**: Small correctness edits with data already in hand: split the
  design-boundary lemma into its true compute half and false spectral half, retire the
  monomial-basis spectrum figures for the Gegenbauer-projected ones (the 1.81x
  calibration gap was a basis artifact, and the finite-width correction has the wrong
  sign to explain it), fix the tail-scaling erratum to degree ten, state the
  degree-six wall in one convention (~44x short in points), and keep telescoped-CReLU
  filed closed.
- **causal_mechanism**: The antipodal pair identity exists only at layer one (the
  compute half stands) while the rectifier's odd channel is exactly linear with
  degree-flat gain one half at every layer, so the entry profile transports
  shape-intact (the spectral half falls); projecting the composed kernel on monomials
  instead of the exact zonal basis moves per-degree shares by 1.7x, which was the
  whole of the supposed finite-width gap.
- **cheapest_falsifier**: Already discharged: the measured ladder profile matches the
  squared-kink law at six rungs against a rival law wrong by 49 orders of magnitude;
  the Gegenbauer projection reproduces the measured structured degree-four share to
  13.5%; the finite-d deficit law (l-1)/(2d) verified at seven rungs with the sign
  that refutes the steepening story.
- **cost_vs_B**: 0.
- **host**: both.
- **machinery**: pure theorem — no run needed.
- **collision_check**: None; filing hygiene. Lane 4's own round-1 finite-width
  explanation is withdrawn in the record, not silently replaced.
- **evidence_now**: Lane 4 round 2 (bridge identity, deficit law, withdrawal); Lane 1
  ATK6 (basis-artifact identification closing O1); Lane 2 K11 + Lane 1 ATK9 (tail
  erratum, two routes).

---

## 2. Dropped — refuted in round 2 (with who killed what)

- **L5 flatness theorem (K6) + K17 "headroom worthless except selection" + the
  "+1.21% at most" rider** — refuted by L3/L1/L4 and conceded by L5; replaced by
  entry 4.
- **L1's 0.3911 design-share conversion** and every MSE figure priced with it (6.97%
  completion, 0.135-point tax) — withdrawn by L1; bracket now [8.9%, 17.8%] with the
  share-near-one reading favored; identification read in entry 2.
- **L1's weighted-optimality "iff" at w* = 178192/9039** — arithmetic confirmed,
  theorem vacuous (L5: the two-degree truncation is never absolutely monotone; L2:
  explicit kernels above the threshold still have the flat optimum). Superseded by
  entry 5.
- **L4's round-1 completion x selection splice (+28.6% to +34.3%)** — self-refuted
  (ATK8): the interaction is total in the proxy's channel, not zero. Replaced by
  entry 1.
- **L5's antithetic ladder at rbar = -0.027 (+40.5% at K=16)** — refuted by the exact
  convexity floor (21.8x smaller); the measured -0.027 is a different object. Retired
  into entry 13.
- **L5's 23.4% realized selection figure** — capped at ~16% by its own stated
  mechanism (L4 ATK7); adjudication in entry 9.
- **L1's split-rotation Pareto control** — replaced by the block-Gram control (the
  split-rotation family never beats independent frames anywhere, so it tests nothing).
- **L3's operator-tax decomposition (4mk + 7kn)** — withdrawn by L3's own self-attack
  (underdetermined at the fitted shape); the two-signal constancy survives.
- **L2's leaf-lane erratum against L3 K4** — itself refuted (synthesizer adjudication;
  L4 and L5 confirm formula and value; the attack misparsed an inline-evaluated
  bracket as a dimension).
- **L4's degree-four "factor-two slack" reopening** — adjudicated closed (the
  even-embedding rank bound binds in lines and is weight-insensitive; its equality
  case is integrality-killed). L4's degree-six correction stands.
- **L4's finite-width explanation of the 1.81x spectrum gap** — withdrawn (wrong
  sign); the gap is a monomial-vs-Gegenbauer basis artifact (L1 ATK6).
- **L2's attribution arm as an edit to the sealed 129 spec** — re-scoped into entry
  3's standalone arm (the sealed cell must not be touched; the split is obtained by
  difference).
- **L1's both-negative window economics (5.1x)** — corrected to 6.0x (L2 found a
  63-ppm moment-constraint slip at a seventh-significant-figure signal); still
  economically dead.

## 3. Disagreement ledger — who won on the algebra

- Carrier optimality: **Lane 2 over Lane 1** (unconditional theorem beats the vacuous
  threshold; Lane 1 broke its own "iff" and said so).
- Design-share conversion: **Lanes 2/4/5 over Lane 1** (three independent refutations
  plus Lane 1's withdrawal); residual ambiguity is a receipts read, not physics.
- Carrier-size flatness: **Lanes 3/1/4 over Lane 5** (Lane 5 conceded); the DIRECTION
  is genuinely open — beta vs phi, entry 2.
- Selection magnitude: **Lane 4's cap vs Lane 5's copula — unresolved**, productively:
  one panel correlation settles it, and each branch kills a different lane's claim.
- Degree-four wall scope: **Lane 1 restored at degree four** (synthesizer adjudication
  via the line-counting rank bound), **Lane 4 upheld at degree six** (~44x, not ~88x).
- Leaf-lane bills: **Lane 3 over Lane 2** (misparse; two confirming lanes).

## 4. Open items (each with its settling check)

1. beta vs phi (sign of the carrier-size axis) — entry 2 reads + one residual reading
   at a second size.
2. s11's denominator and whether the completion cost multiplier was pre-applied — one
   receipts read; decides how the sealed 129 result will be read.
3. Proxy channel decomposition (degree-four-driven or not) — one panel correlation;
   adjudicates entry 9 and the post-swap fate of F7.
4. Deployed carrier geometry (orthonormal frames vs independent points) — one Gram
   assertion; gates entry 3's exact figure (floor vs estimate).
5. Conditional unbiasedness of the deployed estimator's frame weights — one source
   read; the premise under both the exchangeability theorem and the variance leg of
   flatness.
6. Whether the score's cost multiplier may exceed one (rules read) — caps entry 11's
   uncapped ladder at +2.8% if a hard ceiling binds.
7. The residual law along the size axis — never fitted there; one residual reading at
   a second carrier size (the gate-verifier collects it anyway).
8. ETF existence at the deployed line count — open in frame theory, decision-irrelevant
   (<= 0.073% weighted ceiling).

## 5. Ladder plan (respecting the CPU hold; round-4 harness run owns the machine)

- **Rung 0 — NOW (zero estimator CPU)**: file entries 1, 4, 5, 6, 12, 13, 14; execute
  entry 2's four reads and entry 9's three statistics; run entry 3's two static gates
  (Gram assertion; the one-import TypeError probe) and fix the two-line signature if
  the owner approves touching the operator source; set up entry 5's optional LP as
  negative space. Nothing here touches the harness.
- **Rung 1 — rides the NEXT fold predict (behind the round-4 measurement)**: entry
  10's two integer counters and the workspace-dial peak probe. Kill-or-crown: carve
  count <= 1 kills the pin before any timing; three distinct left builds banks the
  hoist for free.
- **Rung 2 — cell seals, queued behind the fold measurement**: entry 3's variance-only
  swap arm (sealed with band [0.76, 0.85]); entry 7's block-Gram spectrometer (sealed
  with the three-axis predeclaration); entry 8's degree-four rung (sealed with the
  exact-zero arm). Each is ~0.11% of B per call or less and none reads truth.
- **Rung 3 — behind the sealed 129 cell (untouched)**: read its result against the
  standing band (second adjudication of the conversion, after entry 2's first); form
  the carrier/completion attribution split by difference with entry 3's arm (98.3/1.7
  predeclared in score terms); then and only then the beta/phi-gated forks — entry
  11's hybrid ladder, the conditional depth-cap migration, and any F7 proxy build
  (predicted dead post-swap; its lawful home is pre-swap or never).
- **Falsifier chain**: Gram assertion fails -> entry 3's figure becomes a floor and
  the arm is re-predeclared with an A_2 column before sealing. Base-MSE ratio = 1/4 ->
  beta ~ 0: Lane 1's dilution is dead a second way, entry 11's amortisation leg lives,
  trims stay expensive. Ratio well above 1/4 -> the shrink direction opens and entry
  11 dies. Spectrometer at or below 1.00 -> the closure theorem falls, entries 5/6
  freeze, and the frame-geometry axis reopens campaign-wide. Panel correlation
  strongly positive -> entry 1's exclusivity softens but Lane 5's K15 reopening dies
  with it — either way the dossier stops double-booking the channel.

---
*Every number above is exact-rational or a committed measurement; scripts live in the
session scratchpad (lane1/, lane2/, lane3r2/, lane4r2/, lane5r2/ and round-2 attack
files). The fold ledger remains the source of truth for every committed figure.*

---

## MARKED CORRECTION — orchestrator self-audit of 2026-08-19 (append-only; original text above is unedited)

Authority: `corpus/whestbench/audit_self/CONFABULATION_AUDIT_20260819.md` (families F1,
F2, F3, F8) and `audit_self/self_graph.json`. Stamp: 2026-08-20T01:04:31Z.

1. **"F7" (L304, L561, L593) is not a corpus label.** The rotation-selection lane it
   names is ledger idx 204 (`gen3_p2_rotation_selection`) and idx 245
   (`gm_p2b_proxy`). A pre-existing corpus "F7" denotes a different object (the
   EXACT-CONTROL/ABI failure node, `FAILURE_MODE_GRAPH_20260810.md:27`). Re-bind
   before funding anything named "F7" here.

2. **This slate's lanes were briefed on a contaminated GROUND block** (workflow
   `wf_01a886a8-2ad`, headed "ESTABLISHED CAMPAIGN MATHEMATICS (all verified; cite, do
   not re-litigate)"), which carried three defects the lanes were structurally
   forbidden to falsify:
   - *"oracle-of-8 rotations = 50.3% MSE reduction"* — CONFABULATED. The committed
     value is **61.6%** (`gm_p2b_proxy/results.json p2_oracle_of_8_panel_gain =
     0.6160089092709584`; CI95 [0.4876, 0.6684]).
   - *"corrected proxies … proxy-guided realizes ~25%"* — no committed artifact. The
     committed verdict is the opposite: `pb1_premise_battery/p2b_results.json` —
     *"P2b KILLED: no weights-only proxy reaches |rho|>=0.4; the 61.6% oracle headroom
     is unharvestable with known proxies."*
   - *"COMPUTE FLOORS (certified): … Fold m-band union [1.86, 2.64] s"* — not
     certified: two disagreeing runs (1.86–2.03 vs 2.4055/2.6373) with no value in
     [2.03, 2.41], and `m` is dimensionless (not seconds).
3. **Gate (binding):** any entry above that priced the F7-selection family — entry 9's
   cap arithmetic and every "selection figure" it bounds — is re-derived against
   oracle 61.6% and the p2b unharvestable verdict before it is funded.
