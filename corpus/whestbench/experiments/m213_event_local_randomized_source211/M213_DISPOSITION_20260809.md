# M213 disposition: KILL physical Source211 provider; retain narrow local evidence

Status: `KILLED_FROZEN_IDENTITY_AND_COLLISION_OWNER_GATES`.

M213 changed M149's rejected deterministic outer rule to one sampled outer
normal for a `[2,1,1]` event. The event uses one M178 call for the conditional
ReLU pair, centralizes the pair, and subtracts the deterministic local
`V_ii V_jk + 2 V_ij V_ik` and tree terms. The mathematical tower-property
description remains the intended exact-in-expectation mechanism; frozen
floating PRNG draws are not claimed literally bitwise unbiased.

## What passed narrowly

M178's value and all three derivative enclosure widths propagate through the
local pair reconstruction. The frozen width-2--7 generated census has maximum
M178-local radius ratio `1.1259529437230464e-07`, below the predeclared
`2e-7 * (1 + |midpoint|)` threshold. A distinct event makes exactly one
conditional M178 call; M178's inherited worst inclusive cost is 4,048 charged
FLOPs per call. This is a local numerical primitive result only.

## Frozen falsifications

1. The reference-only 64/96 Gauss--Hermite outer checks disagree by
   `7.10e-7`, `1.19e-6`, `7.25e-7`, `2.85e-6`, and `3.00e-6` at widths 3--7,
   respectively, all above the predeclared `2e-8` identity tolerance. M213
   does not relax the tolerance, increase the rule, or convert it into a
   provider. This is recorded as M149-like deterministic outer sensitivity.
2. The attempted finite Tallis recurrence for `[3,1]` and `[2,2]` was wrong:
   differentiating a first ReLU power creates a truncated boundary-indicator
   moment, not an unconditional marginal moment. The candidate then broke
   swap/permutation covariance (for example, the generated width-5 K22 pair
   `(0,3)` versus `(3,0)` differed by about `0.1304`). M213 therefore returns
   explicit unsupported sentinels for both strata and never emits a numeric
   K31/K22 owner.

Because full physical K4/K31/K22 owners are unavailable, M167/M205's complete
ownership mapping cannot be populated by M213. Their orbit factors are not a
license to fill missing physics with zero. Likewise `[1,1,1,1]` returns an
explicit unsupported sentinel with no coefficient; it is not silently zeroed.

## Stopped work

M213 does not build a full source table, contract through the next affine W,
bind the next pre-ReLU context for M198, claim any inclusive source/compiler
cost, or run source variance, response variance, MSE, score, or leaderboard
work. The confidence interval happened to contain the reference-only value,
but cannot override either frozen failure.
