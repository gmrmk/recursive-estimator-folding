# M243 binding disposition — 2026-08-09

## Status

`KILLED_FROZEN_G0A_SOURCE_BYTE_ABI_AND_CERTIFIED_TAIL_RADIUS`

M243 consumed its sole authorized G0A launch.  The durable postpublication
receipt records `g0a_pass=false`, `component_verdict=false`, and
`relaunch_authorized=false`.  G0B sampling, G0B shards, native work, response
work, and all estimator credit remain closed.

## Durable evidence

- launch intent SHA-256:
  `7ab4044a2b2048f0b138beb807ff1eb53163ef43664463f377aa9ccf9872a976`
- provisional result SHA-256:
  `9cd0fd4155ed58a2ee35be94506daa8bffb6ed3b4b01b24160eb25e189bb7db8`
- binding postpublication receipt SHA-256:
  `7358e7447e6645319e374698bb80270eda8867ed1834cb83c0ded63ded6850a5`
- executed runner SHA-256:
  `e825142d83f2772a7b439bf12a22838a44c1e768eda6f107c33864a9ffd6265e`
- independent reference SHA-256:
  `b03e9b2ddda22d8ea147c0720ca8644c91bff2d22c8f82bd057b91a55bdc2c25`
- static runner-validation receipt SHA-256:
  `9bb56565c38b5eed6b8938d9e9da20316000ca71db6094545744e9d370236b8e`

The result transport, cross-links, one-scientific-worker topology, frozen
artifact stability, watchdog interval, and publication helper passed.  The
binding lifetime peak working set was 644,034,560 bytes and elapsed wall time
was 193.58936549999635 seconds, within the frozen G0A caps.

## Two independent frozen failures

1. `G0A-SOURCE-HALF-OWNERSHIP` failed its `source_swap_bitwise` subgate for
   both frozen rows.  The physical half-owner convention itself survived:
   proposal mass/support passed, `owner_factor=0.5`, and direct `Z/(2q)` was
   bitwise identical to applying one owner factor.  The failed assertion was
   raw byte equality between separately evaluated
   `source_feature_211(W,i,j,k)` and `source_feature_211(W,i,k,j)`.  The M151
   formula is algebraically singleton-symmetric, but the swap changes IEEE-754
   multiplication and summation order.  This kills the frozen two-orientation
   byte ABI; it does not refute the one-half ownership theorem.

2. The first scientific event, A0 `(0,1,2)`, completed neither requested
   precision.  During q2-or-q4 actual-radius integration, an extreme
   tanh-sinh Gaussian-tail node was converted to binary64 and passed through
   the absolute certified-interval API.  Polynomial radius propagation
   overflowed at `folded_distinct_event -> _outward`, which raised
   `M243DomainRefusal("non-finite certified interval")`.  Consequently
   `G0A-NO-UNCAUGHT-EXCEPTION` failed, zero of eight events completed, and
   thirteen scientific gates were never reached.  This is no evidence that
   the center formula is wrong, but it kills M243's declared all-finite-g
   binary64 certificate.

## Preserved tissue and credit boundary

Preserved only as uncredited algebraic tissue: the static beta/R formulas,
universal add-back identity, typed collision refusal, exact census, frozen
environment, and firewall.  M243 earned no formula-component, provider,
total-support, source-variance, native-cost, response, or score credit.

No threshold, fixture, source-byte requirement, tail policy, or launch rule may
be changed in M243.  No M243 rerun is lawful.

## Lawful descendants

Any continuation must be a separately predeclared mutation.  The two minimal
premises are independent:

1. canonicalize the unordered singleton pair before every M151 feature call,
   retaining a one-byte-path bitwise gate; and
2. replace unbounded-node binary64 absolute-radius evaluation with an
   analytic or scale-normalized Gaussian-Hermite tail enclosure, including
   structural-zero radii.

A distinct weighted-Galerkin spectrum oracle may instead bypass both failed
interfaces by using canonical scalar owners and independent high-precision
`r(g), b(g)` construction.  It remains a new theorem premise, not M243 credit.
