# M205 notes -- exact physical-owner component, not a usable estimator

M205 isolates one precise repair to M204.  M204's M156 conservation lift
subtracted the rank-one control from a target whose collision cells were zero.
M205 instead puts every collision cell under the physical M167 ownership map:
`K4` for `[4]`, directed `K31` for `[3,1]`, and symmetric `K22` for `[2,2]`.
The residual is the literal table difference `T-c`; no collision cell is
zeroed after ownership is assigned.

The rank-one loading itself is **not** a new competitor to M204's
`u_i=sqrt(V_ii)/sqrt(n_active)` Rademacher choice; it is exactly that fixed
loading, including its all-zero extension.  M205 is distinct only in the
complete physical target/residual owner boundary.  Thus no loading-level
efficacy claim or additional tuning degree of freedom has been introduced.

The two active B=1 nodes have signed conditional means `mu+u` and `mu-u`,
with weights one-half and a common nonnegative conditional-variance star.
This is valid even when a coordinate's mean is zero.  The loading is the
predeclared M204 diagonal-scale rule and is total at an all-zero variance
diagonal (`u=0`), where the control is identically zero.  This represents the
canonical rank-one state built from the diagonal; it does **not** claim to
reproduce every off-diagonal entry of the incoming M179 covariance.

The response-free tests exhaust widths 3, 4, and 5 and verify the following
separate statements:

- the M151 distinct-label Rademacher identity is exact;
- the one-square formula reconstructs every `aaaa`, `aaab`, and `aabb` slot of
  the full ordered rank-one control;
- all physical collision owners are enumerated and the complete source obeys
  `source(T) = source(c) + source(T-c)`;
- source labels may be permuted and positive ReLU gauge may be applied without
  changing the reconstructed source.

These are component identities only.  The physical source coefficients still
have no demonstrated layer-bound provider.  Nor is a complete-domain proposal
or residual-event accounting present.  A protected float64 square through 31
source layers is `2,595,389,440` billed FLOPs, already above the strict
`1,986,871,472` no-replacement headroom.  M151's apparent dense-emission
booking is an unexecuted allowance: it cannot be reused as a cost credit until
an integrated trace proves the exact same operands, dtype, result, lifetime,
and billed call.  The only honest disposition is therefore
`BLOCKED_PHYSICAL_COLLISION_PROVIDER_AND_NATIVE_COST`.

No source-variance, response, MSE, score, truth, scorer, contest-instance, or
submission path was created or run.  The first lawful next gate is a separate
native physical-provider plus whole-pipeline cost trace; it must pass before
any efficacy experiment may be predeclared.
