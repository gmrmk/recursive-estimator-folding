# M216 disposition: kill this provider implementation; preserve the exact antithetic atom

Status: `KILLED_FROZEN_NUMERICAL_ENVELOPE_AND_NATIVE_WALL_GATES`.

M216 is not a complete Source211 provider.  It intentionally covers only
pairwise-distinct `[2,1,1]`; every collision and the four-distinct wedge raise
a typed refusal.  No source-variance, response, truth, scorer, challenge
weight, MSE, score, or leaderboard run was opened.

## Passed components

The changed mathematical mechanism survived.  For a standard normal outer
coordinate, `(Z(g)+Z(-g))/2` has the same expectation as `Z(g)` by exact normal
symmetry.  Ten frozen width-3--7 events agreed with the independent
60/80-digit adaptive one-dimensional oracle; the worst absolute expectation
error was `3.7996949421630283e-13`.  M213's GH64/96 rule and its tolerance were
not reused or changed.

All 150 singleton-swap, positive-gauge, and permutation probes passed.  The
largest scaled gauge error was `4.60206259935462e-16`; singleton swap was
bitwise identical in the tested implementation.  The static local-DAG census
was 1,288 charged FLOPs against the frozen 4,096 allowance.  The resulting
9,384 observed worst event count is below the declared 12,192 ceiling, and an
actual two-call FlopScope probe billed exactly 7,064, matching the static
M178 branch counts.  Peak RSS was 44.45--44.53 MiB, below 512 MiB.

These are preserved components, not promotion evidence.

## Frozen failures

The numerical envelope failed without refusal or nonfinite output.  Across
2,730 frozen event probes the worst normalized propagated radius was
`9.206313540235052e-7`, 4.603 times the locked `2e-7` limit.  It occurred at
width 6, seed `216700006`, labels `(1,1,0,2)`, and `g=8`, where the midpoint
was `-0.030630120000540686` and the radius `9.488304028735055e-7`.  The
repeated centered activation is squared, so a broad but valid conditional
pair enclosure is amplified in the far outer tail.  M216 does not claim that
the actual value is inaccurate; it claims that this implementation cannot
certify the frozen pointwise envelope.

The interpreter implementation independently failed the native wall gate.
Exactly 3,968 coupled events took 1.613--1.728 seconds across five seeds.  The
hostile effective costs were `806.744B--864.222B`, or 118.2--126.6 times the
`6.824272176B` component ceiling.  Static arithmetic is cheap; scalar Python
dispatch through two M178 evaluations is the binding resource mechanism.

## Firewall and salvage-directed next mutation

Because not all prerequisite gates passed, no variance predeclaration, runner,
or result exists.  In particular M216 did not inspect a coefficient-only
variance and did not omit the nested outer randomness from `F/(2q0)`.

The surviving branch is narrower but real: strict-distinct antithetic
conditioning is algebraically exact, highly covariance-consistent, and
statically cheap.  A legitimate successor must change both failed links:

1. derive a joint antithetic tail enclosure (or an analytic saturated-tail
   chart) whose *proved* radius survives the squared repeated activation,
   rather than relaxing or probability-weighting away the pointwise gate;
2. batch/compile M178 chart strata so 7,936 primitive calls no longer cross
   Python one by one, then rerun the same 3,968-event hostile native gate.

Either change is a new named mutation.  The present M216 implementation stays
killed and is not retuned.
