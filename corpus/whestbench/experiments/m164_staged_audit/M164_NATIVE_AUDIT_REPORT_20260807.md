# M164 native audit closeout

Status: `RESOURCE_CLOSED_NO_DEVELOPMENT_EFFICACY_RUN`.

M164 audited the frozen M163 exterior edge `A = V o (1 - R^2)` with the exact five-product M156 source compiler, using generated width-256 float64 Gaussian matrices for 31 layers.  It read no model response or contest artifact.

All five fresh processes were finite, used exactly 155 matrix multiplications, and billed 10,444,656,904 FLOPs, below the 14,019,121,200 compiler slot. Peak physical working set was 81.34 MiB. The generated permutation and diagonal-gauge tests passed at `2.22e-16` and `1.67e-16`, respectively, versus a `2e-8` tolerance.

The binding failure is residual wall time. The five deployment traces had 9.456--10.639 ms residual time; the p99 was 10.625 ms. The inherited cost plus this compiler bill leaves only 7.149 ms per process under the required hostile 5x residual calculation. Consequently, every trace projects over the 100B cap (p99: 101.738B).

The source-variance screen was intentionally not opened: its sealed precondition was that every native resource and invariance gate pass. Therefore there is no valid full-source ratio, source p99, width trend, ijj-only collision contribution, or M159 robust numerical envelope for M164. This is a resource closure, not an inference or efficacy result.

Inference consequence: M163 successfully removes the `iii`, `iik`, and `iji` collision strata algebraically and preserves the symmetries, but a candidate with this many dense dispatches has too little residual-time headroom in the inherited cost envelope. Future variants must retain the exterior null while reducing call/dispatch/allocation overhead before another efficacy screen is justified.
