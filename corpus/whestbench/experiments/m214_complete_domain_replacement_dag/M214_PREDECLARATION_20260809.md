# M214 predeclaration -- complete-domain replacement DAG ledger

Date: 2026-08-09. Frozen before implementation. M214 is a response-free
static architecture/cost audit. It may read only committed component ledgers;
challenge models, truth, scorer, responses, leaderboard, submissions, and
efficacy records are banned.

## Parent failure and changed mechanism

M199 treated every later complete-domain component as additive to frozen M151,
leaving only 1.986871472B before unknowns. M206 correctly forbids claiming
that M205/M212 is arithmetic-identical to M151's strict distinct-only source
emission. M214 does not claim reuse or overlap. It changes the DAG itself:

1. retain the full conservative M151 common-fixed subtotal and K=128 five-
   product residual subtotal;
2. do not execute M151's distinct-only B=1 control core;
3. do not execute its deterministic endpoint coefficient provider;
4. execute M179 additively with no legacy-background replacement credit;
5. execute M212 as the new complete-domain rank-one control compiler;
6. conditionally execute M213 as the new complete-domain event-local residual
   coefficient provider.

Removing a node that the new estimator never calls is not sharing its credit.
Any old K4/K31/K22 output still computed inside the retained common subtotal
must be discarded before source assembly; its cost stays included. M205/M167
owns the replacement source exactly once.

## Frozen arithmetic

All amounts are billions of billed/protected FLOPs under their source ledger:

```text
retained M151 common fixed                 80.326002640
retained K=128 five-product residual        3.247411200
additive M179 exact background              8.304492288
M212 complete-domain control compiler       1.249253376
conditional M213 event provider             0.048568320
------------------------------------------------------
known replacement-DAG subtotal             93.175727824
remaining before all unknowns                6.824272176
```

The M213 line is a frozen provider-only projection, not earned cost credit.
M198 conversion, complete-owner issuer, terminal response, allocation/copies,
integrated residual wall, and full DAG routing remain nonnegative unknowns.
The explicit terminal covariance/readout floor 0.134217216B may be reported as
a sensitivity, leaving 6.690054960B, but is not a complete terminal trace.

## Gates

1. Exact arithmetic reproduces every source subtotal and the M151 total.
2. No M179/legacy background overlap or M151 source-emission reuse is credited.
3. M151 B1 and endpoint nodes are marked `removed_by_new_dag`, never
   `overlapped` or `replaced_same_call`.
4. Old collision outputs are retired from semantics while their retained
   common cost remains charged.
5. Status remains blocked unless every unknown plus actual integrated wall is
   <=6.824272176B, M213 passes its independent provider gate, and M205
   complete-domain conservation is bound at the actual caller.

No variance, MSE, score, or winner claim is authorized by this ledger.
