# M235 strict-TDD receipt

Date: 2026-08-09. Generated-only. G0 remained unopened throughout every
receipt below.

## Original setup/algebra RED

`test_m235_setup_and_algebra.py` was created at
`2026-08-09 11:16:11 -05:00`, before
`m235_setup_shared_philox_row_receipt.py` was created at
`2026-08-09 11:17:33 -05:00`. The pinned command was:

```text
work/whest-v014/Scripts/python.exe -m unittest -v test_m235_setup_and_algebra.py
```

The collection failed for the intended reason:

```text
ModuleNotFoundError: No module named 'm235_setup_shared_philox_row_receipt'
```

The original test-file SHA-256 prefix independently captured before module
creation was `5410B13C...`; the complete original bytes and an exact command
wall timestamp were not retained, so this receipt does not invent either.

At `11:17:45 -05:00`, after the first GREEN attempt, the test received one
harness-only 48-byte repair: import `issue_full_domain_receipt` from
`m215_flopscope_sidecar` and call that imported name instead of assuming the
pure algebra module `m215_rankone_collision_correction` exported the sidecar
receipt constructor. No M235 operation, coefficient, seed, threshold, cost,
or candidate behavior changed.

## Adversarial coverage RED and GREEN

The missing frozen row-loop, gauge, zero, and cubic gates were added for every
width `3..9`; the target row-oracle comparison was expanded from layers
`0,15,30` to all 31 layers. A production-sandbox assertion was also added.

Exact RED interval:

```text
2026-08-09T11:21:31.4147568-05:00
2026-08-09T11:21:32.2218393-05:00
```

The pinned command ran five tests: four passed and one failed only because
the production module still contained `import numpy as np`. The independent
row-loop, gauge, zero, cubic, marginal-exhaustive, and all-31-layer algebra
checks already passed in that RED execution.

The minimal GREEN removed the host-only co-permutation helper and raw NumPy
import from the production module; label co-permutation remains in hostile
test code. Exact GREEN interval:

```text
2026-08-09T11:21:53.8724532-05:00
2026-08-09T11:21:54.6739546-05:00
Ran 5 tests in 0.452s
OK
```

A second static RED then exposed the production dependency on
`m215_flopscope_sidecar`, whose pure oracle import would transitively load raw
NumPy. The only repair was to remove that import/path dependency and use the
existing receipt's structural ABI; no arithmetic changed.

## Native RED

Exact interval:

```text
2026-08-09T11:22:00.4693194-05:00
2026-08-09T11:22:00.6307347-05:00
```

Command:

```text
work/whest-v014/Scripts/python.exe -m unittest -v test_m235_native_contract.py
```

It failed at collection for the intended missing implementation:

```text
ModuleNotFoundError: No module named 'run_m235_native_process'
Ran 1 test in 0.000s
FAILED (errors=1)
```

No native result or G0 result existed at this point.

## Same-worker topology RED

The first native implementation was statically rejected without executing its
split lifecycle. Exact RED interval:

```text
2026-08-09T11:31:14.8663078-05:00
2026-08-09T11:31:17.0426945-05:00
```

The test failed because host `run_process` still contained a direct
`m235.setup_component` after closing the official worker. It was replaced by
one persistent official setup/predict worker plus hostile external snapshots.

The same-worker audit then exposed, in order, and preserved these RED states:

1. entrypoint sibling-import order could not locate `m212_flopscope_sidecar`;
2. timed stage code still allocated lists and scanned shape/dtype metadata;
3. a setup-frozen identity tuple failed to detect a hostile live-field rebind;
4. the Philox rank receipt was Fortran-contiguous, not C-contiguous;
5. `ReadProcessMemory` targeted the venv redirector PID rather than the actual
   worker PID;
6. a custom Sequence was not recursively stripped by FlopScope `stack`.

Each repair was confined to audit/provenance topology: import ordering,
setup-owned staging slots, live post-kernel identity enumeration, declared-
stride remote reconstruction, manifest `os.getpid()`, and a supported list
container for the unchanged two stack calls. No estimator coefficient, seed,
`k`, positive operation, bill, or threshold changed.

## Final native resource RED / stop

The final one-process run executed all three official predictions on one
worker. Exact test interval:

```text
2026-08-09T11:43:27.7078643-05:00
2026-08-09T11:43:39.2862784-05:00
```

Every algebra, exact-call, exact-bill, component-wall, combined-wall,
identity, receipt, finiteness, symmetry, and bitwise replay gate passed. Peak
worker RSS was `468.613281`, `554.625000`, and `557.644531 MiB`; predictions
2 and 3 violated the frozen `512 MiB` cap. Fixed M235 was therefore killed.
The ten-process aggregate and G0 remained unopened.
