# M172 predeclared protocol: selective physical `[2,2]` owner fusion

Status: `PREDECLARED_NOT_EXECUTED`. M172 may execute only after an exact M169
M163 compiler descendant passes every frozen native resource gate. No outcome,
truth, scorer, leaderboard, submission, or champion access is authorized.

## One changed mechanism

M167 proved that the two complete-table representatives of the physical
multiset `{i,i,j,j}` each own `K22[i,j]/2`. M172 transfers **only** this
physical `[2,2]` owner into M163's complete table and retires exactly the old
separate `[2,2]` source. The `[4]` and `[3,1]` physical owners remain where
they are because M163's exterior control is already zero on their collision
patterns.

For `i != j`, with M163

```text
A_ij = V_ij (1-R_ij^2),
cE_ijj = -2 A_ij^2,
```

the M172 residual is

```text
r_ijj = K22_ij/2 - cE_ijj.
```

Distinct `[2,1,1]` entries retain their exact target-minus-control residual.
The `iii`, `iik`, and `iji` rows are identically zero in this arm and their
physical source remains owned exactly once by the unchanged separate owners.
No M163 coefficient, endpoint, dtype, carrier, or control compiler changes.

## Bias and ownership class

Exact add/subtract control conditional on the frozen proposal:

```text
old [2,2] physical source
  = sum_{i<j} K22_ij F_ij
  = sum_{ordered ijj reps} (cE_ijj + (K22_ij/2-cE_ijj)) F_ijj.
```

Retaining any part of the old `[2,2]` source after this transfer is a hard
double-count failure. Re-zeroing collisions with M156's old `residual_table`
is also a hard failure; M167's complete residual interface is mandatory.

## Prediction

The old M163 `ijj` term was an artificial zero-target residual `-cE`; M172
aligns the same control with the physical target on exactly the same support.
The predeclared prediction is that this both removes the support-mismatch tail
and replaces the separate hard `[2,2]` probe without adding a dense product.

## Premise and kill ladder

1. **Resource precondition:** the exact M169 compiler must pass five fresh
   hostile processes below `100B`, exact parity, RSS, invariance, and bill.
   Otherwise M172 remains unopened.
2. **Ownership theorem:** exhaustive widths `2..7` must match an independent
   symmetric fourth-tensor source, preserve permutation and positive-gauge
   covariance, and detect both retained-owner double count and collision
   re-zeroing. Kill on any discrepancy above `3e-10`.
3. **Endpoint:** every frozen physical `K22` cell must pass the existing exact
   M122/M147 pair-stratum certificate. No clipping, ridge, retry, or fallback.
4. **Cost:** the removed separate `[2,2]` calls and the added eventwise
   coefficient/subtraction/sampler operations must be traced inclusively.
   Kill if any hostile projection loses M169's resource pass.
5. **Development source variance:** run exactly once on six new generated
   cells, fixed before target construction:

```text
(iso_w5,5,1720501,.22)  (factor_w5,5,1720502,.50)
(iso_w6,6,1720601,.22)  (factor_w6,6,1720602,.50)
(iso_w7,7,1720701,.22)  (factor_w7,7,1720702,.50)
```

Use M163's unchanged target-width collision mass
`eta=0.011688232421875`, now uniformly over the nonzero ordered `ijj`
representatives; zero rows receive no mass because they are exactly absent
from this arm. Distinct support and its mass remain frozen.

Development passes only if the paired-bootstrap upper-90 residual/raw source
variance is `<.25`, maximum normalized p99 is `<=1.25`, `[2,2]` p99 is
`<=1.25`, and the mean ratio slope versus width is nonpositive. Report the
raw physical `K22/2`, `cE`, and residual contributions separately.

6. **Confirmation:** only after every development gate passes, run once on
   the sealed cells

```text
(iso_w5c,5,1721501,.22) (factor_w5c,5,1721502,.50)
(iso_w6c,6,1721601,.22) (factor_w6c,6,1721602,.50)
(iso_w7c,7,1721701,.22) (factor_w7c,7,1721702,.50).
```

No response/output efficacy opens from a source pass. A subsequent one-carrier
integration must receive its own frozen protocol and matched whole-network
gate.

## Failure localization

- If ownership passes but variance fails, preserve selective owner transfer
  and reject `cE` as a `[2,2]` control.
- If variance passes but cost fails, preserve the source mechanism and mutate
  only the compiler/sampler schedule.
- If M169 fails first, M172 is neither passed nor killed; it remains blocked on
  the resource precondition.
