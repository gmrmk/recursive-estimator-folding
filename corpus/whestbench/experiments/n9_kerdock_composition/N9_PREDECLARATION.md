# N9 predeclaration — the Kerdock composition (frames + tangent control + deeper fold)

Date: 2026-08-08 (before code). The last unbuilt honest lever: three
measured-compatible components that have never been composed.

## Components and their measured priors

1. Kerdock phased-Hadamard spherical design (v3 draw stage): 2.0-3.2x over
   radially-conditioned MC (N8a, observed).
2. Moment-tangent control (tangent lineage, frozen lambda ~0.9807):
   -19.8% adjusted ON ITS OWN LINEAGE (official dev20, reported). Unknown on
   Kerdock frames — the frames may already capture much of the first-layer
   residual the control subtracts (redundancy risk, cf. N5/N8a).
3. Deeper terminal folding (fold3 machinery, dead/on/kink): reduces
   per-sample billed FLOPs; v3 already folds at L1, so the increment is
   L1 -> L3.

## Fold-discipline note

Composing two winners requires an interaction test FIRST (skill: "Before
composing two winners, run a factorial interaction test"). G0 is exactly
that, run at premise scale.

## G0 interaction premise (cheapest falsifier, both halves)

- G0-a (tangent-on-frames): on 3 synthetic He nets at matched samples,
  paired variance of (i) the Kerdock spherical mean vs (ii) the same minus
  lambda x the tangent correction (frozen lambda, the lineage's exact
  response map). KILL the tangent component if mean variance reduction < 10%
  (the lineage's own gain was ~20-25%; frames plausibly absorb it).
- G0-b (fold increment): static billed-FLOP count of the v3 pipeline vs an
  L3-folded variant structure (counting only, no build). KILL the fold
  component if the billed reduction < 15%.
- If BOTH die: N9 KILLED — and the honest local program is exhausted with
  the composition explicitly measured, not assumed.

## Build gates (for surviving components only)

- G1: minimal diff into the frozen v3 scaffold (component-only change).
- G2: paired factorial vs frozen v3 (>= 6 synthetic nets x 4 replicates):
  combined adjusted-equivalent improvement >= 1.2x (variance gain x billing
  gain), bootstrap CI excluding 1.0; billed accounting exact; ZERO memory
  regression (the 1.445 MiB hosted margin binds).
- G3: folder-mode package + validate-package + contract validate + member
  listing.

## Honesty bound (stated in advance)

Full success ~ 1.25 x 1.4 ~ 1.75x -> adjusted ~ 9e-8: top-tier of the honest
frontier, projected rank ~10-14 on the displayed board. This mutation is NOT
a route to the displayed #1 (4e-10, an unmetered-compute tier under organizer
review); it maximizes the honest position the user inherits at login.

## Firewall

Synthetic nets only; frozen v3 and tangent sources read-only; no sealed
cells; no public rows; no submission; all variant work metered.
