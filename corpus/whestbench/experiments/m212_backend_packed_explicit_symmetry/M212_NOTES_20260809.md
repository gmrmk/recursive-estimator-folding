# M212 disposition

M212 folds M210's successful four-call dispatch topology with M211's explicit
transpose ownership. Supported `fnp.matmul` calls retain their non-contiguous
views because FlopScope bills matmul from operand shapes independent of backend
packing. The caller-created overlapping transpose add is removed: the existing
named `scratch` plane receives the transpose by charged `copyto`.

All three contract tests and all five fresh target-shape traces passed.

- exact inclusive bill: `1,249,253,376`;
- exact matmul bill/calls: `1,167,925,248` / 4;
- exact reshape bill/calls: `16,252,928` / 4;
- residual range: `1.131999859..1.201899868` ms;
- worst five-times-wall projection: `1,850,203,309.868833`;
- minimum hostile margin: `136,668,162.131167`;
- no user-created untracked full-plane temporary;
- a target-shape allocator probe measured only 8,404 bytes of traced peak
  allocation for the explicit copy-plus-add, versus a 16.25 MiB plane;
- RSS remained below 512 MiB.

Disposition:
`METER_LAWFUL_RESOURCE_COMPONENT_PASS_PROVIDER_AND_STREAM_BLOCKED`.

This is an isolated response-free component. It does not prove M179-bound
factor staging, immediate M198 consumption, physical K4/K31/K22/C211,
residual proposal, terminal cost, source variance, MSE, score, or winner
status. Under M199 strict no-replacement accounting it leaves
`737,618,096` FLOPs before every such missing term.
