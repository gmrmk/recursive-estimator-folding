# M228 disposition: kernel-only execution component passes

M228 preserves M226's exact 171-call `5467N` kernel and resolves the false
claim that its two event-dependent singleton-sigma columns were free direct
inputs. The full caller ABI is now prepared and disclosed before binding; the
raw timer contains only the already-bound persistent kernel.

All five frozen fresh processes and an independent repeat of seed `221720001`
pass the unchanged raw gate. Wall times span `11.5401..13.0404 ms`, speedups
span `123.7226..139.8074x`, and all traces retain exact bill `21,693,056`,
171 calls, zero fallback/chart mismatch, and M224-radius parity. The separate
allocation audit measures 536 source-attributed Python bytes per process and
stable actual persistent slabs of 8,515,328 bytes.

This is not an inclusive challenge-predictor result. Caller setup remains
event-dependent: it constructs 63,488 bytes of marginal singleton-sigma
inputs and takes `1.9291..2.0289 ms` in these fresh processes. M228 receives
zero integrated-cost credit unless an upstream implementation proves those two
columns already exist as live caller operands. Variance, scoring, truth, and
response work remain closed.
