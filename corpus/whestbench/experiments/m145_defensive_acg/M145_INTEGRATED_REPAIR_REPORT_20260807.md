# M145 integrated repair and zero-outcome handoff -- 2026-08-07

## Disposition

**INTEGRATED STRUCTURAL SURVIVOR; AWAITING AN INDEPENDENT THIRD HOSTILE AUDIT.**

The second-audit integration failures have been repaired in an isolated
Formal-L1 descendant. The candidate now constructs the promised pilot statistic,
freezes the ACG proposal before touching main frames, executes the complete split
Formal path, restores its provisional bank bit-for-bit, and has a matched
transport-disabled comparator. Exact FlopScope traces, peak-working-set traces,
truth-free Formal parity, replay checks, a frozen generated protocol, and a
separate unbiased cross-reference risk protocol are sealed.

This is not an efficacy result. No truth, reference output, error, MSE, risk,
adjusted score, contest row, rank, submission, designation, or champion change
was opened. The old isolated sidecar trace remains historical failed evidence;
the integrated traces listed below supersede it.

## Repairs against the second hostile audit

### Radius-scaled law

Every stored frame row has norm `rho_256`, matching immutable Formal-L1. The
Householder target is `rho_256 * anchor`, not a unit anchor. A direct test checks
the normalized conditioned first row against the anchor while checking the full
radius-squared Gram matrix. The target setup bank is independently reconstructed
and is bitwise equal to the estimator bank.

### Exact provisional QR convention

Candidate and comparator use the exact raw `fnp.linalg.qr` convention of the
hash-bound Formal source. No unbilled positive-diagonal normalization was added.
Tests reconstruct that convention directly, distinguish it from sign-normalized
QR, and check sign-invariant second/fourth projective moments plus the conditional
completion second moment. This aligns the tested and executed conventions.

The tests are finite moment evidence, not a general theorem that every
projective-frame functional of the raw LAPACK convention is Haar. That remaining
mathematical scope is explicitly left for the independent third audit; no
efficacy claim depends on silently promoting it to a proof.

### Pilot-first proposal

The candidate performs a direct raw 32-layer forward pass over the four pilot
frames (`1024` radius-scaled lines and their antipodes). It materializes the
`1024 x 256` even final-activation surrogate, then fits the full symmetric
scatter/eigendecomposition proposal. The proposal and all mixture coefficients
are frozen before any of the 122 main frames is reflected.

The locked event order is:

```text
pilot_surrogate_materialized
proposal_frozen_from_pilot_only
main_transport_applied_after_proposal
split_formal_path_entered
split_formal_path_complete
main_transport_restored_and_canonicalized
```

### Complete split Formal-L1 path

The integrated descendant preserves the first-256 rescue, 1024-line fold
refinement, dead/on/kink regimes, terminal folding, all-output assembly, and
moment tangent. Pilot and main streams are separate. Frozen conditional weights
are applied only to complete-frame linear means; they are not applied pointwise
to dependent nodes.

The source imports immutable Formal-L1 dependencies through a hash-bound path.
The frozen dependency tar hash is
`bc2ec39558c76a67b12b587ca4ee70bb1e8921489643d83707e052d086e8ae36`.

### Matched causal comparator

`MatchedComparator` consumes the exact same setup seed and bitwise-identical
provisional radius-scaled frame bank. Transport is disabled and all 126 frame
coefficients are ordinary. Its locked trace deliberately has
`second_predict=null`: the comparator was run once because it never mutates its
bank. This is not a failed replay. The candidate, whose transport does mutate
and restore state, received the mandatory second prediction and reproduced its
output bit-for-bit.

### Liveness and restoration

The final schedule uses one owned 4096-row Winograd workspace, direct streamed
dispatch into owned output, and streamed layer-31/terminal kink reductions. A
charged immutable copy of the provisional main bank is restored after the
self-inverse reflections in a `finally` path. Both first and second restore
defects are exactly zero, and the repeated prediction maximum difference is
zero.

## Locked structural evidence

All values below are truth-free and come only from the final JSON traces.

| measure | integrated candidate | matched comparator |
|---|---:|---:|
| billed FLOPs | 184,270,895,262 | 176,455,830,878 |
| residual wall seconds | 0.140115606 | 0.121653715 |
| effective compute | 198,282,455,828.72 | 188,621,202,369.57 |
| exact matmul calls | 1,078 | 798 |
| operational peak working set | 481.977 MiB | 448.523 MiB |
| failure | none | none |

The paired integrated delta is `7,815,064,384` billed FLOPs plus
`0.018461891 s` residual, or `9,661,253,459.15` effective operations. Applying
that measured delta to the previously measured Formal-L1 distribution gives:

```text
protected mean C                  199,513,809,459.15
protected max C                   232,066,610,459.15
headroom to 258.4B safety line     26,333,389,540.85
mean cost ratio                              1.050888
raw-risk ratio needed for adjusted .80       0.761261
```

These are cost identities, not performance estimates.

## Truth-free Formal parity

The immutable Formal-L1 estimator and the split matched comparator were run on
the same generated network and same provisional bank without truth:

```text
both finite                         true
maximum absolute difference         1.2474601e-5
RMS difference                      4.2827081e-7
maximum relative to output scale    2.5947432e-6
```

This certifies close float32 reassociation parity for the named hooks. It does
not certify efficacy or exact bitwise equivalence to Formal-L1.

## Complete structural rerun ledger

No development iteration is omitted. Intermediate timing is diagnostic only;
only the final locked JSON traces are evidence.

| ID | structural result | bill | residual seconds | effective C | peak MiB | disposition |
|---|---|---:|---:|---:|---:|---|
| I0 | first candidate console execution | not retained | not retained | not retained | not retained | stdout was lost after yield; immediately rerun; no locked evidence |
| I1 | initial candidate | 184,253,842,496 | 0.256191 | 209,872,901,878.01 | truncated | finite, but second restore defect `2.384e-6`; not locked |
| C0 | initial comparator | 176,446,773,505 | 0.243391 | 200,785,852,430.55 | 602.988 | failed memory |
| C1 | weighted-temporary/pre32 folding | 176,446,773,761 | 0.190088 | 195,455,603,322.06 | 544.551 | failed memory |
| C2 | shared workspace/terminal stream | 175,474,696,785 | 0.175268 | 193,001,446,865.29 | 522.703 | failed memory and semantically invalid pilot-view overwrite |
| C3 | x31 stream | 175,476,679,869 | 0.218901 | 197,366,829,297.51 | 522.855 | failed memory and same overwrite |
| C4 | stage-instrumented | 175,476,679,869 | 0.215332 | 197,009,870,211.60 | 522.691 | failed memory and same overwrite |
| C5 | preallocated activations; overwrite fixed | 176,434,485,470 | 0.136005 | 190,034,944,373.24 | 530.297 | semantically repaired; failed memory |
| C6 | dynamic activation plus 4096 workspace | 176,451,000,542 | 0.221109 | 198,561,852,605.63 | 522.840 | failed memory |
| C7 | 4096 terminal chunks | 176,455,830,878 | 0.235503 | 200,006,110,712.31 | 523.430 | failed memory |
| C8 | activation reuse | 176,455,830,878 | 0.164798 | 192,935,640,204.12 | 516.762 | failed memory by 4.76 MiB |
| C9 | direct stream into owned workspace | 176,455,830,878 | 0.117821 | 188,237,961,034.64 | 448.480 | resource pass |
| A1 | integrated candidate before canonical copy | 184,262,899,870 | 0.149960 | 199,258,879,643.81 | 450.801 | restore defects `1.907e-6`,`2.384e-6`; replay failed |
| A2 | immutable canonical-copy repair | 184,270,895,262 | 0.140116 | 198,282,455,828.72 | 481.977 | final candidate pass; locked |
| C10 | final-source comparator rerun | 176,455,830,878 | 0.121654 | 188,621,202,369.57 | 448.523 | final comparator pass; locked |
| P0 | immutable Formal vs split comparator | see parity JSON | see parity JSON | structural only | not used | parity pass; locked |

The C2-C4 overwrite bug is retained in the ledger as a failed mechanism rather
than erased. It arose because a shared workspace overwrote a pilot first-product
view before the pilot surrogate had been materialized. C5 repaired that semantic
link; later mutations solved memory without reopening it.

## Frozen outcome protocols

### Existing 2 x 1e9 truth protocol

`M145_GENERATED_EFFICACY_PROTOCOL_20260807.json` remains byte-for-byte unchanged
at SHA-256
`68ea6625322df0870fc5c19f4667597eb883c667d834cdc443f0a924a8977a41`.
Its execution flag remains false.

### New independent cross-reference risk protocol

A separate protocol freezes eight independent randomized-Haar reference pairs
for every one of the 24 generated networks. For fixed `A`, independent unbiased
references `R1,R2` give

```text
E[(A-R1).(A-R2)/256 | W,A] = MSE(A|W).
```

Using the same pair for candidate and comparator cancels `R1.R2` exactly in the
paired difference. All finite negative absolute-risk estimates are valid and
retained. Inference uses a joint hierarchical network/reference bootstrap, and
ratio promotion fails as ambiguous if any comparator aggregate denominator is
nonpositive.

The protocol freezes 384 unique rotation seeds under seed-table SHA-256
`908782ec1f4bd5ffa825d0320b372e6d68ecec4caf59f954aeefd0962d65b1cb`.
Its dense-plus-activation campaign lower bound is
`103,903,848,824,832` operations. This is offline validation compute, never
submitted-estimator compute.

No standalone reference evaluator exists yet, so this protocol is explicitly a
recommended next descendant awaiting implementation and independent audit. It
has not generated a single reference vector.

## Verification

The final combined manual structural harness collected and passed **32/32**
tests under the WhestBench 0.14 environment. `py_compile` passed over the core,
sidecar, crosswalk, integrated estimator, both runners, and both test modules.
The added tests bind:

- the unchanged sealed truth protocol;
- the exact cross-reference seed-table encoding and disjointness;
- all Kerdock reference-asset hashes;
- direct cost arithmetic and the cross-risk proof fields;
- the zero-outcome manifest and execution/rank firewalls.

No test consumed truth or produced an efficacy outcome.

## Remaining blockers and next gate

1. A third hostile auditor must inspect the integrated source, exact raw-QR
   projective-law scope, locked traces, parity, protocol arithmetic, and hashes.
2. The cross-reference path needs a standalone streamed direct evaluator and a
   truth-free wall/storage/numerical dry run, followed by its own hostile audit.
3. Only a later explicit authorization may run one frozen efficacy protocol.
4. Until that run passes its predeclared gates, M145 has no performance rank and
   cannot be called a winner, promotion, or champion replacement.

Recursive-estimator-folding disposition: radius scaling, defensive density,
pilot-first proposal, centered complete-frame coefficients, matched coupling,
streamed memory schedule, canonical restoration, and common-reference
cross-risk survive as reusable components. The raw-QR general-law proof and all
efficacy claims remain unresolved rather than dismissed.
