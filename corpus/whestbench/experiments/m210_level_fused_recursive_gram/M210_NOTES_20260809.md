# M210 closeout -- hostile resource pass, provider still blocked

M210 repairs exactly the M209 failure. It retains the depth-3 symmetric Gram
tree and M205 source algebra, but batches all same-level nodes. The resulting
target schedule uses four matmul dispatches instead of fifteen.

Eight response-free tests pass. They cover integer/f64 Gram parity, dense
M205 source parity, exact symmetry, gauge/permutation/zero-state behavior,
canonical layer order, duplicate-object rejection, and compile-before-stage
refusal. Five fresh pinned FlopScope 0.10.0 processes all report:

- matmul: 4 calls, exact bill `1,167,925,248`;
- reshape: 4 calls, exact bill `16,252,928`;
- inclusive compiler bill: `1,245,190,144`;
- arithmetic margin within the strict component slot: `741,681,328`;
- residual wall: 1.0826--1.2303 ms;
- hostile five-times effective maximum: `1,860,340,069.58`, leaving
  `126,531,402.42`;
- peak RSS below 176 MiB; persistent arrays reported explicitly; no rank-3
  coefficient table.

This is a real resource-component promotion. It escapes M206's dense-cost
premise by using a different exact arithmetic class; it does not claim free
reuse of M151 or M179. The f64 association differs from the historical dense
spelling, but the real algebra is exact and the source error stays below the
frozen `2e-10` threshold.

## Critical boundary

The executed caller owns synthetic generated `LayerInput` records. M210 does
not yet prove that the production M179 -> physical provider -> M198 -> M125b
chain can retain all 31 factors, Jacobians, source outputs, and weights with
the same lifetime and bill. M200 proved a streaming semantic ABI, not this
all-layer staging interface. Therefore the precise status is:

`RESOURCE_COMPONENT_PASS_CONDITIONAL_ALL_LAYER_STAGING_PROVIDER_BLOCKED`.

The next decisive mutation is not a deeper Gram recursion. It is an integrated
caller/lifecycle trace that emits the factor bank while M179 runs, invokes
M210 once all factors exist, and then feeds the exact physical residual and
M125b carrier without rebuilding the background. That trace must include the
still-missing physical `K4/K31/K22/C211` provider, M198 conversion, terminal
response, allocations, and wall. Until then M210 earns no source-variance,
MSE, score, leaderboard, champion, or winner claim.
