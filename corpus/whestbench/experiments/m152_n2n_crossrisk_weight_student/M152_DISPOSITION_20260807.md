# M152 disposition

## KILL — no generated cross-reference campaign

The noise2noise/cross-risk objective is valid under its stated independence assumptions, and the f32 fixed-radius target is explicitly defined. That does not justify execution.

M152 freezes the same 50-column analytic/gauge-equivariant message bank already subjected to generated-only cleanroom evidence. Its stronger non-deployable per-MLP oracle retained about 17.8% of anchor error and reported MSE `1.126683597e-5`; the universal ridge reported `2.144424096e-5`. The cited strict reference is `1.544730044e-7`.

An independent f32 Kerdock campaign would cost at least 69.269T dense-plus-ReLU operations for the minimum 32-MLP, four-pair plan. It would add label noise without adding weight-derived information or model capacity. The previous oracle is more permissive than the deployable ridge and fails the new protocol's deliberately forgiving 5%-of-anchor capacity threshold.

No completed semantic-equivalence fixture shows that the current direct f32 program is the intended reference program. M152 cannot honestly pass to premise while that gate is open.

This kill applies only to the frozen 50-feature universal-ridge mechanism. It does not reject cross-risk mathematics, generated-only research, or a future mechanism with a fresh protocol and fresh capacity oracle.

No M152 references, labels, model weights, metrics, public data, scorer calls, submissions, or champion changes exist.

