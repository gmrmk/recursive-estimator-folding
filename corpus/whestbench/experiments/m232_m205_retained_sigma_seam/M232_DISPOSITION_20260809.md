# M232 disposition: constructive identity preserved; live seam blocked

M232 proves the proposed retained vector is mathematically viable without
claiming it exists today. For generated M205/M224 material, the retained
`sqrt(diag(V))` equals M224's singleton marginal sigmas exactly, preserves
zero diagonals, reproduces M205's `factor=sigma/sqrt(n_active)`, preserves
gauge and permutation behavior, and yields M224-radius value parity through
the unchanged M228 kernel. Copy, epoch, and conditional substitutions refuse.

The current parent seam fails its frozen ABI requirement. `RankOneB1State`
retains `factor` and `diagonal_residual`, but no `marginal_sigma`; M212's
`LayerInput` accepts only `layer`, `weight`, `factor`, and `producer_epoch`.
Thus no actual live vector has object provenance from M205 through M212 to an
M224 event label gather.

No inclusive seam trace or six-process wall test was run, and M232 takes zero
integrated-cost/reuse credit. A separate proposed M233 may add a live retained
sigma producer wrapper and must restart with fully billed sqrt, factor
division/copy, label packing, gathers, and unchanged M228 compute. M233 is
not implemented here.
