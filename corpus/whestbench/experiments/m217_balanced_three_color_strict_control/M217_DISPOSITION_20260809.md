# M217 disposition -- kill the native configuration, preserve exact coloring algebra

Status: `KILLED_FROZEN_HOSTILE_WALL_GATE`.

M217 is a genuine strict-support mutation. A uniformly random balanced
three-color partition makes every repeated-label row bitwise zero, and the
inverse all-distinct probability makes its coefficient expectation equal to
M151's deterministic strict table. Conditional on the realized partition,
adding the full colored control to an event residual `Delta-c_h` is exactly
conservative. Generated widths 3--9 pass coefficient, source, positive-gauge,
joint-permutation, and conservation tests.

The target arithmetic also falsifies the claim that three class-local Grams
must pay three dense square products. A depth-three symmetry-recursive circuit
with the class and layer as batch axes uses exactly four matmul calls and bills
`1,172,496,384` matmul FLOPs. The inclusive component bill is
`1,474,032,640`, below the frozen `1.600B` arithmetic cap.

The same first frozen hostile seed fails the independent wall gate. Residual
time is `7.896099793 ms`, giving

```text
1,474,032,640 + 5e11 * 0.007896099793 = 5,422,082,536.280281,
```

well above the predeclared `2.250B` effective cap. Persistent arrays are
`260.109375 MiB` and peak working set is `324.340 MiB`, so memory passes. One
frozen failure is sufficient; the remaining seeds were not mined for a
favorable outcome. No threshold, color count, class sharing, or wall model is
retuned.

An independent Rao--Blackwell audit also proves that at fixed `K`, proposal,
and provider, averaging out the coloring gives the deterministic M151 control;
therefore coloring can only add variance at equal residual sample count. Its
only possible value would have been a compiler-cost reduction large enough to
buy a prespecified higher `K`. The frozen wall failure prevents opening that
tradeoff here.

No source variance, M198, terminal, response, MSE, score, or leaderboard work
was run. A future child must change the failed wall mechanism itself (for
example, a single supported fused contraction with a new predeclared cost
model); deleting accounting or rerunning M217 with looser thresholds is not a
repair.

