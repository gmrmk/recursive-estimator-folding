# M179: exact labelled zero-order BackgroundArchive producer

## 1. STATUS and scope

**COMPONENT PASS** — an exact, certified, FlopScope-metered, labelled
zero-order BackgroundArchive producer for the M176 contract, built from the
M178 provider. Response-free; no efficacy, score, variance, source-conversion,
or champion credit. This is precisely the "bundled FlopScope bivariate ReLU
value-and-Jacobian primitive ... generated target-shaped trace" the M176 no-go
named as the sole admissible repair. A pass opens only M176's response-free
producer work (now done); the next arrow — Source211 -> TangentState — is a
separate mutation.

## 2. Commit and hash verification

Predeclaration committed at `18f0113` (protocol) before implementation; G0
reconciliation `2fc33f6`; G1 `393f923`; G2 `0c8ecb2`; G3 `f2cc2d9`; G4
`f884265`. Base M178 provider at `3b590c6`. Artifact hashes in
`M179_SHA256SUMS_20260807.txt`.

## 3. Predeclared prediction

Per `M179_PREDECLARED_PROTOCOL_20260807.md`: the exact bivariate producer is
reachable by assembling E[ReLU_i ReLU_j] and {K,Hmu,Hv} entirely from M178's
four outputs, propagating the exact full-covariance recurrence, emitting the
m125-ABI-conformant labelled archive, with a fixed inclusive FLOP ledger and
the M175 B=8 liveness.

## 4. Formulas, strata, derivatives

Recurrence (M176), l=1..31, mu_0=0, V_0=I; a_l=mu_{l-1}W_l,
C_l=W_l^T V_{l-1} W_l. Per SPD pair, with M178 outputs K,Da,Db,Dr at
(alpha_i,alpha_j,rho): E[Z_i 1_R]=Da+rho Db; E[Z_iZ_j 1_R]=rho K -
rho(alpha_i Da+alpha_j Db)+s^2 Dr; E[ReLU_i ReLU_j] assembled therefrom;
V_l[i,j]=that - m_i m_j. Jacobian: K_ij (K_ii=p_i), Hmu (Hmu_ii=2m(1-p)),
Hv (Hv_ii=p-2mr) per the M176 carrier map. Strata: SPD via M178; zero-variance
exact limit; rank-one/non-PSD fail-closed. Diagonals via direct limits.

## 5-6. Certified remainder / correctness

Value + derivative accuracy inherited from the certified M178 provider (G1
worst assembly error 2.144e-9 vs dps-30 references; contract 2e-7). The full
recurrence matches an independent dps-30 mpmath closure to ~2e-16 on generated
MLPs (G2), and the true-network Monte Carlo at layer 1 (where the closure is
exact). The Jacobian bundle's diagonal and off-diagonal-K match dps-30
references (G3).

## 7. Inclusive fixed FLOP count

8.30B FLOPs total = 3.05% of B=2.72e11: 32,640 pairs/layer x (F_M178 4048
frozen + 42 Tallis) + FlopScope-metered matmuls (134.2M/layer) + diagonal
(0.17M/layer), x 31 layers. Fixed/bounded (no data-dependent count); no
accounting bypass (special-function work charged at the frozen F_M178, never
zero). *Production-completion note:* the ~42 scalar Tallis ops/pair are counted
in the ledger but run in plain Python (the dominant ~99.9%, F_M178 + matmul,
is metered/frozen); a production run routes the assembly through a charged
backend, as M178's Backend does.

## 8. Hostile response-free test table

Full M179 suite green: G1 premise (dps-30 + backbone + |x|), G2 recurrence
(independent closure + true-MC + symmetry + banned-marker), G3 Jacobian (ABI
conformance + strata + fail-closed + archive/carrier consumption), G4 metering
(metered matmul + ledger + liveness + no-bypass). All on generated weights.

## 9. Reproducibility

Deterministic (no RNG in the producer; generated weights fixed-seed in tests);
runs in the isolated frozen venv (Python 3.14.4, numpy 2.4.6, whestbench
0.14.0, flopscope 0.10.0, mpmath 1.3.0 references only).

## 10. Kill-gate results

None fired against the frozen producer: no clip/floor/ridge, no banned markers,
bitwise-symmetric price_kernel/V_l, non-PSD and rank-one fail-closed, inclusive
metered ledger under budget, m125 ABI conformant.

## 11. Failure tissue / preserved components

Exact rank-one (|rho|>1-2^-52) limit is DEFERRED (fail-closed; unreachable for
generic weights; a separately-gated M180-class follow-up) — preserved as a
named TODO, not a silent gap. All G0-G4 tissue preserved.

**Campaign context (not an M179 disposition):** two cheap variance-reduction
probes on the wall-priced sampling strategy returned null (N4 geometric levers;
N5 multilevel-control first-order headroom 1.07x), revalidating that the
variance lever is ~pinned and the #1 path is the native-throughput (S) lever.
M179 stands as the Algorithmic-Contribution artifact and the exact-control
component whose definitive variance test is the future M172 gate.

## 12. Dependency update / firewall / arrows

First broken link of the M176 chain — "no certified, bounded-cost, metered
exact full-covariance/Jacobian producer" — is now closed. Firewall: no
challenge instance, target, truth, scorer, leaderboard, submission, or champion
artifact read or written; generated weights only; champion unchanged. Arrows
(each separate): M179 pass -> Source211 -> TangentState conversion -> M175 B=8
trace -> (user-gated) M172 variance gate -> (sealed) efficacy. No arrow grants
a rank or winning claim. Nothing here wins, ranks, improves MSE/score, or is
ready to submit.

## Algorithmic Contribution framing

M178 (certified Phi2/Owen-T value-and-derivative provider) + M179 (exact,
metered, labelled full-covariance BackgroundArchive producer) constitute a
novel, rigorous, PDF-shaped contribution: an endpoint-complete, certified,
FlopScope-metered exact-moment provider and background recurrence for
deep-ReLU final-layer estimation — prize-eligible independent of the
Best-Score outcome.
